openapi: 3.0.3
info:
  title: Design Flow V7 API
  version: 0.90.0
  description: |
    Current v0.9 MAIN backend contract.
    Read this OpenAPI together with:
    - `docs/V0_9_BACKEND_SOURCE_OF_TRUTH.md` for route classification and compatibility governance
    - `transport/http.go` for the mounted runtime
    Current canonical families include:
    - `/v1/auth/*`
    - `/v1/users*`
    - `/v1/erp/products*`
    - `/v1/tasks*`
    - `/v1/tasks/{id}/asset-center/*`
    Compatibility and deprecated routes remain documented only so existing callers can migrate safely.
    New frontend or new integrations must not start on deprecated or compatibility-only operations.
servers:
  - url: /
tags:
  - name: ReadyForFrontend
    description: V7 endpoints ready for frontend integration in the current iteration.
  - name: ERPBridge
    description: Frontend-ready MAIN facade over the Bridge-owned ERP/JST adapter query surface. Keyword search is the primary original-product picker entry; bridge categories are auxiliary only and must not block selection when incomplete. Same-host deployment should point `ERP_BRIDGE_BASE_URL` at loopback unless a different local ingress is intentional. Bridge owns adapter query semantics and ERP mutation execution. Raw ERP mutation APIs are not exposed here as general MAIN routes.
  - name: CategoryCenter
    description: Configurable category center skeleton. Explicitly models first-level ERP search-entry semantics through `search_entry_code` and `is_search_entry`.
  - name: CategoryMappings
    description: Independent category-to-ERP positioning skeleton. Maintains first-level ERP search-entry mappings and reserved secondary/tertiary search conditions without real ERP lookup yet.
  - name: CostRules
    description: Configurable cost-rule center skeleton. Supports governed rule maintenance, lineage read models, version/effective-window traceability, and minimal preview without a full rule-expression engine or approval platform.
  - name: Workbench
    description: Frontend-facing workbench bootstrap and saved-preference APIs using session-backed user scope.
  - name: ExportCenter
    description: Export-center skeleton. Persists export jobs and static template metadata over existing stable read models without real file generation or storage.
  - name: IntegrationCenter
    description: Integration-center boundary. Persists internal API call logs plus explicit execution attempts for mostly placeholder connectors, and also carries the narrow ERP Bridge product-filing trace used by task business-info filing. This is observability for the Bridge-owned mutation boundary, not a general-purpose ERP mutation surface on MAIN.
  - name: JSTSync8082
    description: Resident JST sync service contract (port 8082). Documented here for cross-service integration clarity; not served by 8080/8081 cmd/server runtime.
  - name: InternalPlaceholder
    description: Implemented internal-only placeholder APIs. Do not treat as frontend-ready.
  - name: MockPlaceholderOnly
    description: Mock or prototype-only APIs kept for temporary flows and demos.
  - name: V6-Legacy
    description: Legacy public routes documented for compatibility or retirement tracking only. Do not use them as canonical MAIN routes or new integration targets.
components:
  schemas:
    V7Role:
      type: string
      enum: [Member, SuperAdmin, HRAdmin, OrgAdmin, RoleAdmin, TeamLead, DesignDirector, DesignReviewer, Ops, Designer, Audit_A, Audit_B, Warehouse, Outsource, ERP, Admin, DepartmentAdmin, CustomizationReviewer, CustomizationOperator]
    Department:
      type: string
      description: Dynamic backend org-master department name. Values come from enabled `org_departments` rows exposed by `/v1/org/options`; this is no longer a fixed frontend enum. v1.0 business baseline is exactly `人事部`, `运营部`, `设计研发部`, `定制美工部`, `审核部`, `云仓部` (plus the system bucket `未分配`). Legacy names (`设计部`, `采购部`, `仓储部`, `烘焙仓储部`) remain only as disabled compatibility rows for historical integrity and are not returned by `/v1/org/options` nor accepted by registration / user admin / task create inputs.
    PlaceholderAuthMode:
      type: string
      enum: [debug_header_role_enforced, session_token_role_enforced]
    RequestActor:
      type: object
      properties:
        id: { type: integer }
        username: { type: string }
        roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        source: { type: string }
        auth_mode:
          $ref: '#/components/schemas/PlaceholderAuthMode'
    RouteAccessRule:
      type: object
      properties:
        method: { type: string }
        path: { type: string }
        readiness:
          $ref: '#/components/schemas/APIReadiness'
        required_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        auth_mode:
          $ref: '#/components/schemas/PlaceholderAuthMode'
        session_required:
          type: boolean
        debug_compatible:
          type: boolean
    RuleTemplate:
      type: object
      properties:
        id: { type: integer }
        template_type: { type: string, enum: [cost-pricing, product-code, short-name] }
        config_json: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    Incident:
      type: object
      properties:
        id: { type: integer }
        sku_id: { type: integer }
        job_id: { type: integer, nullable: true }
        status: { type: string }
        reason: { type: string }
        assignee_id: { type: integer, nullable: true }
        resolved_by: { type: integer, nullable: true }
        resolved_at: { type: string, format: date-time, nullable: true }
        closed_by: { type: integer, nullable: true }
        closed_at: { type: string, format: date-time, nullable: true }
        close_reason: { type: string, nullable: true }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    SystemPolicy:
      type: object
      properties:
        id: { type: integer }
        key: { type: string }
        value: { type: string }
        version: { type: integer }
        updated_by: { type: integer }
        updated_at: { type: string, format: date-time }
    SKU:
      type: object
      properties:
        id: { type: integer }
        sku_code: { type: string }
        name: { type: string }
        workflow_status: { type: string }
        current_ver_id: { type: integer, nullable: true }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    SKUSyncStatusResult:
      type: object
      properties:
        sku:
          $ref: '#/components/schemas/SKU'
        latest_sequence: { type: integer }
        events:
          type: array
          items:
            type: object
            additionalProperties: true
    JSTUserImportResult:
      type: object
      properties:
        fetched_count: { type: integer }
        created_count: { type: integer }
        updated_count: { type: integer }
        disabled_count: { type: integer }
        mapping_failed_count: { type: integer }
        trace_updated_count: { type: integer }
        roles_written: { type: integer }
        errors:
          type: array
          items: { type: string }
    JSTUserImportPreviewResult:
      type: object
      properties:
        fetched_count: { type: integer }
        matched_count: { type: integer }
        to_create_count: { type: integer }
        to_update_count: { type: integer }
        org_mapping_missed:
          type: array
          items: { type: object, additionalProperties: true }
        role_mapping_missed:
          type: array
          items: { type: object, additionalProperties: true }
        risks:
          type: array
          items: { type: object, additionalProperties: true }
        preview_items:
          type: array
          items: { type: object, additionalProperties: true }
    AgentSyncResult:
      type: object
      properties:
        asset_version_id: { type: integer, nullable: true }
    PullJobResult:
      type: object
      properties:
        attempt_id: { type: integer, nullable: true }
        job:
          type: object
          additionalProperties: true
        lease_expires_at: { type: string, format: date-time, nullable: true }
    HeartbeatResult:
      type: object
      properties:
        lease_expires_at: { type: string, format: date-time, nullable: true }
    AuditSubmitResult:
      type: object
      properties:
        action: { type: string }
        jobs:
          type: array
          items: { type: object, additionalProperties: true }
    BatchTaskActionResult:
      type: object
      properties:
        batch_request_id: { type: string }
        total: { type: integer }
        succeeded: { type: integer }
        failed: { type: integer }
        items:
          type: array
          items: { type: object, additionalProperties: true }
    UserStatus:
      type: string
      enum: [active, disabled]
    EmploymentType:
      type: string
      enum: [full_time, part_time]
    JSTUser:
      type: object
      description: "JST getcompanyusers item. Source: domain.JSTUser."
      properties:
        u_id:
          type: integer
        name:
          type: string
        loginId:
          type: string
        enabled:
          type: boolean
        created:
          type: string
        modified:
          type: string
        last_login_time:
          type: string
        pwd_modified:
          type: string
        remark:
          type: string
        role_ids:
          type: string
        roles:
          type: string
        ug_ids:
          type: string
        ug_names:
          type: array
          items:
            type: string
        creator:
          type: string
        modifier:
          type: string
        empId:
          type: string
    JSTUserListResponse:
      type: object
      description: "ERP-style JST getcompanyusers response. Source: domain.JSTUserListResponse."
      properties:
        current_page:
          type: string
        page_size:
          type: string
        count:
          type: string
        pages:
          type: string
        datas:
          type: array
          items:
            $ref: '#/components/schemas/JSTUser'
    WorkflowUser:
      type: object
      properties:
        id: { type: integer }
        username: { type: string }
        account: { type: string }
        display_name: { type: string }
        name: { type: string }
        department:
          $ref: '#/components/schemas/Department'
        team: { type: string }
        group: { type: string, description: Compatibility alias of team }
        mobile: { type: string }
        phone: { type: string }
        email: { type: string, nullable: true }
        managed_departments:
          type: array
          items:
            type: string
        managed_teams:
          type: array
          items:
            type: string
        status:
          $ref: '#/components/schemas/UserStatus'
        employment_type:
          $ref: '#/components/schemas/EmploymentType'
        roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        last_login_at:
          type: string
          format: date-time
          nullable: true
        jst_u_id:
          type: integer
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        frontend_access:
          $ref: '#/components/schemas/FrontendAccess'
    CreateManagedUserRequest:
      type: object
      required: [username, display_name, department, team, mobile, password]
      properties:
        username:
          type: string
        account:
          type: string
          description: Compatibility alias of username.
        display_name:
          type: string
        name:
          type: string
          description: Compatibility alias of display_name.
        department:
          type: string
          description: Must match one enabled department from backend org master exposed by `/v1/org/options`.
        team:
          type: string
          description: Must match one enabled team under the selected department in backend org master exposed by `/v1/org/options`.
        group:
          type: string
          description: Compatibility alias of team.
        mobile:
          type: string
        phone:
          type: string
          description: Compatibility alias of mobile.
        email:
          type: string
          nullable: true
        password:
          type: string
        roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        status:
          $ref: '#/components/schemas/UserStatus'
        employment_type:
          $ref: '#/components/schemas/EmploymentType'
    ResetUserPasswordRequest:
      type: object
      required: [password]
      properties:
        password:
          type: string
    FrontendAccess:
      type: object
      properties:
        is_super_admin:
          type: boolean
        is_department_admin:
          type: boolean
        department:
          type: string
        team:
          type: string
        roles:
          type: array
          items:
            type: string
        scopes:
          type: array
          items:
            type: string
        menus:
          type: array
          items:
            type: string
        pages:
          type: array
          items:
            type: string
        actions:
          type: array
          items:
            type: string
        modules:
          type: array
          items:
            type: string
        managed_departments:
          type: array
          items:
            type: string
        permission_flags:
          type: array
          items:
            type: string
        page_keys:
          type: array
          items:
            type: string
        menu_keys:
          type: array
          items:
            type: string
        module_keys:
          type: array
          items:
            type: string
        access_scopes:
          type: array
          items:
            type: string
    DepartmentOption:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          $ref: '#/components/schemas/Department'
        teams:
          type: array
          items:
            type: string
        team_items:
          type: array
          items:
            $ref: '#/components/schemas/OrgTeamOption'
        enabled:
          type: boolean
    OrgTeamOption:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        enabled:
          type: boolean
    OrgDepartment:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        enabled:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    OrgTeam:
      type: object
      properties:
        id:
          type: integer
          format: int64
        department_id:
          type: integer
          format: int64
        department:
          type: string
        name:
          type: string
        enabled:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CreateOrgDepartmentRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
          description: Unique department name in backend org master.
    UpdateOrgDepartmentRequest:
      type: object
      required: [enabled]
      properties:
        enabled:
          type: boolean
          description: Current backend update semantic is enable/disable only.
    CreateOrgTeamRequest:
      type: object
      required: [name]
      anyOf:
        - required: [department_id]
        - required: [department]
      properties:
        department_id:
          type: integer
          format: int64
          description: Backend org department id. Optional when `department` is provided.
        department:
          type: string
          description: Backend org department name. Optional when `department_id` is provided.
        name:
          type: string
          description: Globally unique team name in backend org master.
    UpdateOrgTeamRequest:
      type: object
      required: [enabled]
      properties:
        enabled:
          type: boolean
          description: Current backend update semantic is enable/disable only.
    RegistrationOptions:
      type: object
      properties:
        departments:
          type: array
          items:
            $ref: '#/components/schemas/DepartmentOption'
    ConfiguredUserAssignment:
      type: object
      properties:
        username:
          type: string
          nullable: true
        display_name:
          type: string
          nullable: true
        department:
          $ref: '#/components/schemas/Department'
        team:
          type: string
        roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        managed_departments:
          type: array
          items:
            type: string
        managed_teams:
          type: array
          items:
            type: string
        status:
          $ref: '#/components/schemas/UserStatus'
    OrgOptions:
      type: object
      description: Canonical org-options payload. `departments[].teams` is the authoritative shape as of v1.8.
      properties:
        departments:
          type: array
          description: Canonical department tree. Each department entry carries its nested `teams` array.
          items:
            $ref: '#/components/schemas/DepartmentOption'
        teams_by_department:
          type: object
          deprecated: true
          description: "Deprecated compatibility mirror retained in v1.8. Clients should switch to `departments[].teams`. Responses that include this field emit `Deprecation: version=\"v1.8\"`."
          additionalProperties:
            type: array
            items:
              type: string
        role_catalog_summary:
          type: array
          items:
            $ref: '#/components/schemas/RoleCatalogEntry'
        unassigned_pool_enabled:
          type: boolean
        configured_assignments:
          type: array
          items:
            $ref: '#/components/schemas/ConfiguredUserAssignment'
    AuthSession:
      type: object
      properties:
        session_id: { type: string }
        token: { type: string }
        token_type: { type: string, example: Bearer }
        expires_at:
          type: string
          format: date-time
    AuthResult:
      type: object
      properties:
        user:
          $ref: '#/components/schemas/WorkflowUser'
        session:
          $ref: '#/components/schemas/AuthSession'
    RoleCatalogEntry:
      type: object
      properties:
        role:
          $ref: '#/components/schemas/V7Role'
        name: { type: string }
        description: { type: string }
        capabilities:
          type: array
          items:
            type: string
    PermissionLog:
      type: object
      properties:
        id: { type: integer }
        actor_id:
          type: integer
          nullable: true
        actor_username: { type: string }
        actor_source: { type: string }
        auth_mode:
          $ref: '#/components/schemas/PlaceholderAuthMode'
        readiness:
          $ref: '#/components/schemas/APIReadiness'
        session_required:
          type: boolean
        debug_compatible:
          type: boolean
        action_type: { type: string }
        actor_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        target_user_id:
          type: integer
          nullable: true
        target_username: { type: string }
        target_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        method: { type: string }
        route_path: { type: string }
        required_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        granted: { type: boolean }
        reason: { type: string }
        created_at:
          type: string
          format: date-time
    OperationLogSource:
      type: string
      enum: [task_event, export_event, integration_call]
    OperationLogEntry:
      type: object
      properties:
        source:
          $ref: '#/components/schemas/OperationLogSource'
        log_id: { type: string }
        reference_type: { type: string }
        reference_id: { type: string }
        event_type: { type: string }
        summary: { type: string }
        actor_id:
          type: integer
          nullable: true
        actor_type: { type: string }
        status: { type: string }
        payload: {}
        created_at:
          type: string
          format: date-time
    PolicyMode:
      type: string
      description: |
        Policy scaffolding mode.
        Indicates route-role visibility scaffolding only.
        This does not imply an identity provider, org sync, or full RBAC/ABAC engine.
      enum: [route_role_visibility_scaffolding]
    PolicyAPISurface:
      type: string
      description: Endpoint exposure classification used by policy scaffolding action summaries.
      enum: [frontend_ready, internal, admin, mock_placeholder]
    ActionPolicySummary:
      type: object
      description: Default action-level policy summary for scaffolding only.
      properties:
        action_key:
          type: string
        allowed_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        api_surface:
          $ref: '#/components/schemas/PolicyAPISurface'
        note:
          type: string
    ResourceAccessPolicy:
      type: object
      description: Default resource-level policy summary for scaffolding only.
      properties:
        resource_key:
          type: string
        visible_to_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        action_roles:
          type: array
          items:
            $ref: '#/components/schemas/ActionPolicySummary'
        policy_mode:
          $ref: '#/components/schemas/PolicyMode'
    PolicyScopeSummary:
      type: object
      description: Cross-center policy scaffolding envelope. Not a real auth/org system.
      properties:
        scope_key:
          type: string
        scope_name:
          type: string
        auth_mode:
          $ref: '#/components/schemas/PlaceholderAuthMode'
        policy_mode:
          $ref: '#/components/schemas/PolicyMode'
        resource_access_policy:
          $ref: '#/components/schemas/ResourceAccessPolicy'
        is_placeholder:
          type: boolean
        note:
          type: string
    PlatformEntryMode:
      type: string
      description: |
        Platform-entry boundary mode placeholder.
        This does not imply a BI platform, finance/accounting system, or report engine.
      enum: [scaffolding_only]
    PlatformEntryStatus:
      type: string
      description: Entry-readiness status for one platform-entry placeholder lane.
      enum: [candidate, conditional, not_applicable]
    KPIEntrySummary:
      type: object
      description: Placeholder KPI entry summary for future KPI docking. Not a real KPI computation module.
      properties:
        entry_status:
          $ref: '#/components/schemas/PlatformEntryStatus'
        eligible_now:
          type: boolean
        source_read_model_fields:
          type: array
          items:
            type: string
        placeholder_fields:
          type: array
          items:
            type: string
        future_metric_hints:
          type: array
          items:
            type: string
        is_placeholder:
          type: boolean
        not_ready_blocking_reason:
          type: string
        note:
          type: string
    FinanceEntrySummary:
      type: object
      description: Placeholder finance entry summary for future finance docking. Not a real accounting/reconciliation/settlement/invoice system.
      properties:
        entry_status:
          $ref: '#/components/schemas/PlatformEntryStatus'
        eligible_now:
          type: boolean
        source_read_model_fields:
          type: array
          items:
            type: string
        placeholder_fields:
          type: array
          items:
            type: string
        future_finance_scope_hints:
          type: array
          items:
            type: string
        is_placeholder:
          type: boolean
        not_ready_blocking_reason:
          type: string
        note:
          type: string
    ReportEntrySummary:
      type: object
      description: Placeholder report/export entry summary for future report-platform docking. Not a real report-generation engine.
      properties:
        entry_status:
          $ref: '#/components/schemas/PlatformEntryStatus'
        eligible_now:
          type: boolean
        source_read_model_fields:
          type: array
          items:
            type: string
        placeholder_fields:
          type: array
          items:
            type: string
        future_report_scope_hints:
          type: array
          items:
            type: string
        is_placeholder:
          type: boolean
        not_ready_blocking_reason:
          type: string
        note:
          type: string
    PlatformEntryBoundary:
      type: object
      description: Unified platform-entry boundary envelope reused across task, procurement, cost-governance, and export read models.
      properties:
        entry_mode:
          $ref: '#/components/schemas/PlatformEntryMode'
        scope_key:
          type: string
        scope_name:
          type: string
        source_object:
          type: string
        source_layer:
          type: string
        kpi_entry_summary:
          allOf: [{ $ref: '#/components/schemas/KPIEntrySummary' }]
          nullable: true
        finance_entry_summary:
          allOf: [{ $ref: '#/components/schemas/FinanceEntrySummary' }]
          nullable: true
        report_entry_summary:
          allOf: [{ $ref: '#/components/schemas/ReportEntrySummary' }]
          nullable: true
        is_placeholder:
          type: boolean
        note:
          type: string
    PaginationMeta:
      type: object
      properties:
        page: { type: integer }
        page_size: { type: integer }
        total: { type: integer }
    ServerLog:
      type: object
      properties:
        id: { type: integer }
        level: { type: string }
        msg: { type: string }
        details: { type: object }
        created_at: { type: string, format: date-time }
    TaskBoardView:
      type: string
      enum: [all, ops, designer, audit, procurement, warehouse]
    TaskMainStatus:
      type: string
      enum: [draft, created, filed, pending_warehouse_receive, warehouse_processing, pending_close, closed]
    TaskSubStatusCode:
      type: string
      enum:
        - not_required
        - not_triggered
        - not_started
        - pending_design
        - designing
        - rework_required
        - pending_audit
        - in_review
        - rejected
        - outsourcing
        - outsourced
        - preparing
        - ready
        - in_progress
        - pending_inbound
        - pending_receive
        - received
        - completed
        - pending_review
        - final_ready
        - approved
        - reserved
    TaskSubStatusSource:
      type: string
      enum: [task_type, task_status, task_asset, warehouse_receipt, procurement_record, reserved]
    TaskSubStatusItem:
      type: object
      required: [code, label, source]
      properties:
        code:
          $ref: '#/components/schemas/TaskSubStatusCode'
        label:
          type: string
        source:
          $ref: '#/components/schemas/TaskSubStatusSource'
    TaskSubStatusSnapshot:
      type: object
      properties:
        design:
          $ref: '#/components/schemas/TaskSubStatusItem'
        audit:
          $ref: '#/components/schemas/TaskSubStatusItem'
        procurement:
          $ref: '#/components/schemas/TaskSubStatusItem'
        warehouse:
          $ref: '#/components/schemas/TaskSubStatusItem'
        customization:
          $ref: '#/components/schemas/TaskSubStatusItem'
        outsource:
          description: Compatibility alias of `customization` for legacy sub-status consumers.
          $ref: '#/components/schemas/TaskSubStatusItem'
        production:
          $ref: '#/components/schemas/TaskSubStatusItem'
    TaskSubStatusScope:
      type: string
      description: Canonical customization scope is `customization`; `outsource` remains a compatibility alias.
      enum: [design, audit, procurement, warehouse, customization, outsource, production]
    WorkflowReason:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
          enum:
            - task_not_found
            - task_detail_missing
            - task_already_pending_warehouse
            - task_already_closed
            - task_awaiting_close
            - task_blocked
            - warehouse_already_received
            - warehouse_already_completed
            - missing_final_design_asset
            - audit_not_approved
            - missing_task_no
            - missing_sku
            - warehouse_not_received
            - warehouse_rejected_pending_resolution
            - warehouse_not_completed
            - pending_exception_resolution
            - filed_at_missing
            - category_missing
            - spec_text_missing
            - cost_price_missing
            - procurement_record_missing
            - procurement_price_missing
            - procurement_quantity_missing
            - procurement_not_ready
            - not_pending_close
        message:
          type: string
    TaskWorkflowSnapshot:
      type: object
      properties:
        main_status:
          $ref: '#/components/schemas/TaskMainStatus'
        sub_status:
          $ref: '#/components/schemas/TaskSubStatusSnapshot'
        can_prepare_warehouse: { type: boolean }
        warehouse_blocking_reasons:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowReason'
        can_close: { type: boolean }
        closable: { type: boolean }
        cannot_close_reasons:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowReason'
    Product:
      type: object
      properties:
        id: { type: integer }
        erp_product_id: { type: string }
        sku_code: { type: string }
        product_name: { type: string }
        category: { type: string }
        spec_json: { type: string }
        status: { type: string }
        source_updated_at: { type: string, format: date-time, nullable: true }
        sync_time: { type: string, format: date-time, nullable: true }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    ProductSearchMatchedMapping:
      type: object
      properties:
        mapping_id: { type: integer }
        category_code: { type: string }
        search_entry_code: { type: string }
        erp_match_type:
          $ref: '#/components/schemas/CategoryERPMatchType'
        erp_match_value: { type: string }
        secondary_condition_key: { type: string }
        secondary_condition_value: { type: string }
        tertiary_condition_key: { type: string }
        tertiary_condition_value: { type: string }
        is_primary: { type: boolean }
        priority: { type: integer }
    ProductSearchResult:
      allOf:
        - $ref: '#/components/schemas/Product'
        - type: object
          properties:
            matched_category_code:
              type: string
              description: Category code of the mapping rule that matched this local ERP product.
            matched_search_entry_code:
              type: string
              description: First-level search-entry code used to position this local ERP product.
            matched_mapping_rule:
              $ref: '#/components/schemas/ProductSearchMatchedMapping'
    TaskProductSelectionSummary:
      type: object
      description: Lightweight original-product provenance summary for task list, task-board, and procurement-summary consumption. Local mapped-search provenance remains supported, and an additive ERP Bridge snapshot may be included for the selected external product.
      properties:
        selected_product_id:
          type: integer
          nullable: true
        selected_product_name: { type: string }
        selected_product_sku_code: { type: string }
        matched_category_code:
          type: string
          description: Category code that ultimately matched the chosen local ERP product.
        matched_search_entry_code:
          type: string
          description: First-level search-entry code used while locating the chosen local ERP product.
        source_product_id:
          type: integer
          nullable: true
        source_product_name: { type: string }
        source_match_type:
          type: string
          description: |
            Stable provenance source label.
            Typical values include `mapped_product_search`, `manual_existing_product_binding`,
            `legacy_existing_product_binding`, and ERP-origin labels such as `erp_bridge_keyword_search`.
            There is no separate `product_selection.erp_product.source`; use this field as the source indicator.
        source_match_rule:
          type: string
          description: Stable lightweight rule/value summary for the local provenance path.
        source_search_entry_code:
          type: string
          description: Persisted first-level ERP search-entry code used before the final product was selected.
        erp_product:
          allOf: [{ $ref: '#/components/schemas/ERPBridgeProductSnapshot' }]
          description: Primary SKU nested ERP snapshot object. Read nested sku/product fields from this object.
          nullable: true
    TaskProductSelectionContext:
      type: object
      description: Full original-product provenance contract for task read and detail views. It extends the lightweight summary with the local matched mapping snapshot and an additive ERP Bridge product snapshot.
      properties:
        selected_product_id:
          type: integer
          nullable: true
        selected_product_name: { type: string }
        selected_product_sku_code: { type: string }
        matched_category_code:
          type: string
          description: Category code returned by `GET /v1/products/search` for the matched local mapping rule.
        matched_search_entry_code:
          type: string
          description: First-level search-entry code returned by `GET /v1/products/search` for original-product picker narrowing.
        matched_mapping_rule:
          allOf: [{ $ref: '#/components/schemas/ProductSearchMatchedMapping' }]
          nullable: true
        source_product_id:
          type: integer
          nullable: true
        source_product_name: { type: string }
        source_match_type:
          type: string
          description: |
            Trace label describing how the selected existing product was located.
            Typical values include `mapped_product_search`, `manual_existing_product_binding`,
            `legacy_existing_product_binding`, and ERP-origin labels such as `erp_bridge_keyword_search`.
            There is no separate `product_selection.erp_product.source`; use this field as the source indicator.
        source_match_rule:
          type: string
          description: Lightweight trace string for the applied product-match rule. For mapped search this normally mirrors the matched mapping value.
        source_search_entry_code:
          type: string
          description: Persisted first-level ERP search-entry code used before the concrete product was selected.
        erp_product:
          allOf: [{ $ref: '#/components/schemas/ERPBridgeProductSnapshot' }]
          description: Primary SKU nested ERP snapshot object. Read nested sku/product fields from this object.
          nullable: true
    ERPBridgeProduct:
      type: object
      description: Normalized ERP product contract returned by the MAIN facade. `product_id` is the stable lookup key emitted by `GET /v1/erp/products` and accepted by `GET /v1/erp/products/{id}`. Empty strings mean the backend could not obtain that field from bridge/category normalization; frontend should render its own placeholder for empty `image_url`.
      properties:
        product_id:
          type: string
          description: Opaque ERP facade lookup key returned by list and accepted by detail/binding flows.
        sku_id:
          type: string
          description: Canonical ERP SKU identifier.
        i_id:
          type: string
          description: Canonical ERP style identifier.
        sku_code:
          type: string
          description: Compatibility display code. This is not equivalent to `sku_id` or `i_id`.
        name:
          type: string
          description: Canonical ERP product display name.
        product_name: { type: string }
        short_name:
          type: string
          description: Canonical ERP short name.
        category_id:
          type: string
          description: Canonical category id resolved by MAIN when the category catalog can map the item; otherwise empty string.
        category_code:
          type: string
          description: Current minimal category code for result-page display/filter echo. When available it equals the resolved category id.
        category_name:
          type: string
          description: Display-ready category name. Empty string means bridge/category normalization had no category match.
        product_short_name:
          type: string
          description: Optional upstream short name. Empty string means bridge did not expose one.
        image_url:
          type: string
          description: Display image URL. Empty string means no image was available and frontend should render its own placeholder.
        price:
          type: number
          format: double
          nullable: true
        s_price:
          type: number
          format: double
          nullable: true
        wms_co_id:
          type: string
          description: Warehouse dimension code resolved from ERP.
        currency: { type: string }
    ERPBridgeProductSnapshot:
      allOf: [{ $ref: '#/components/schemas/ERPBridgeProduct' }]
      description: Persisted ERP product snapshot stored inside task `product_selection`. This additive snapshot preserves the selected ERP lookup key plus display fields used by task bind/replay, even though task binding still lands on a local cached `products.id`.
    ERPBridgeProductSearchFilter:
      type: object
      description: "Normalized `/v1/erp/products` query echo. Supported filters are keyword contains-match, exact `sku_code`, exact `category_id` / `category_name`, and browse. Global search, suggestion, and sorting are not supported."
      properties:
        q:
          type: string
        keyword:
          type: string
        sku_code:
          type: string
        category_id:
          type: string
        category_name:
          type: string
        page:
          type: integer
        page_size:
          type: integer
        query_mode:
          type: string
          description: Current normalized search mode such as `keyword`, `sku_code`, `category_auxiliary`, or `browse`.
    ERPIIDOption:
      type: object
      description: Frontend selection option for Jushuitan product style code `i_id`. This is the canonical business selector for new-product and purchase-task creation; `category_code` remains backend-owned compatibility metadata.
      properties:
        i_id: { type: string }
        label: { type: string }
        category: { type: string }
        category_name: { type: string }
        product_count: { type: integer, format: int64 }
    ERPIIDListFilter:
      type: object
      properties:
        q: { type: string }
        page: { type: integer }
        page_size: { type: integer }
    ERPBridgeCategory:
      type: object
      description: Normalized ERP category contract used by MAIN to validate exact category_id / category_name filters.
      properties:
        category_id: { type: string }
        category_name: { type: string }
        parent_id: { type: string }
        level: { type: integer }
    ERPProductUpsertPayload:
      type: object
      description: |
        Write contract sent to Bridge `POST /v1/erp/products/upsert`锛堟櫘閫氬晢鍝佽祫鏂欎笂浼?鏇存柊锛夈€?        鏉冨▉璇箟锛歚sku_id` 鏄晢鍝佸敮涓€缂栫爜锛宍i_id` 鏄寮忕紪鐮侊紝浜岃€呬笉鍙贩娣嗐€?      required: [sku_id]
      properties:
        product_id: { type: string }
        sku_id: { type: string }
        i_id: { type: string }
        sku_code: { type: string, description: 鍘嗗彶鍏煎瀛楁 }
        name: { type: string }
        product_name: { type: string, description: 鍘嗗彶鍏煎瀛楁 }
        short_name: { type: string }
        category_id: { type: string }
        category_code: { type: string }
        category_name: { type: string }
        product_short_name: { type: string, description: 鍘嗗彶鍏煎瀛楁 }
        image_url: { type: string }
        price: { type: number, nullable: true }
        s_price: { type: number, nullable: true }
        remark: { type: string }
        cost_price: { type: number, nullable: true }
        supplier_name: { type: string }
        wms_co_id: { type: string }
        brand: { type: string }
        vc_name: { type: string }
        item_type: { type: string }
        pic: { type: string }
        pic_big: { type: string }
        sku_pic: { type: string }
        properties_value: { type: string }
        weight: { type: number, nullable: true }
        l: { type: number, nullable: true }
        w: { type: number, nullable: true }
        h: { type: number, nullable: true }
        enabled: { type: boolean, nullable: true }
        supplier_sku_id: { type: string }
        supplier_i_id: { type: string }
        market_price: { type: number, nullable: true }
        other_price_1: { type: number, nullable: true }
        other_price_2: { type: number, nullable: true }
        other_price_3: { type: number, nullable: true }
        other_price_4: { type: number, nullable: true }
        other_price_5: { type: number, nullable: true }
        other_1: { type: string }
        other_2: { type: string }
        other_3: { type: string }
        other_4: { type: string }
        other_5: { type: string }
        stock_disabled: { type: boolean, nullable: true }
        operation: { type: string, description: 'product_profile_upsert or original_product_update' }
        sku_immutable: { type: boolean, nullable: true, description: 'Whether SKU binding stays immutable for original-product updates.' }
        auto_generate_short_name: { type: boolean, nullable: true }
        short_name_template_key: { type: string }
        currency: { type: string }
        source: { type: string, description: 'Origin context, e.g. task_business_info_filing' }
        product:
          type: object
          description: Full ERP product snapshot from product_selection.erp_product binding
          properties:
            product_id: { type: string }
            sku_id: { type: string }
            i_id: { type: string }
            sku_code: { type: string }
            name: { type: string }
            product_name: { type: string }
            short_name: { type: string }
            product_short_name: { type: string }
            category_id: { type: string }
            category_code: { type: string }
            category_name: { type: string }
            image_url: { type: string }
            price: { type: number, nullable: true }
            s_price: { type: number, nullable: true }
            wms_co_id: { type: string }
            currency: { type: string }
        task_context:
          type: object
          properties:
            task_id: { type: integer }
            task_no: { type: string }
            task_type: { type: string }
            source_mode: { type: string }
            filed_at: { type: string, format: date-time }
            operator_id: { type: integer }
            remark: { type: string }
        business_info:
          type: object
          properties:
            category: { type: string }
            category_code: { type: string }
            category_name: { type: string }
            spec_text: { type: string }
            material: { type: string }
            size_text: { type: string }
            craft_text: { type: string }
            process: { type: string }
            width: { type: number, nullable: true }
            height: { type: number, nullable: true }
            area: { type: number, nullable: true }
            quantity: { type: integer, nullable: true }
            cost_price: { type: number, nullable: true }
    ERPProductUpsertResult:
      type: object
      description: Normalized Bridge write result. Tolerant schema because upstream bridge may expose several response envelopes.
      properties:
        product_id: { type: string }
        sku_id: { type: string }
        i_id: { type: string }
        sku_code: { type: string }
        name: { type: string }
        product_name: { type: string }
        short_name: { type: string }
        category_id: { type: string }
        category_code: { type: string }
        category_name: { type: string }
        product_short_name: { type: string }
        s_price: { type: number, nullable: true }
        wms_co_id: { type: string }
        route: { type: string, description: 瀹為檯 Bridge 璺敱锛屽 itemskubatchupload }
        sync_log_id: { type: string }
        status: { type: string, description: 'e.g. accepted, stored locally, synced' }
        message: { type: string }
    ERPItemStyleUpdatePayload:
      type: object
      description: Style-update payload written through `POST /v1/erp/products/style/update`.
      required: [i_id]
      properties:
        sku_id: { type: string }
        i_id: { type: string }
        name: { type: string }
        short_name: { type: string }
        category_name: { type: string }
        pic: { type: string }
        pic_big: { type: string }
        sku_pic: { type: string }
        properties_value: { type: string }
        brand: { type: string }
        vc_name: { type: string }
        supplier_i_id: { type: string }
        enabled: { type: boolean, nullable: true }
        operation: { type: string }
        source: { type: string }
        auto_generate_short_name: { type: boolean, nullable: true }
        short_name_template_key: { type: string }
        task_context:
          type: object
          properties:
            task_id: { type: integer }
            task_no: { type: string }
            task_type: { type: string }
            source_mode: { type: string }
            filed_at: { type: string, format: date-time }
            operator_id: { type: integer }
            remark: { type: string }
    ERPItemStyleUpdateResult:
      type: object
      properties:
        sku_id: { type: string }
        i_id: { type: string }
        name: { type: string }
        short_name: { type: string }
        route: { type: string }
        sync_log_id: { type: string }
        status: { type: string }
        message: { type: string }
    ERPSyncLog:
      type: object
      description: Bridge-side ERP write sync log record.
      properties:
        sync_log_id: { type: string }
        connector: { type: string }
        operation: { type: string }
        status: { type: string }
        message: { type: string }
        resource_type: { type: string }
        resource_id:
          type: integer
          nullable: true
        product_id: { type: string }
        sku_code: { type: string }
        request_payload:
          type: object
          nullable: true
          additionalProperties: true
        response_payload:
          type: object
          nullable: true
          additionalProperties: true
        error_message: { type: string }
        created_at: { type: string, format: date-time, nullable: true }
        updated_at: { type: string, format: date-time, nullable: true }
    ERPProductBatchMutationItem:
      type: object
      properties:
        product_id: { type: string }
        sku_id: { type: string }
        i_id: { type: string }
        sku_code: { type: string }
        name: { type: string }
        wms_co_id: { type: string }
        bin_id: { type: string }
        carry_id: { type: string }
        box_no: { type: string }
        qty: { type: integer, nullable: true }
    ERPProductBatchMutationPayload:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ERPProductBatchMutationItem'
        reason: { type: string }
        source: { type: string }
    ERPProductBatchMutationResult:
      type: object
      properties:
        action: { type: string }
        total: { type: integer }
        accepted: { type: integer }
        rejected: { type: integer }
        sync_log_id: { type: string }
        status: { type: string }
        message: { type: string }
    ERPVirtualInventoryUpdateItem:
      type: object
      required: [virtual_qty]
      properties:
        product_id: { type: string }
        sku_id: { type: string }
        i_id: { type: string }
        sku_code: { type: string }
        warehouse_code: { type: string }
        wms_co_id: { type: string }
        virtual_qty: { type: integer }
    ERPWarehouse:
      type: object
      properties:
        name: { type: string }
        wms_co_id: { type: string }
        warehouse_type: { type: string }
    ERPVirtualInventoryUpdatePayload:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ERPVirtualInventoryUpdateItem'
        reason: { type: string }
        source: { type: string }
    ERPVirtualInventoryUpdateResult:
      type: object
      properties:
        total: { type: integer }
        accepted: { type: integer }
        rejected: { type: integer }
        sync_log_id: { type: string }
        status: { type: string }
        message: { type: string }
    JSTSyncPingResponse:
      type: object
      properties:
        status: { type: string }
        service: { type: string }
    JSTIncrementalSyncResponse:
      type: object
      properties:
        status: { type: string }
        run_id: { type: string }
        message: { type: string }
    CategoryType:
      type: string
      enum: [coded_style, board, paper, print, cloth, material, custom, manual_quote, other]
    Category:
      type: object
      properties:
        category_id: { type: integer }
        category_code: { type: string }
        category_name: { type: string }
        display_name: { type: string }
        parent_id:
          type: integer
          nullable: true
        level: { type: integer }
        search_entry_code:
          type: string
          description: Explicit first-level ERP search-entry code. For current top-level categories this equals `category_code`; later second/third-level categories inherit the same entry code.
        is_search_entry:
          type: boolean
          description: Whether this category itself is a first-level ERP search entry.
        category_type:
          $ref: '#/components/schemas/CategoryType'
        is_active: { type: boolean }
        sort_order: { type: integer }
        source: { type: string }
        remark: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    CategoryERPMatchType:
      type: string
      enum: [category_code, product_family, sku_prefix, keyword, external_id]
    CategoryERPMapping:
      type: object
      properties:
        mapping_id: { type: integer }
        category_id:
          type: integer
          nullable: true
        category_code: { type: string }
        search_entry_code:
          type: string
          description: Explicit first-level ERP search-entry code used before later second/third-level conditions are overlaid.
        erp_match_type:
          $ref: '#/components/schemas/CategoryERPMatchType'
        erp_match_value: { type: string }
        secondary_condition_key:
          type: string
          description: Reserved second-level search-condition key for later ERP/product search refinement.
        secondary_condition_value:
          type: string
          description: Reserved second-level search-condition value for later ERP/product search refinement.
        tertiary_condition_key:
          type: string
          description: Reserved third-level search-condition key for later ERP/product search refinement.
        tertiary_condition_value:
          type: string
          description: Reserved third-level search-condition value for later ERP/product search refinement.
        is_primary: { type: boolean }
        is_active: { type: boolean }
        priority: { type: integer }
        source: { type: string }
        remark: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    CostRuleType:
      type: string
      enum: [fixed_unit_price, area_threshold_surcharge, minimum_billable_area, size_based_formula, manual_quote, special_process_surcharge]
    CostRuleGovernanceStatus:
      type: string
      description: Governance/effective-window status for one persisted cost-rule row. This is not an approval-state machine.
      enum: [inactive, scheduled, effective, expired, no_match]
    CostRuleVersionRef:
      type: object
      properties:
        rule_id: { type: integer }
        rule_name: { type: string }
        rule_version: { type: integer }
        governance_status:
          $ref: '#/components/schemas/CostRuleGovernanceStatus'
        effective_from:
          type: string
          format: date-time
          nullable: true
        effective_to:
          type: string
          format: date-time
          nullable: true
        source: { type: string }
    CostRuleVersionChainSummary:
      type: object
      properties:
        root_rule_id: { type: integer }
        root_rule_version: { type: integer }
        latest_rule_id: { type: integer }
        latest_rule_version: { type: integer }
        total_versions: { type: integer }
        supersession_depth: { type: integer }
        is_latest_version: { type: boolean }
    CostRule:
      type: object
      properties:
        rule_id: { type: integer }
        rule_name: { type: string }
        rule_version:
          type: integer
          description: Additive rule-lineage version number for the current `cost_rules` row. This is skeleton governance metadata, not a separate version table.
        category_id:
          type: integer
          nullable: true
        category_code: { type: string }
        product_family: { type: string }
        rule_type:
          $ref: '#/components/schemas/CostRuleType'
        base_price:
          type: number
          format: double
          nullable: true
        tax_multiplier:
          type: number
          format: double
          nullable: true
        min_area:
          type: number
          format: double
          nullable: true
        area_threshold:
          type: number
          format: double
          nullable: true
        surcharge_amount:
          type: number
          format: double
          nullable: true
        special_process_keyword: { type: string }
        special_process_price:
          type: number
          format: double
          nullable: true
        formula_expression: { type: string }
        priority: { type: integer }
        is_active: { type: boolean }
        effective_from:
          type: string
          format: date-time
          nullable: true
        effective_to:
          type: string
          format: date-time
          nullable: true
        supersedes_rule_id:
          type: integer
          nullable: true
        superseded_by_rule_id:
          type: integer
          nullable: true
          description: Derived latest successor rule row that points back through `supersedes_rule_id`.
        governance_note:
          type: string
          description: Lightweight governance note only. This is not an approval comment stream.
        governance_status:
          $ref: '#/components/schemas/CostRuleGovernanceStatus'
        version_chain_summary:
          allOf: [{ $ref: '#/components/schemas/CostRuleVersionChainSummary' }]
          nullable: true
          description: Derived lineage summary for this governed rule row.
        previous_version:
          allOf: [{ $ref: '#/components/schemas/CostRuleVersionRef' }]
          nullable: true
          description: Derived previous governed rule in the same lineage, if any.
        next_version:
          allOf: [{ $ref: '#/components/schemas/CostRuleVersionRef' }]
          nullable: true
          description: Derived next governed rule in the same lineage, if any.
        supersession_depth:
          type: integer
          description: Zero-based depth of the current row inside its derived lineage chain.
        source: { type: string }
        remark: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    CostRuleHistoryReadModel:
      type: object
      properties:
        rule:
          $ref: '#/components/schemas/CostRule'
        version_chain:
          type: array
          items:
            $ref: '#/components/schemas/CostRuleVersionRef'
        current_rule:
          allOf: [{ $ref: '#/components/schemas/CostRuleVersionRef' }]
          nullable: true
          description: Latest reachable governed rule in the same lineage at read time.
    Task:
      type: object
      properties:
        id: { type: integer }
        task_no: { type: string }
        source_mode:
          type: string
          enum: [existing_product, new_product]
        product_id:
          type: integer
          nullable: true
        sku_code: { type: string }
        product_name_snapshot: { type: string }
        task_type:
          type: string
          enum: [original_product_development, new_product_development, purchase_task]
        operator_group_id:
          type: integer
          nullable: true
        owner_team:
          type: string
          description: Compatibility task owner team field.
        owner_department:
          type: string
          description: Canonical task owner department.
        owner_org_team:
          type: string
          description: Canonical task owner org team.
        creator_id: { type: integer }
        requester_id:
          type: integer
          nullable: true
          description: Canonical requester / initiator user id. Historical rows are backfilled from `creator_id`.
        designer_id:
          type: integer
          nullable: true
        current_handler_id:
          type: integer
          nullable: true
        task_status:
          type: string
          enum: [Draft, PendingAssign, Assigned, InProgress, PendingAuditA, RejectedByAuditA, PendingAuditB, RejectedByAuditB, PendingOutsource, Outsourcing, PendingOutsourceReview, PendingCustomizationReview, PendingCustomizationProduction, PendingEffectReview, PendingEffectRevision, PendingProductionTransfer, PendingWarehouseQC, RejectedByWarehouse, PendingWarehouseReceive, PendingClose, Completed, Archived, Blocked, Cancelled]
        priority:
          type: string
          enum: [low, normal, high, critical]
        deadline_at:
          type: string
          format: date-time
          nullable: true
        need_outsource: { type: boolean }
        is_outsource: { type: boolean }
        customization_required: { type: boolean }
        customization_source_type:
          type: string
          enum: [new_product, existing_product]
          nullable: true
        last_customization_operator_id:
          type: integer
          nullable: true
        warehouse_reject_reason:
          type: string
          nullable: true
        warehouse_reject_category:
          type: string
          nullable: true
        is_batch_task: { type: boolean }
        batch_item_count: { type: integer }
        batch_mode:
          type: string
          enum: [single, multi_sku]
        primary_sku_code:
          type: string
          nullable: true
        sku_generation_status:
          type: string
          enum: [not_applicable, pending, partial, completed, failed]
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    TaskDetail:
      type: object
      description: Task supplemental demand information. Field names follow `domain.TaskDetail` json tags in `domain/task.go`.
      properties:
        id: { type: integer }
        task_id: { type: integer }
        demand_text: { type: string }
        copy_text: { type: string }
        style_keywords: { type: string }
        remark: { type: string }
        note: { type: string }
        risk_flags_json: { type: string }
        category: { type: string }
        category_id:
          type: integer
          nullable: true
        category_code: { type: string }
        category_name: { type: string }
        product_selection:
          allOf: [{ $ref: '#/components/schemas/TaskProductSelectionContext' }]
          nullable: true
        change_request: { type: string }
        design_requirement: { type: string }
        product_short_name: { type: string }
        material_mode: { type: string }
        material_other: { type: string }
        cost_price_mode: { type: string }
        base_sale_price:
          type: number
          format: double
          nullable: true
        product_channel: { type: string }
        reference_images_json: { type: string }
        reference_file_refs_json: { type: string }
        reference_link: { type: string }
        spec_text: { type: string }
        material: { type: string }
        size_text: { type: string }
        craft_text: { type: string }
        width:
          type: number
          format: double
          nullable: true
        height:
          type: number
          format: double
          nullable: true
        area:
          type: number
          format: double
          nullable: true
        quantity:
          type: integer
          format: int64
          nullable: true
        process: { type: string }
        cost_price:
          type: number
          format: double
          nullable: true
          description: Current effective internal cost. When `manual_cost_override=false`, this is normally synchronized from `estimated_cost`.
        estimated_cost:
          type: number
          format: double
          nullable: true
          description: Last system preview / prefill result derived from category plus minimal size/area/quantity/process inputs.
        cost_rule_id:
          type: integer
          nullable: true
        cost_rule_name: { type: string }
        cost_rule_source: { type: string }
        matched_rule_version:
          type: integer
          nullable: true
          description: Snapshot of the matched cost-rule version used by the latest task-side preview/prefill.
        prefill_source:
          type: string
          description: Snapshot source of the latest cost prefill trace, such as `cost_rule_preview` or `manual_rule_reference`.
        prefill_at:
          type: string
          format: date-time
          nullable: true
          description: Timestamp of the latest task-side preview/prefill snapshot. Historical tasks are not auto-recomputed later.
        requires_manual_review: { type: boolean }
        manual_cost_override:
          type: boolean
          description: Business data flag only. This is not a permission-system signal.
        manual_cost_override_reason: { type: string }
        override_actor:
          type: string
          description: Lightweight override trace only. This is not a real auth identity or approval actor model.
        override_at:
          type: string
          format: date-time
          nullable: true
        filing_status:
          type: string
          enum: [not_filed, pending_filing, filing, filed, filing_failed]
        filing_error_message: { type: string }
        filing_trigger_source: { type: string }
        last_filing_attempt_at:
          type: string
          format: date-time
          nullable: true
        last_filed_at:
          type: string
          format: date-time
          nullable: true
        erp_sync_required: { type: boolean }
        erp_sync_version: { type: integer, format: int64 }
        filed_at:
          type: string
          format: date-time
          nullable: true
        missing_fields:
          type: array
          nullable: true
          items: { type: string }
        missing_fields_summary_cn:
          type: string
          nullable: true
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    TaskAggregateModule:
      type: object
      description: V1.1-A1 detail fast-path module item. It embeds `domain.TaskModule` and adds visibility/action/projection fields from `task_aggregator.ModuleDetail`.
      properties:
        id: { type: integer }
        task_id: { type: integer }
        module_key:
          type: string
          enum: [basic_info, design, audit, warehouse, customization, procurement, retouch]
        state:
          $ref: '#/components/schemas/TaskModuleState'
        pool_team_code:
          type: string
          nullable: true
        claimed_by:
          type: integer
          nullable: true
        claimed_team_code:
          type: string
          nullable: true
        claimed_at:
          type: string
          format: date-time
          nullable: true
        actor_org_snapshot:
          type: object
          nullable: true
          additionalProperties: true
        entered_at: { type: string, format: date-time }
        terminal_at:
          type: string
          format: date-time
          nullable: true
        data:
          type: object
          additionalProperties: true
        updated_at: { type: string, format: date-time }
        visibility: { type: string }
        allowed_actions:
          type: array
          items: { type: string }
        projection:
          type: object
          additionalProperties: true
    TaskModuleEvent:
      type: object
      description: Recent task module event returned by V1.1-A1 detail fast path, capped by service at 50 events.
      properties:
        id: { type: integer }
        task_id:
          type: integer
          nullable: true
        task_module_id: { type: integer }
        module_key:
          type: string
          nullable: true
        event_type: { type: string }
        from_state:
          allOf: [{ $ref: '#/components/schemas/TaskModuleState' }]
          nullable: true
        to_state:
          allOf: [{ $ref: '#/components/schemas/TaskModuleState' }]
          nullable: true
        actor_id:
          type: integer
          nullable: true
        actor_snapshot:
          type: object
          nullable: true
          additionalProperties: true
        payload:
          type: object
          additionalProperties: true
        created_at: { type: string, format: date-time }
    ReferenceFileRefFlat:
      type: object
      description: Flat task reference-file relation returned by V1.1-A1 detail fast path.
      properties:
        id: { type: integer }
        task_id: { type: integer }
        sku_item_id:
          type: integer
          nullable: true
        ref_id: { type: string }
        owner_module_key: { type: string }
        context:
          type: string
          nullable: true
        attached_at: { type: string, format: date-time }
    TaskAggregateDetailV2:
      type: object
      description: V1.1-A1 fast-path task aggregate detail. Batch tasks include `sku_items`; design uploads include `asset_versions` with `scope_sku_code` for per-SKU grouping.
      properties:
        task:
          $ref: '#/components/schemas/Task'
        task_detail:
          allOf: [{ $ref: '#/components/schemas/TaskDetail' }]
          nullable: true
        modules:
          type: array
          items:
            $ref: '#/components/schemas/TaskAggregateModule'
        events:
          type: array
          items:
            $ref: '#/components/schemas/TaskModuleEvent'
        reference_file_refs:
          type: array
          items:
            $ref: '#/components/schemas/ReferenceFileRef'
        sku_items:
          type: array
          items:
            $ref: '#/components/schemas/TaskSKUItem'
        asset_versions:
          type: array
          items:
            $ref: '#/components/schemas/DesignAssetVersion'
    ProcurementStatus:
      type: string
      enum: [draft, prepared, in_progress, completed]
    ProcurementAction:
      type: string
      enum: [prepare, start, complete, reopen]
    ProcurementCoordinationStatus:
      type: string
      enum: [preparing, awaiting_arrival, ready_for_warehouse, handed_to_warehouse, warehouse_completed]
    ProcurementRecord:
      type: object
      properties:
        id: { type: integer }
        task_id: { type: integer }
        status:
          $ref: '#/components/schemas/ProcurementStatus'
        procurement_price:
          type: number
          format: double
          nullable: true
        quantity:
          type: integer
          format: int64
          nullable: true
        supplier_name: { type: string }
        purchase_remark: { type: string }
        expected_delivery_at:
          type: string
          format: date-time
          nullable: true
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    ProcurementSummary:
      type: object
      properties:
        status:
          $ref: '#/components/schemas/ProcurementStatus'
        coordination_status:
          $ref: '#/components/schemas/ProcurementCoordinationStatus'
        coordination_label:
          type: string
        warehouse_status:
          allOf:
            - type: string
              enum: [received, rejected, completed]
          nullable: true
        warehouse_prepare_ready:
          type: boolean
        warehouse_receive_ready:
          type: boolean
        category_code: { type: string }
        category_name: { type: string }
        procurement_price:
          type: number
          format: double
          nullable: true
        cost_price:
          type: number
          format: double
          nullable: true
        estimated_cost:
          type: number
          format: double
          nullable: true
        cost_rule_id:
          type: integer
          nullable: true
        cost_rule_name: { type: string }
        cost_rule_source: { type: string }
        matched_rule_version:
          type: integer
          nullable: true
        prefill_source: { type: string }
        prefill_at:
          type: string
          format: date-time
          nullable: true
        requires_manual_review:
          type: boolean
        manual_cost_override:
          type: boolean
        manual_cost_override_reason: { type: string }
        override_actor: { type: string }
        override_at:
          type: string
          format: date-time
          nullable: true
        quantity:
          type: integer
          format: int64
          nullable: true
        supplier_name: { type: string }
        expected_delivery_at:
          type: string
          format: date-time
          nullable: true
        product_selection:
          allOf: [{ $ref: '#/components/schemas/TaskProductSelectionSummary' }]
          nullable: true
        matched_rule_governance:
          allOf: [{ $ref: '#/components/schemas/TaskMatchedRuleGovernance' }]
          nullable: true
        override_summary:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideSummary' }]
          nullable: true
        governance_audit_summary:
          allOf: [{ $ref: '#/components/schemas/TaskGovernanceAuditSummary' }]
          nullable: true
        override_governance_boundary:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideGovernanceBoundary' }]
          nullable: true
        policy_mode:
          $ref: '#/components/schemas/PolicyMode'
        visible_to_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        action_roles:
          type: array
          items:
            $ref: '#/components/schemas/ActionPolicySummary'
        policy_scope_summary:
          $ref: '#/components/schemas/PolicyScopeSummary'
        platform_entry_boundary:
          allOf: [{ $ref: '#/components/schemas/PlatformEntryBoundary' }]
          nullable: true
        filing_status:
          type: string
          nullable: true
          enum: [not_filed, pending_filing, filing, filed, filing_failed]
        filing_error_message: { type: string, nullable: true }
        filing_trigger_source: { type: string, nullable: true }
        last_filing_attempt_at: { type: string, format: date-time, nullable: true }
        last_filed_at: { type: string, format: date-time, nullable: true }
        erp_sync_required: { type: boolean }
        erp_sync_version: { type: integer, format: int64 }
        missing_fields:
          type: array
          items: { type: string }
          nullable: true
        missing_fields_summary_cn: { type: string, nullable: true }
        filed_at: { type: string, format: date-time, nullable: true }
    TaskMatchedRuleSnapshot:
      type: object
      properties:
        rule_id: { type: integer }
        rule_name: { type: string }
        rule_version: { type: integer }
        rule_source: { type: string }
        governance_status:
          $ref: '#/components/schemas/CostRuleGovernanceStatus'
        prefill_source: { type: string }
        prefill_at:
          type: string
          format: date-time
          nullable: true
        requires_manual_review: { type: boolean }
        is_current_rule: { type: boolean }
    TaskMatchedRuleGovernance:
      type: object
      properties:
        matched_rule:
          allOf: [{ $ref: '#/components/schemas/TaskMatchedRuleSnapshot' }]
          nullable: true
        current_rule:
          allOf: [{ $ref: '#/components/schemas/CostRuleVersionRef' }]
          nullable: true
        version_chain_summary:
          allOf: [{ $ref: '#/components/schemas/CostRuleVersionChainSummary' }]
          nullable: true
        is_rule_outdated: { type: boolean }
        current_rule_version_hint:
          type: integer
          nullable: true
    TaskCostOverrideAuditEventType:
      type: string
      enum: [override_applied, override_updated, override_released]
    TaskCostOverrideReviewStatus:
      type: string
      enum: [not_required, pending, approved, rejected]
    TaskCostOverrideFinanceStatus:
      type: string
      enum: [not_required, pending, ready_for_view, marked_for_view]
    TaskCostOverrideBoundaryActionSummary:
      type: object
      description: Ready-for-frontend lightweight placeholder-boundary action summary. This is not a real approval or finance workflow event stream.
      properties:
        action_type: { type: string }
        status: { type: string }
        actor:
          type: string
          description: Lightweight placeholder actor only. This is not a real identity or approver model.
        acted_at:
          type: string
          format: date-time
          nullable: true
        note: { type: string }
        source:
          type: string
          description: Current source is either the persisted placeholder table or a derived fallback summary.
    TaskCostOverrideApprovalPlaceholderSummary:
      type: object
      description: Ready-for-frontend approval-placeholder summary layered above `cost_override_events`. This is not a real approval workflow.
      properties:
        override_event_id: { type: string }
        task_id: { type: integer }
        review_record_id:
          type: integer
          nullable: true
        review_required: { type: boolean }
        review_status:
          $ref: '#/components/schemas/TaskCostOverrideReviewStatus'
        approval_placeholder_status:
          $ref: '#/components/schemas/TaskCostOverrideReviewStatus'
        review_note: { type: string }
        review_actor:
          type: string
          description: Lightweight placeholder actor only. This is not a real identity or approver model.
        reviewed_at:
          type: string
          format: date-time
          nullable: true
        latest_review_action:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideBoundaryActionSummary' }]
          nullable: true
        source: { type: string }
        is_placeholder_boundary_only: { type: boolean }
    TaskCostOverrideFinancePlaceholderSummary:
      type: object
      description: Ready-for-frontend finance-placeholder summary layered above `cost_override_events`. This is not a real finance, accounting, or settlement system.
      properties:
        override_event_id: { type: string }
        task_id: { type: integer }
        finance_record_id:
          type: integer
          nullable: true
        finance_required: { type: boolean }
        finance_status:
          $ref: '#/components/schemas/TaskCostOverrideFinanceStatus'
        finance_placeholder_status:
          $ref: '#/components/schemas/TaskCostOverrideFinanceStatus'
        finance_note: { type: string }
        finance_marked_by:
          type: string
          description: Lightweight placeholder actor only. This is not a real finance identity system.
        finance_marked_at:
          type: string
          format: date-time
          nullable: true
        finance_view_ready:
          type: boolean
          description: Placeholder-only indicator that the override may enter the future finance-facing layer. This is not a ledger-posted state.
        latest_finance_action:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideBoundaryActionSummary' }]
          nullable: true
        source: { type: string }
        is_placeholder_boundary_only: { type: boolean }
    TaskCostOverrideGovernanceBoundarySummary:
      type: object
      description: Ready-for-frontend unified governance-boundary summary reused across task/detail/procurement/timeline reads. This is not a real approval or finance system.
      properties:
        review_required: { type: boolean }
        review_status:
          $ref: '#/components/schemas/TaskCostOverrideReviewStatus'
        finance_required: { type: boolean }
        finance_status:
          $ref: '#/components/schemas/TaskCostOverrideFinanceStatus'
        finance_view_ready: { type: boolean }
        latest_review_action:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideBoundaryActionSummary' }]
          nullable: true
        latest_finance_action:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideBoundaryActionSummary' }]
          nullable: true
        latest_boundary_actor: { type: string }
        latest_boundary_at:
          type: string
          format: date-time
          nullable: true
        is_placeholder_boundary_only: { type: boolean }
    TaskCostOverrideGovernanceBoundary:
      type: object
      description: Unified ready-for-frontend governance boundary layered above `cost_override_events`. It consolidates approval placeholder, finance placeholder, and latest-action summary reads without introducing a real approval workflow, finance system, or ERP writeback contract.
      properties:
        override_event_id: { type: string }
        task_id: { type: integer }
        review_record_id:
          type: integer
          nullable: true
        finance_record_id:
          type: integer
          nullable: true
        review_required: { type: boolean }
        review_status:
          $ref: '#/components/schemas/TaskCostOverrideReviewStatus'
        approval_placeholder_status:
          $ref: '#/components/schemas/TaskCostOverrideReviewStatus'
        review_note: { type: string }
        review_actor:
          type: string
          description: Lightweight placeholder actor only. This is not a real identity or approver model.
        reviewed_at:
          type: string
          format: date-time
          nullable: true
        finance_required: { type: boolean }
        finance_status:
          $ref: '#/components/schemas/TaskCostOverrideFinanceStatus'
        finance_placeholder_status:
          $ref: '#/components/schemas/TaskCostOverrideFinanceStatus'
        finance_note: { type: string }
        finance_marked_by:
          type: string
          description: Lightweight placeholder actor only. This is not a real finance identity system.
        finance_marked_at:
          type: string
          format: date-time
          nullable: true
        finance_view_ready:
          type: boolean
          description: Placeholder-only indicator that the override may enter the future finance-facing layer. This is not a ledger-posted state.
        latest_review_action:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideBoundaryActionSummary' }]
          nullable: true
        latest_finance_action:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideBoundaryActionSummary' }]
          nullable: true
        latest_boundary_actor: { type: string }
        latest_boundary_at:
          type: string
          format: date-time
          nullable: true
        governance_boundary_summary:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideGovernanceBoundarySummary' }]
          nullable: true
        approval_placeholder_summary:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideApprovalPlaceholderSummary' }]
          nullable: true
        finance_placeholder_summary:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideFinancePlaceholderSummary' }]
          nullable: true
        is_placeholder_boundary_only: { type: boolean }
        policy_mode:
          $ref: '#/components/schemas/PolicyMode'
        visible_to_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        action_roles:
          type: array
          items:
            $ref: '#/components/schemas/ActionPolicySummary'
        policy_scope_summary:
          $ref: '#/components/schemas/PolicyScopeSummary'
        platform_entry_boundary:
          allOf: [{ $ref: '#/components/schemas/PlatformEntryBoundary' }]
          nullable: true
    TaskCostOverrideEventSummary:
      type: object
      properties:
        event_id: { type: string }
        sequence: { type: integer }
        event_type:
          $ref: '#/components/schemas/TaskCostOverrideAuditEventType'
        cost_price:
          type: number
          format: double
          nullable: true
        previous_estimated_cost:
          type: number
          format: double
          nullable: true
        previous_cost_price:
          type: number
          format: double
          nullable: true
        override_cost:
          type: number
          format: double
          nullable: true
        result_cost_price:
          type: number
          format: double
          nullable: true
        category_code: { type: string }
        matched_rule_id:
          type: integer
          nullable: true
        matched_rule_version:
          type: integer
          nullable: true
        matched_rule_source: { type: string }
        governance_status:
          $ref: '#/components/schemas/CostRuleGovernanceStatus'
        reason: { type: string }
        actor: { type: string }
        source: { type: string }
        note: { type: string }
        occurred_at:
          type: string
          format: date-time
    TaskCostOverrideSummary:
      type: object
      properties:
        current_override_active: { type: boolean }
        current_override_reason: { type: string }
        current_override_actor: { type: string }
        current_override_at:
          type: string
          format: date-time
          nullable: true
        current_cost_price:
          type: number
          format: double
          nullable: true
        override_event_count: { type: integer }
        latest_override_event:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideEventSummary' }]
          nullable: true
        latest_release_event:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideEventSummary' }]
          nullable: true
        latest_audit_event:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideEventSummary' }]
          nullable: true
        history_source:
          type: string
          description: Current summary source. This is a governance-audit skeleton only, not a dedicated approval flow.
    TaskGovernanceAuditSummary:
      type: object
      properties:
        audit_layer: { type: string }
        general_event_layer: { type: string }
        event_count: { type: integer }
        latest_event_id: { type: string }
        latest_event_type:
          $ref: '#/components/schemas/TaskCostOverrideAuditEventType'
        latest_event_at:
          type: string
          format: date-time
          nullable: true
        current_override_active: { type: boolean }
    TaskCostOverrideAuditEvent:
      type: object
      properties:
        event_id: { type: string }
        task_id: { type: integer }
        task_detail_id:
          type: integer
          nullable: true
        sequence: { type: integer }
        event_type:
          $ref: '#/components/schemas/TaskCostOverrideAuditEventType'
        category_code: { type: string }
        matched_rule_id:
          type: integer
          nullable: true
        matched_rule_version:
          type: integer
          nullable: true
        matched_rule_source: { type: string }
        governance_status:
          $ref: '#/components/schemas/CostRuleGovernanceStatus'
        previous_estimated_cost:
          type: number
          format: double
          nullable: true
        previous_cost_price:
          type: number
          format: double
          nullable: true
        override_cost:
          type: number
          format: double
          nullable: true
        result_cost_price:
          type: number
          format: double
          nullable: true
        override_reason: { type: string }
        override_actor: { type: string }
        override_at:
          type: string
          format: date-time
        source: { type: string }
        note: { type: string }
        created_at:
          type: string
          format: date-time
        override_governance_boundary:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideGovernanceBoundary' }]
          nullable: true
    TaskCostOverrideAuditTimeline:
      type: object
      properties:
        task_id: { type: integer }
        events:
          type: array
          items:
            $ref: '#/components/schemas/TaskCostOverrideAuditEvent'
        governance_audit_summary:
          allOf: [{ $ref: '#/components/schemas/TaskGovernanceAuditSummary' }]
          nullable: true
        override_governance_boundary:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideGovernanceBoundary' }]
          nullable: true
    TaskCostOverrideReviewRequest:
      type: object
      description: Placeholder-only review boundary update. This does not create a real approval workflow or identity approval chain.
      properties:
        review_required:
          type: boolean
          nullable: true
        review_status:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideReviewStatus' }]
          nullable: true
        review_note: { type: string }
        review_actor:
          type: string
          description: Optional explicit placeholder actor. When omitted, the debug-header actor placeholder may be used.
        reviewed_at:
          type: string
          format: date-time
          nullable: true
    TaskCostOverrideFinanceMarkRequest:
      type: object
      description: Placeholder-only finance-boundary update. This does not create a real finance, accounting, reconciliation, settlement, or invoice system.
      properties:
        finance_required:
          type: boolean
          nullable: true
        finance_status:
          allOf: [{ $ref: '#/components/schemas/TaskCostOverrideFinanceStatus' }]
          nullable: true
        finance_note: { type: string }
        finance_marked_by:
          type: string
          description: Optional explicit placeholder actor. When omitted, the debug-header actor placeholder may be used.
        finance_marked_at:
          type: string
          format: date-time
          nullable: true
    AuditHandover:
      type: object
      properties:
        id: { type: integer }
        handover_no: { type: string }
        task_id: { type: integer }
        from_auditor_id: { type: integer }
        to_auditor_id: { type: integer }
        reason: { type: string }
        current_judgement: { type: string }
        risk_remark: { type: string }
        status:
          type: string
          enum: [pending_takeover, taken_over, cancelled]
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    OutsourceOrder:
      type: object
      properties:
        id: { type: integer }
        outsource_no: { type: string }
        task_id: { type: integer }
        vendor_name: { type: string }
        outsource_type: { type: string }
        delivery_requirement: { type: string }
        settlement_note: { type: string }
        status:
          type: string
          enum: [created, packaged, sent, in_production, returned, reviewing, approved, rejected, closed]
        returned_at:
          type: string
          format: date-time
          nullable: true
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    WarehouseReceipt:
      type: object
      properties:
        id: { type: integer }
        task_id: { type: integer }
        receipt_no: { type: string }
        workflow_lane:
          type: string
          enum: [normal, customization]
        source_department:
          type: string
          nullable: true
          description: Canonical upstream source department for the warehouse intake item (`Design R&D Department`, `Customization Art Department`, or `Cloud Warehouse Department` for purchase intake).
        task_type:
          type: string
          nullable: true
          description: Task type of the originating task (e.g. `original_product_development`, `new_product_development`, `purchase_task`).
        status:
          type: string
          enum: [received, rejected, completed]
        receiver_id:
          type: integer
          nullable: true
        received_at:
          type: string
          format: date-time
          nullable: true
        completed_at:
          type: string
          format: date-time
          nullable: true
        reject_reason: { type: string }
        remark: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        warehouse_ready_version:
          allOf: [{ $ref: '#/components/schemas/DesignAssetVersion' }]
          nullable: true
    CustomizationJob:
      type: object
      properties:
        id: { type: integer }
        task_id: { type: integer }
        source_asset_id:
          type: integer
          nullable: true
        current_asset_id:
          type: integer
          nullable: true
        order_no:
          type: string
          nullable: true
        customization_level_code: { type: string }
        customization_level_name: { type: string }
        review_reference_unit_price:
          type: number
          format: double
          nullable: true
          description: Business-entered review reference unit price for the current phase. This is not the frozen settlement snapshot.
        review_reference_weight_factor:
          type: number
          format: double
          nullable: true
          description: Business-entered review reference weight factor for the current phase. This is not the frozen settlement snapshot.
        unit_price:
          type: number
          format: double
          nullable: true
          description: Execution-stage frozen settlement unit price snapshot resolved at the pricing freeze point.
        weight_factor:
          type: number
          format: double
          nullable: true
          description: Execution-stage frozen settlement weight factor snapshot resolved at the pricing freeze point.
        note: { type: string }
        customization_review_decision:
          type: string
          enum: ["", approved, return_to_designer, reviewer_fixed]
          description: Empty before the first customization review decision is submitted.
        decision_type:
          type: string
          enum: [final, effect_preview]
        assigned_operator_id:
          type: integer
          nullable: true
        last_operator_id:
          type: integer
          nullable: true
        pricing_worker_type:
          allOf: [{ $ref: '#/components/schemas/EmploymentType' }]
          nullable: true
          description: Execution-stage frozen settlement worker employment type captured at the pricing freeze point.
        status:
          type: string
          enum: [pending_customization_review, pending_customization_production, pending_effect_review, pending_effect_revision, pending_production_transfer, pending_warehouse_qc, rejected_by_warehouse, completed]
        warehouse_reject_reason:
          type: string
          nullable: true
        warehouse_reject_category:
          type: string
          nullable: true
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    TaskEvent:
      type: object
      description: General task event stream. Payloads may include action-specific before/after task status, handler, and result context for business audit and troubleshooting.
      properties:
        id:
          type: string
          format: uuid
        task_id: { type: integer }
        sequence: { type: integer }
        event_type: { type: string }
        operator_id:
          type: integer
          nullable: true
        operator_name:
          type: string
          description: Display name resolved from `operator_id` for timeline rendering.
        creator_id:
          type: integer
          nullable: true
          description: Task creator id, repeated on each event for frontend timeline context.
        creator_name:
          type: string
          description: Task creator display name, repeated on each event for frontend timeline context.
        payload:
          type: object
          additionalProperties: true
        created_at:
          type: string
          format: date-time
    TaskAsset:
      type: object
      description: Task-scoped asset timeline item. Asset semantics are now canonicalized to `reference/source/delivery/preview`, while legacy input aliases remain compatibility-only.
      properties:
        id: { type: integer }
        task_id: { type: integer }
        asset_id:
          type: integer
          nullable: true
        asset_type:
          type: string
          enum: [reference, source, delivery, preview, design_thumb]
        version_no: { type: integer }
        asset_version_no:
          type: integer
          nullable: true
        upload_mode:
          type: string
          nullable: true
          enum: [small, multipart]
          description: Compatibility-only persisted upload mode for timeline/history payloads.
        upload_request_id:
          type: string
          nullable: true
        storage_ref_id:
          type: string
          nullable: true
        file_name: { type: string }
        original_filename:
          type: string
          nullable: true
        mime_type:
          type: string
          nullable: true
        file_size:
          type: integer
          format: int64
          nullable: true
        file_path:
          type: string
          nullable: true
        storage_key:
          type: string
          nullable: true
        whole_hash:
          type: string
          nullable: true
        upload_status:
          type: string
          nullable: true
          enum: [pending, uploaded, cancelled]
        preview_status:
          type: string
          nullable: true
          enum: [pending, not_applicable]
        storage_ref:
          allOf: [{ $ref: '#/components/schemas/AssetStorageRef' }]
          nullable: true
        uploaded_by: { type: integer }
        uploader_name:
          type: string
          nullable: true
          description: Display name resolved from uploaded_by. Empty string when uploader is unknown or not resolvable.
        uploaded_at:
          type: string
          format: date-time
          nullable: true
        remark: { type: string }
        remote_file_id:
          type: string
          nullable: true
        scope_sku_code:
          type: string
          nullable: true
        source_module_key:
          type: string
          nullable: true
        source_task_module_id:
          type: integer
          nullable: true
        is_archived: { type: boolean }
        archived_by:
          type: integer
          nullable: true
        archived_at:
          type: string
          format: date-time
          nullable: true
        cleaned_at:
          type: string
          format: date-time
          nullable: true
        deleted_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
    DesignAssetUploadMode:
      type: string
      description: Compatibility-only internal upload-mode enum (`small`/`multipart`). New frontend integrations must use `upload_strategy` returned by upload-session creation instead of choosing this field.
      enum: [small, multipart]
    DesignAssetUploadStatus:
      type: string
      enum: [pending, uploaded, cancelled]
    DesignAssetPreviewStatus:
      type: string
      enum: [pending, not_applicable]
    DesignAssetSessionStatus:
      type: string
      enum: [created, completed, cancelled, expired]
    DesignAssetStorageProvider:
      type: string
      enum: [oss]
    DesignAssetAccessPolicy:
      type: string
      enum: [reference_direct, source_controlled, delivery_flow, preview_assist]
    DesignAssetSourceAccessMode:
      type: string
      enum: [standard]
    DesignAssetVersion:
      type: object
      properties:
        id: { type: integer }
        task_id: { type: integer }
        task_no:
          type: string
          nullable: true
        asset_id: { type: integer }
        asset_no:
          type: string
          nullable: true
        source_asset_id:
          type: integer
          nullable: true
          description: Optional source-asset linkage used by backend-owned preview/thumb derivatives.
        scope_sku_code:
          type: string
          nullable: true
          description: Optional batch-SKU scope. When present, this version belongs to exactly one `sku_items[].sku_code`.
        asset_type:
          type: string
          enum: [reference, source, delivery, preview, design_thumb]
        version_no: { type: integer }
        timeline_version_no: { type: integer }
        upload_mode:
          $ref: '#/components/schemas/DesignAssetUploadMode'
          description: Compatibility input alias. New frontend integrations should prefer `asset_kind` and let backend strategy selection drive `upload_strategy`.
        original_filename: { type: string }
        remote_file_id:
          type: string
          nullable: true
        storage_key: { type: string }
        file_size:
          type: integer
          format: int64
          nullable: true
        file_hash:
          type: string
          nullable: true
        mime_type:
          type: string
        upload_status:
          $ref: '#/components/schemas/DesignAssetUploadStatus'
        preview_status:
          $ref: '#/components/schemas/DesignAssetPreviewStatus'
        uploaded_by: { type: integer }
        uploader_name:
          type: string
          nullable: true
          description: Display name resolved from uploaded_by. Empty string when uploader is unknown or not resolvable.
        uploaded_at:
          type: string
          format: date-time
          nullable: true
        remark: { type: string }
        upload_session_id:
          type: string
          nullable: true
        is_source_file: { type: boolean }
        is_delivery_file: { type: boolean }
        is_preview_file: { type: boolean }
        is_design_thumb: { type: boolean }
        source_access_mode:
          $ref: '#/components/schemas/DesignAssetSourceAccessMode'
        access_policy:
          $ref: '#/components/schemas/DesignAssetAccessPolicy'
        preview_available:
          type: boolean
          description: |
            Whether this asset version is currently previewable.
            For `asset_kind=source`, preview is available immediately only when source format is OSS IMG direct-preview capable (`jpg/png/bmp/gif/webp/tiff/heic/avif`);
            otherwise preview depends on backend-derived `preview/design_thumb` assets linked by `source_asset_id`.
        warehouse_ready: { type: boolean }
        approved_for_flow: { type: boolean }
        current_version_role:
          type: string
          nullable: true
          enum: [current_version, approved_version, warehouse_ready_version, current_approved_version, current_warehouse_ready_version]
        download_url:
          type: string
          nullable: true
          description: Canonical business file URL. New frontend integration should use this field directly; runtime prefers browser-direct OSS/download service URLs and only falls back to `/v1/assets/files/{path}` for compatibility.
        public_download_allowed:
          type: boolean
          description: Compatibility-only boolean retained for old callers. Obsolete for frontend rollout; new integration must use `download_url` and `download_mode`.
        preview_public_allowed:
          type: boolean
          description: Compatibility-only boolean retained for old callers. Obsolete for frontend rollout; frontend preview behavior should follow `preview_available` and `download_url`.
        access_hint:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
    DesignAsset:
      type: object
      properties:
        id: { type: integer }
        task_id: { type: integer }
        asset_no: { type: string }
        source_asset_id:
          type: integer
          nullable: true
          description: Optional source-asset linkage used by backend-owned preview/thumb derivatives.
        scope_sku_code:
          type: string
          nullable: true
          description: Optional batch-SKU scope for the asset root. Empty/null means task-level asset shared across the task.
        asset_type:
          type: string
          enum: [reference, source, delivery, preview, design_thumb]
        current_version_id:
          type: integer
          nullable: true
        approved_version_id:
          type: integer
          nullable: true
        warehouse_ready_version_id:
          type: integer
          nullable: true
        upload_status:
          $ref: '#/components/schemas/DesignAssetUploadStatus'
        archive_status:
          type: string
          enum: [active, archived]
        archived_at:
          type: string
          format: date-time
          nullable: true
        last_access_at:
          type: string
          format: date-time
          nullable: true
        created_by: { type: integer }
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        current_version:
          allOf: [{ $ref: '#/components/schemas/DesignAssetVersion' }]
          nullable: true
        approved_version:
          allOf: [{ $ref: '#/components/schemas/DesignAssetVersion' }]
          nullable: true
        warehouse_ready_version:
          allOf: [{ $ref: '#/components/schemas/DesignAssetVersion' }]
          nullable: true
    UploadSession:
      type: object
      properties:
        id: { type: string }
        task_id: { type: integer }
        asset_id:
          type: integer
          nullable: true
        asset_type:
          type: string
          nullable: true
          enum: [reference, source, delivery, preview, design_thumb]
        target_sku_code:
          type: string
          nullable: true
          description: Optional target SKU captured on the upload-session business view for batch-task uploads. The completed asset root and asset version later expose the persisted SKU scope on `scope_sku_code`.
        upload_mode:
          $ref: '#/components/schemas/DesignAssetUploadMode'
          description: Compatibility-only persisted upload mode. New frontend integration must rely on `upload_strategy`.
        filename: { type: string }
        expected_size:
          type: integer
          format: int64
          nullable: true
        mime_type: { type: string }
        storage_provider:
          $ref: '#/components/schemas/DesignAssetStorageProvider'
        session_status:
          $ref: '#/components/schemas/DesignAssetSessionStatus'
        remote_upload_id: { type: string }
        remote_file_id:
          type: string
          nullable: true
        created_by: { type: integer }
        created_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
          nullable: true
        last_synced_at:
          type: string
          format: date-time
          nullable: true
        remark: { type: string }
        current_version_id:
          type: integer
          nullable: true
    RemoteUploadSessionPlan:
      type: object
      properties:
        upload_id: { type: string }
        file_id:
          type: string
          nullable: true
        base_url: { type: string }
        upload_url:
          type: string
          nullable: true
        complete_url:
          type: string
          nullable: true
        abort_url:
          type: string
          nullable: true
        method: { type: string }
        headers:
          type: object
          additionalProperties:
            type: string
          description: Browser upload request headers for the returned direct upload URL. Internal backend tokens are not exposed here.
        part_size_hint:
          type: integer
          format: int64
        upload_mode:
          $ref: '#/components/schemas/DesignAssetUploadMode'
          description: Compatibility-only remote-plan mirror field. New frontend integration should execute the returned plan by `upload_strategy` instead of selecting upload mode manually.
        session_status:
          $ref: '#/components/schemas/DesignAssetSessionStatus'
        filename:
          type: string
        expected_size:
          type: integer
          format: int64
          nullable: true
        mime_type:
          type: string
          description: Normalized upload content type recorded for the session. When omitted at create time, MAIN defaults this to `application/octet-stream`.
        storage_key:
          type: string
        expires_at:
          type: string
          format: date-time
          nullable: true
        last_synced_at:
          type: string
          format: date-time
          nullable: true
        is_stub:
          type: boolean
    AssetOwnerType:
      type: string
      enum: [task, task_asset, export_job, outsource_order, warehouse_receipt]
    AssetStorageAdapter:
      type: string
      enum: [mock_upload, placeholder_storage, export_placeholder, oss_upload_service]
    AssetStorageRefType:
      type: string
      enum: [task_asset_object, export_result, generic_object]
    UploadRequestStatus:
      type: string
      enum: [requested, bound, expired, cancelled]
    UploadRequestAdvanceAction:
      type: string
      enum: [cancel, expire]
    AssetStorageRefStatus:
      type: string
      enum: [recorded, superseded, archived]
    AdapterMode:
      type: string
      description: Cross-center adapter-boundary mode. The same term is reused across export, integration, and storage/upload, but each center may support only a subset of values.
      enum: [dispatch_then_attempt, call_log_then_execution, upload_request_then_storage_ref]
    DispatchMode:
      type: string
      description: Cross-center handoff/disposition mode. Reused across export dispatches, integration executions, and upload-request binding without implying one shared table or one shared worker.
      enum: [dispatch_record, execution_progress, upload_request_binding]
    BoundaryStorageMode:
      type: string
      description: Cross-center storage-side mode. Reused to express placeholder storage semantics without forcing export and asset storage to share one persistence model.
      enum: [lifecycle_managed_result_ref, asset_storage_ref]
    AdapterRefSummary:
      type: object
      description: Minimal shared adapter/connector/storage-adapter identity summary reused by export, integration, and storage/upload read models. This unifies language only; it does not imply the underlying tables or services are merged.
      properties:
        ref_type:
          type: string
        ref_key:
          type: string
        is_placeholder:
          type: boolean
        note:
          type: string
    ResourceRefSummary:
      type: object
      description: Minimal shared resource-reference summary reused by export result references and asset storage references. It aligns placeholder resource language without forcing one table or one file-service model.
      properties:
        ref_type:
          type: string
        ref_key:
          type: string
        file_name:
          type: string
        mime_type:
          type: string
        file_size:
          type: integer
          format: int64
          nullable: true
        checksum_hint:
          type: string
        expires_at:
          type: string
          format: date-time
          nullable: true
        is_placeholder:
          type: boolean
        note:
          type: string
    HandoffRefSummary:
      type: object
      description: Minimal shared handoff summary reused by export dispatches, integration executions, and upload requests. It unifies reference language for cross-boundary handoff state without merging those center-specific lifecycles.
      properties:
        ref_type:
          type: string
        ref_key:
          type: string
        status:
          type: string
        requested_at:
          type: string
          format: date-time
          nullable: true
        received_at:
          type: string
          format: date-time
          nullable: true
        finished_at:
          type: string
          format: date-time
          nullable: true
        expires_at:
          type: string
          format: date-time
          nullable: true
        is_placeholder:
          type: boolean
        note:
          type: string
    AssetStorageRef:
      type: object
      description: Structured placeholder storage reference metadata for task assets and later shared file-like outputs. It aligns with export `result_ref` through shared `adapter_ref_summary` and `resource_ref_summary`. This is metadata only; it is not a real NAS handle, object-storage pointer, signed URL, or proof that bytes were uploaded.
      properties:
        ref_id:
          type: string
        asset_id:
          type: integer
          nullable: true
        owner_type:
          $ref: '#/components/schemas/AssetOwnerType'
        owner_id:
          type: integer
        upload_request_id:
          type: string
        storage_adapter:
          $ref: '#/components/schemas/AssetStorageAdapter'
        ref_type:
          $ref: '#/components/schemas/AssetStorageRefType'
        ref_key:
          type: string
        file_name:
          type: string
        mime_type:
          type: string
        file_size:
          type: integer
          format: int64
          nullable: true
        is_placeholder:
          type: boolean
        checksum_hint:
          type: string
        status:
          $ref: '#/components/schemas/AssetStorageRefStatus'
        adapter_mode:
          $ref: '#/components/schemas/AdapterMode'
        storage_mode:
          $ref: '#/components/schemas/BoundaryStorageMode'
        adapter_ref_summary:
          $ref: '#/components/schemas/AdapterRefSummary'
        resource_ref_summary:
          $ref: '#/components/schemas/ResourceRefSummary'
        created_at:
          type: string
          format: date-time
    UploadRequest:
      type: object
      description: Placeholder upload-intent contract only. Upload requests are storage/upload handoff records aligned with shared export and integration boundary language, with lifecycle readiness hints for `requested -> bound|expired|cancelled`. Creating or advancing an upload request does not upload bytes and does not allocate a real object-storage or NAS location.
      properties:
        request_id:
          type: string
        owner_type:
          $ref: '#/components/schemas/AssetOwnerType'
        owner_id:
          type: integer
        task_id:
          type: integer
        target_sku_code:
          type: string
          nullable: true
        asset_id:
          type: integer
          nullable: true
        source_asset_id:
          type: integer
          nullable: true
        task_asset_type:
          type: string
          nullable: true
          enum: [reference, source, delivery, preview, design_thumb]
        storage_adapter:
          $ref: '#/components/schemas/AssetStorageAdapter'
        upload_mode:
          $ref: '#/components/schemas/DesignAssetUploadMode'
          description: Compatibility-only upload-mode snapshot on upload-request records.
        ref_type:
          $ref: '#/components/schemas/AssetStorageRefType'
        storage_provider:
          $ref: '#/components/schemas/DesignAssetStorageProvider'
        file_name:
          type: string
        mime_type:
          type: string
        file_size:
          type: integer
          format: int64
          nullable: true
        expected_size:
          type: integer
          format: int64
          nullable: true
        checksum_hint:
          type: string
        status:
          $ref: '#/components/schemas/UploadRequestStatus'
        session_status:
          $ref: '#/components/schemas/DesignAssetSessionStatus'
        remote_upload_id:
          type: string
        remote_file_id:
          type: string
        is_placeholder:
          type: boolean
        adapter_mode:
          $ref: '#/components/schemas/AdapterMode'
        dispatch_mode:
          $ref: '#/components/schemas/DispatchMode'
        storage_mode:
          $ref: '#/components/schemas/BoundaryStorageMode'
        adapter_ref_summary:
          $ref: '#/components/schemas/AdapterRefSummary'
        handoff_ref_summary:
          $ref: '#/components/schemas/HandoffRefSummary'
        can_bind:
          type: boolean
        can_cancel:
          type: boolean
        can_expire:
          type: boolean
        bound_asset_id:
          type: integer
          nullable: true
        bound_ref_id:
          type: string
        created_by:
          type: integer
        expires_at:
          type: string
          format: date-time
          nullable: true
        last_synced_at:
          type: string
          format: date-time
          nullable: true
        policy_mode:
          $ref: '#/components/schemas/PolicyMode'
        visible_to_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        action_roles:
          type: array
          items:
            $ref: '#/components/schemas/ActionPolicySummary'
        policy_scope_summary:
          $ref: '#/components/schemas/PolicyScopeSummary'
        remark:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CreateUploadRequestRequest:
      type: object
      required: [owner_type, owner_id, file_name]
      properties:
        owner_type:
          $ref: '#/components/schemas/AssetOwnerType'
        owner_id:
          type: integer
        task_asset_type:
          type: string
          enum: [reference, source, delivery, preview, design_thumb]
        storage_adapter:
          $ref: '#/components/schemas/AssetStorageAdapter'
        ref_type:
          $ref: '#/components/schemas/AssetStorageRefType'
        file_name:
          type: string
        mime_type:
          type: string
        file_size:
          type: integer
          format: int64
          nullable: true
        checksum_hint:
          type: string
        remark:
          type: string
    CreateTaskAssetUploadSessionRequest:
      type: object
      description: |
        `task_id` is required on the top-level `/v1/assets/upload-sessions` route and ignored on task-scoped compatibility routes.
        New frontend integrations should send one upload intent and let the backend choose `upload_strategy`.
        `asset_kind` and `asset_type` are equivalent inputs; `asset_kind` is the canonical frontend-facing name.
        `upload_mode` is compatibility-only and must not be used by new frontend integrations as strategy selector.
        `reference` assets use single-part upload internally. `source`, `delivery`, `preview`, and `design_thumb` use multipart internally.
        `source_asset_id` is used for backend-owned preview/thumb derivatives and links them to a source asset.
        For multi-SKU batch tasks, non-reference uploads must pass `target_sku_code` to bind the uploaded
        design asset to one child SKU.
        Asset-center upload sessions now return OSS-backed remote upload instructions.
        Frontend uploads bytes using the returned remote plan, then calls `complete`.
      properties:
        task_id:
          type: integer
          nullable: true
          description: Required on `POST /v1/assets/upload-sessions`; ignored on task-scoped compatibility routes where task context comes from path.
        created_by:
          type: integer
        asset_id:
          type: integer
          nullable: true
        source_asset_id:
          type: integer
          nullable: true
          description: Optional linkage to a source asset. Allowed for `preview` and `design_thumb` intents.
        asset_type:
          type: string
          enum: [reference, source, delivery, preview, design_thumb]
          description: Compatibility alias of `asset_kind` retained for migration safety.
        asset_kind:
          type: string
          enum: [reference, source, delivery, preview, design_thumb]
          description: Canonical upload intent field for new frontend integrations.
        upload_mode:
          $ref: '#/components/schemas/DesignAssetUploadMode'
          description: Compatibility-only input. New frontend integrations must not send this field.
        filename:
          type: string
          description: Compatibility alias of `file_name`. At least one of `file_name` or `filename` must be provided.
        file_name:
          type: string
          description: Canonical file name field for new frontend integrations. At least one of `file_name` or `filename` must be provided.
        expected_size:
          type: integer
          format: int64
          nullable: true
          description: Optional size hint in bytes.
        file_size:
          type: integer
          format: int64
          nullable: true
          description: Optional compatibility alias of `expected_size`.
        mime_type:
          type: string
          description: Optional MIME hint.
        file_hash:
          type: string
        remark:
          type: string
        target_sku_code:
          type: string
          description: Required for multi-SKU batch-task non-reference uploads. Backend validates that the SKU belongs to the task, returns it on the upload-session business view as `target_sku_code`, and persists the completed asset scope on `scope_sku_code` for the asset root and asset version.
    CreateTaskAssetUploadSessionResponseData:
      type: object
      properties:
        session:
          $ref: '#/components/schemas/UploadSession'
        remote:
          $ref: '#/components/schemas/RemoteUploadSessionPlan'
        upload_strategy:
          type: string
          description: Canonical frontend-facing upload strategy label. Current runtime emits `single_part` or `multipart`; `single_part` corresponds to internal `upload_mode=small`.
        required_upload_content_type:
          type: string
          description: Exact `Content-Type` value that the browser must send on OSS direct PUT and echo on `complete` as `upload_content_type`.
        complete_endpoint:
          type: string
          description: Canonical completion endpoint returned by the session creator.
        cancel_endpoint:
          type: string
          description: Canonical cancellation endpoint returned by the session creator.
    CreateAssetUploadSessionRequestCanonical:
      allOf:
        - $ref: '#/components/schemas/CreateTaskAssetUploadSessionRequest'
        - type: object
          required: [task_id, asset_kind]
          properties:
            task_id:
              type: integer
              minimum: 1
            asset_kind:
              type: string
              enum: [reference, source, delivery, preview, design_thumb]
            file_name:
              type: string
              minLength: 1
            filename:
              type: string
              minLength: 1
          anyOf:
            - required: [file_name]
            - required: [filename]
    CompleteTaskAssetUploadSessionRequest:
      type: object
      properties:
        completed_by:
          type: integer
        file_hash:
          type: string
        upload_content_type:
          type: string
          description: Exact `required_upload_content_type` echoed back by the client when finalizing an OSS direct upload.
        remark:
          type: string
    CompleteTaskAssetUploadSessionResponseData:
      type: object
      properties:
        session:
          $ref: '#/components/schemas/UploadSession'
        asset:
          $ref: '#/components/schemas/DesignAsset'
        version:
          $ref: '#/components/schemas/DesignAssetVersion'
    CancelTaskAssetUploadSessionRequest:
      type: object
      properties:
        cancelled_by:
          type: integer
        remark:
          type: string
    CreateTaskReferenceUploadSessionRequest:
      type: object
      required: [filename]
      description: Creates one compatibility pre-task reference upload session. Prefer `POST /v1/tasks/reference-upload` for the formal frontend entry.
      properties:
        created_by:
          type: integer
        filename:
          type: string
        expected_size:
          type: integer
          format: int64
          nullable: true
        mime_type:
          type: string
        file_hash:
          type: string
        remark:
          type: string
    CreateTaskReferenceUploadSessionResponseData:
      type: object
      properties:
        session:
          $ref: '#/components/schemas/UploadSession'
        remote:
          $ref: '#/components/schemas/RemoteUploadSessionPlan'
    CompleteTaskReferenceUploadSessionRequest:
      type: object
      properties:
        completed_by:
          type: integer
        file_hash:
          type: string
        remark:
          type: string
    CompleteTaskReferenceUploadSessionResponseData:
      type: object
      properties:
        session:
          $ref: '#/components/schemas/UploadSession'
        reference_file_ref:
          type: string
          description: Compatibility ref id. New integrations should use `ref_object` and object arrays in `POST /v1/tasks`.
        storage_ref:
          $ref: '#/components/schemas/AssetStorageRef'
        ref_object:
          $ref: '#/components/schemas/ReferenceFileRef'
    ReferenceFileRef:
      type: object
      required: [asset_id, source]
      properties:
        asset_id:
          type: string
          description: Canonical reference object id used for backend validation.
        ref_id:
          type: string
          description: Compatibility alias of `asset_id`.
        upload_request_id:
          type: string
        filename:
          type: string
        mime_type:
          type: string
        file_size:
          type: integer
          format: int64
          nullable: true
        url:
          type: string
          nullable: true
          description: Compatibility-only alias of `download_url`. Obsolete for frontend rollout; new integration must consume `download_url`.
        download_url:
          type: string
          nullable: true
          description: Signed download URL. For OSS Direct-backed references, this URL is short-lived (default 15 minutes, see download_url_expires_at). Consumers SHOULD refresh via GET /v1/tasks/{id} when the URL expires or when a 403/404 is observed.
        storage_key:
          type: string
          nullable: true
        download_url_expires_at:
          type: string
          format: date-time
          nullable: true
        source:
          type: string
          enum: [task_reference_upload, task_create_asset_center]
        status:
          type: string
          enum: [uploaded]
    AdvanceUploadRequestRequest:
      type: object
      required: [action]
      properties:
        action:
          $ref: '#/components/schemas/UploadRequestAdvanceAction'
        remark:
          type: string
    TaskListItem:
      type: object
      properties:
        id: { type: integer }
        task_no: { type: string }
        sku_code: { type: string }
        primary_sku_code:
          type: string
          nullable: true
        product_name_snapshot: { type: string }
        task_type:
          type: string
          enum: [original_product_development, new_product_development, purchase_task]
        source_mode:
          type: string
          enum: [existing_product, new_product]
        owner_team:
          type: string
          description: Compatibility task owner team field.
        owner_department:
          type: string
          description: Canonical task owner department.
        owner_org_team:
          type: string
          description: Canonical task owner org team.
        creator_id: { type: integer }
        requester_id:
          type: integer
          nullable: true
        designer_id:
          type: integer
          nullable: true
        current_handler_id:
          type: integer
          nullable: true
        requester_name:
          type: string
          nullable: true
        creator_name:
          type: string
          nullable: true
        designer_name:
          type: string
          nullable: true
        current_handler_name:
          type: string
          nullable: true
        task_status:
          type: string
          enum: [Draft, PendingAssign, Assigned, InProgress, PendingAuditA, RejectedByAuditA, PendingAuditB, RejectedByAuditB, PendingOutsource, Outsourcing, PendingOutsourceReview, PendingCustomizationReview, PendingCustomizationProduction, PendingEffectReview, PendingEffectRevision, PendingProductionTransfer, PendingWarehouseQC, RejectedByWarehouse, PendingWarehouseReceive, PendingClose, Completed, Archived, Blocked, Cancelled]
        updated_at:
          type: string
          format: date-time
        deadline_at:
          type: string
          format: date-time
          nullable: true
        need_outsource: { type: boolean }
        customization_required: { type: boolean }
        workflow_lane:
          type: string
          enum: [normal, customization]
          description: Explicit business lane projection derived from `customization_required`.
        customization_source_type:
          type: string
          enum: [new_product, existing_product]
          nullable: true
        last_customization_operator_id:
          type: integer
          nullable: true
        warehouse_reject_reason:
          type: string
          nullable: true
        warehouse_reject_category:
          type: string
          nullable: true
        is_batch_task: { type: boolean }
        batch_item_count: { type: integer }
        batch_mode:
          type: string
          enum: [single, multi_sku]
        warehouse_status:
          type: string
          nullable: true
          enum: [received, rejected, completed]
        latest_asset_type:
          type: string
          nullable: true
          description: Latest task asset type by highest `version_no`, breaking ties by later `created_at`.
          enum: [reference, source, delivery, preview, design_thumb]
        workflow:
          $ref: '#/components/schemas/TaskWorkflowSnapshot'
        procurement_summary:
          allOf: [{ $ref: '#/components/schemas/ProcurementSummary' }]
          nullable: true
        product_selection:
          allOf: [{ $ref: '#/components/schemas/TaskProductSelectionSummary' }]
          nullable: true
        policy_mode:
          $ref: '#/components/schemas/PolicyMode'
        visible_to_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        action_roles:
          type: array
          items:
            $ref: '#/components/schemas/ActionPolicySummary'
        policy_scope_summary:
          $ref: '#/components/schemas/PolicyScopeSummary'
        platform_entry_boundary:
          allOf: [{ $ref: '#/components/schemas/PlatformEntryBoundary' }]
          nullable: true
        category: { type: string, nullable: true }
        category_code: { type: string, nullable: true }
        category_name: { type: string, nullable: true }
        priority: { type: string, nullable: true }
        material: { type: string, nullable: true }
        size_text: { type: string, nullable: true }
        spec_text: { type: string, nullable: true }
        craft_text: { type: string, nullable: true }
        expected_delivery_at: { type: string, format: date-time, nullable: true }
        cost_price: { type: number, nullable: true }
        estimated_cost: { type: number, nullable: true }
        cost_rule_id: { type: integer, nullable: true }
        cost_rule_name: { type: string, nullable: true }
        cost_rule_source: { type: string, nullable: true }
        matched_rule_version: { type: integer, nullable: true }
        source_product_id: { type: integer, nullable: true }
        source_product_name: { type: string, nullable: true }
        source_search_entry_code: { type: string, nullable: true }
        source_match_type: { type: string, nullable: true }
        source_match_rule: { type: string, nullable: true }
        matched_category_code: { type: string, nullable: true }
        matched_search_entry_code: { type: string, nullable: true }
        product_selection_snapshot_json:
          type: object
          nullable: true
          additionalProperties: true
        procurement_status: { type: string, nullable: true }
        procurement_quantity: { type: integer, nullable: true }
        procurement_price: { type: number, nullable: true }
        supplier_name: { type: string, nullable: true }
        is_outsource: { type: boolean }
        erp_sync_required: { type: boolean }
        erp_sync_version: { type: integer, nullable: true }
        filing_status: { type: string, nullable: true }
        filing_error_message: { type: string, nullable: true }
        filing_trigger_source: { type: string, nullable: true }
        prefill_source: { type: string, nullable: true }
        requires_manual_review: { type: boolean }
        manual_cost_override: { type: boolean }
        manual_cost_override_reason: { type: string, nullable: true }
        override_actor: { type: string, nullable: true }
        prefill_at: { type: string, format: date-time, nullable: true }
        override_at: { type: string, format: date-time, nullable: true }
        last_filing_attempt_at: { type: string, format: date-time, nullable: true }
        last_filed_at: { type: string, format: date-time, nullable: true }
        filed_at: { type: string, format: date-time, nullable: true }
        missing_fields:
          type: array
          nullable: true
          items: { type: string }
        missing_fields_summary_cn: { type: string, nullable: true }
        created_at:
          type: string
          format: date-time
    TaskReadModel:
      allOf:
        - $ref: '#/components/schemas/Task'
        - type: object
          properties:
            sku_items:
              type: array
              items:
                $ref: '#/components/schemas/TaskSKUItem'
            workflow:
              $ref: '#/components/schemas/TaskWorkflowSnapshot'
            procurement:
              allOf: [{ $ref: '#/components/schemas/ProcurementRecord' }]
              nullable: true
            procurement_summary:
              allOf: [{ $ref: '#/components/schemas/ProcurementSummary' }]
              nullable: true
            product_selection:
              allOf: [{ $ref: '#/components/schemas/TaskProductSelectionContext' }]
              nullable: true
            matched_rule_governance:
              allOf: [{ $ref: '#/components/schemas/TaskMatchedRuleGovernance' }]
              nullable: true
            override_summary:
              allOf: [{ $ref: '#/components/schemas/TaskCostOverrideSummary' }]
              nullable: true
            governance_audit_summary:
              allOf: [{ $ref: '#/components/schemas/TaskGovernanceAuditSummary' }]
              nullable: true
            override_governance_boundary:
              allOf: [{ $ref: '#/components/schemas/TaskCostOverrideGovernanceBoundary' }]
              nullable: true
            policy_mode:
              $ref: '#/components/schemas/PolicyMode'
            visible_to_roles:
              type: array
              items:
                $ref: '#/components/schemas/V7Role'
            action_roles:
              type: array
              items:
                $ref: '#/components/schemas/ActionPolicySummary'
            policy_scope_summary:
              $ref: '#/components/schemas/PolicyScopeSummary'
            platform_entry_boundary:
              allOf: [{ $ref: '#/components/schemas/PlatformEntryBoundary' }]
              nullable: true
            assignee_id:
              type: integer
              nullable: true
              description: Designer/assignee user id (same as designer_id when assigned).
            assignee_name:
              type: string
              nullable: true
              description: Resolved display name of assignee.
            requester_name:
              type: string
              nullable: true
              description: Resolved display name of requester / initiator.
            creator_name:
              type: string
              nullable: true
              description: Resolved display name of task creator.
            designer_name:
              type: string
              nullable: true
              description: Resolved display name of canonical `designer_id`.
            current_handler_name:
              type: string
              nullable: true
              description: Resolved display name of canonical `current_handler_id`.
            design_requirement:
              type: string
              nullable: true
              description: Design requirement text. For `original_product_development`, this is a compatibility alias carrying the same value as `change_request`; for `new_product_development`, it carries the submitted design requirement.
            change_request:
              type: string
              nullable: true
              description: Optional response field populated from `task_details.change_request`. Emitted for `original_product_development`, where it equals the original "修改要求"; omitted for `new_product_development` and `purchase_task`.
            note:
              type: string
              nullable: true
              description: Task note.
            reference_file_refs:
              type: array
              description: Formal task-level reference-image summary. Always present and may be empty. For batch tasks, this array is the task-level union summary; SKU-specific refs are returned on `sku_items[].reference_file_refs`.
              items:
                $ref: '#/components/schemas/ReferenceFileRef'
            design_assets:
              type: array
              description: Task design assets grouped by asset root. They become visible after upload-session completion and carry batch-SKU scope through `scope_sku_code`.
              items:
                $ref: '#/components/schemas/DesignAsset'
            asset_versions:
              type: array
              description: Flattened design asset versions ordered by asset root and version order. Batch-task SKU scope is exposed by `scope_sku_code`.
              items:
                $ref: '#/components/schemas/DesignAssetVersion'
    TaskSKUItem:
      type: object
      properties:
        id: { type: integer }
        task_id: { type: integer }
        sequence_no: { type: integer }
        sku_code: { type: string }
        sku_status:
          type: string
          enum: [reserved, generated, filed, filing_failed]
        product_id:
          type: integer
          nullable: true
        erp_product_id:
          type: string
          nullable: true
        filing_status:
          type: string
          enum: [not_filed, pending_filing, filing, filed, filing_failed]
          description: Per-SKU ERP filing status for batch tasks.
        erp_sync_status:
          type: string
          enum: [not_filed, pending_filing, filing, filed, filing_failed]
          description: Per-SKU ERP sync projection, currently mirrors filing_status.
        erp_sync_required: { type: boolean }
        erp_sync_version:
          type: integer
          format: int64
        last_filed_at:
          type: string
          format: date-time
          nullable: true
        filing_error_message: { type: string }
        product_name_snapshot: { type: string }
        product_short_name: { type: string }
        product_i_id:
          type: string
          description: Per-SKU ERP product i_id. For batch new-product tasks this is row-scoped and may differ across `sku_items`.
        category_code: { type: string }
        material_mode: { type: string }
        cost_price_mode: { type: string }
        quantity:
          type: integer
          format: int64
          nullable: true
        base_sale_price:
          type: number
          format: double
          nullable: true
        design_requirement:
          type: string
          nullable: true
          description: Per-SKU design requirement. For `original_product_development`, this is a compatibility alias carrying the same value as `change_request`.
        change_request:
          type: string
          nullable: true
          description: Optional per-SKU change request derived from task detail. Emitted for `original_product_development`; omitted for `new_product_development` and `purchase_task`.
        variant_json:
          type: object
          nullable: true
          additionalProperties: true
        reference_file_refs:
          type: array
          description: Formal per-SKU reference-image field for batch tasks. Empty means this SKU has no dedicated refs; use top-level `reference_file_refs` only as the task-level union summary.
          items:
            $ref: '#/components/schemas/ReferenceFileRef'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    TaskFilingStatusView:
      type: object
      properties:
        task_id: { type: integer }
        task_type:
          type: string
          enum: [original_product_development, new_product_development, purchase_task]
        task_status: { type: string }
        filing_status:
          type: string
          enum: [not_filed, pending_filing, filing, filed, filing_failed]
        filing_error_message: { type: string }
        filing_trigger_source: { type: string }
        last_filing_attempt_at: { type: string, format: date-time, nullable: true }
        last_filed_at: { type: string, format: date-time, nullable: true }
        erp_sync_required: { type: boolean }
        erp_sync_version: { type: integer, format: int64 }
        filed_at: { type: string, format: date-time, nullable: true }
        missing_fields:
          type: array
          items: { type: string }
        missing_fields_summary_cn: { type: string }
        can_retry: { type: boolean }
    RetryTaskFilingRequest:
      type: object
      properties:
        operator_id:
          type: integer
          nullable: true
        remark: { type: string }
    CreateTaskRequest:
      type: object
      description: Task creation payload for `original_product_development`, `new_product_development`, `purchase_task`, `retouch_task`, `customer_customization`, and `regular_customization`. Canonical task ownership is `owner_department` plus `owner_org_team`. `owner_team` remains accepted as a compatibility field. `original_product_development` does not support batch SKU creation. `new_product_development` and `purchase_task` accept `batch_sku_mode=multiple` with `batch_items`.
      required: [task_type, owner_team]
      properties:
        task_type:
          type: string
          enum: [original_product_development, new_product_development, purchase_task, retouch_task, customer_customization, regular_customization]
        source_mode:
          type: string
          enum: [existing_product, new_product]
          nullable: true
        owner_team:
          type: string
          description: Required compatibility owner-team input. Supported `/v1/org/options` org-team values with deterministic task mappings may be normalized before validation and persisted into canonical ownership fields. Unsupported values return `invalid_owner_team`.
        owner_department:
          type: string
          description: Optional canonical task owner department hint. When provided with `owner_org_team` or a compatible org-team `owner_team`, backend validates consistency before create.
        owner_org_team:
          type: string
          description: Optional canonical task owner org-team hint. When omitted, backend may resolve it from `owner_team` when the mapping is deterministic.
        due_at:
          type: string
          format: date-time
        deadline_at:
          type: string
          format: date-time
          nullable: true
        creator_id:
          type: integer
          nullable: true
        operator_group_id:
          type: integer
          nullable: true
        designer_id:
          type: integer
          nullable: true
        priority:
          type: string
          enum: [low, normal, high, critical]
        is_outsource:
          type: boolean
          description: Compatibility-only legacy create flag. When true, backend normalizes the request into `customization_required=true`.
        customization_required:
          type: boolean
          description: Canonical creation-time customization lane selector. When true, task enters customization review directly, bypasses the normal design workbench, and immediately gets one primary `customization_job`.
        customization_source_type:
          type: string
          enum: [new_product, existing_product]
          nullable: true
          description: Business source classification inside the customization lane; it does not select the lane by itself.
        reference_file_refs:
          type: array
          description: Reference file ref objects returned by `POST /v1/tasks/reference-upload` or the compatibility upload flow. Object arrays are the formal contract. `POST /v1/tasks` rejects direct `reference_images` payloads with `400 INVALID_REQUEST`.
          items:
            $ref: '#/components/schemas/ReferenceFileRef'
        remark:
          type: string
        note:
          type: string
        batch_sku_mode:
          type: string
          enum: [single, multiple]
          default: single
        source_draft_id:
          type: integer
          nullable: true
          description: "Optional task draft source linkage. Source: V1_INFORMATION_ARCHITECTURE §3.5.9."
        batch_items:
          type: array
          items:
            $ref: '#/components/schemas/CreateTaskBatchItem'
        product_id:
          type: integer
          nullable: true
        sku_code:
          type: string
        product_name_snapshot:
          type: string
        product_selection:
          allOf: [{ $ref: '#/components/schemas/TaskProductSelectionContext' }]
          nullable: true
        change_request:
          type: string
        category_code:
          type: string
          description: Backend-owned compatibility category/cost/SKU-prefix field. New frontend creation should prefer `i_id`; backend may resolve an internal `category_code` from `i_id` when possible.
        i_id:
          type: string
          description: Canonical frontend selector for product family/style. For new-product and purchase-task creation, pass a value selected from `GET /v1/erp/iids`.
        product_i_id:
          type: string
          description: Compatibility alias for `i_id`.
        material_mode:
          type: string
          enum: [preset, other]
        material:
          type: string
        material_other:
          type: string
        new_sku:
          type: string
        product_name:
          type: string
        product_short_name:
          type: string
        design_requirement:
          type: string
        cost_price_mode:
          type: string
          enum: [manual, template]
        cost_price:
          type: number
          format: double
          nullable: true
        quantity:
          type: integer
          nullable: true
        base_sale_price:
          type: number
          format: double
          nullable: true
        reference_link:
          type: string
        sync_erp_on_create:
          type: boolean
          description: When true, backend triggers the ERP product upsert immediately after task creation using the minimal create-time payload (`product_name`, generated/provided sku, and `i_id`). Later edits can enrich ERP data.
        purchase_sku:
          type: string
        product_channel:
          type: string
        demand_text:
          type: string
        copy_text:
          type: string
        style_keywords:
          type: string
    CreateTaskBatchItem:
      type: object
      properties:
        product_name:
          type: string
        product_short_name:
          type: string
        category_code:
          type: string
        i_id:
          type: string
          description: Per-row ERP product i_id selected from `GET /v1/erp/iids`. For batch new-product ERP filing, each row must carry its own i_id.
        product_i_id:
          type: string
          description: Compatibility alias for `i_id`.
        material_mode:
          type: string
          enum: [preset, other]
        design_requirement:
          type: string
        new_sku:
          type: string
        purchase_sku:
          type: string
        cost_price_mode:
          type: string
          enum: [manual, template]
        quantity:
          type: integer
          format: int64
          nullable: true
        base_sale_price:
          type: number
          format: double
          nullable: true
        variant_json:
          type: object
          nullable: true
          additionalProperties: true
        reference_file_refs:
          type: array
          description: Optional per-batch-line reference uploads using the same object contract as top-level `reference_file_refs`. When `batch_sku_mode=multiple`, the server validates and persists each line, returns it on `sku_items[].reference_file_refs`, maintains a deduplicated task-level union summary, and uses the first row-level reference as that SKU/product image during ERP filing.
          items:
            $ref: '#/components/schemas/ReferenceFileRef'
    PrepareTaskProductCodesRequest:
      type: object
      required: [task_type]
      properties:
        task_type:
          type: string
          enum: [new_product_development, purchase_task]
        category_code:
          type: string
          description: Required when `batch_items` is omitted.
        count:
          type: integer
          minimum: 1
          maximum: 500
          description: Defaults to 1 when omitted. Used only when `batch_items` is omitted.
        batch_items:
          type: array
          description: If provided, backend allocates one code per item and ignores top-level `count`.
          items:
            type: object
            required: [category_code]
            properties:
              category_code:
                type: string
    PrepareTaskProductCodesResponse:
      type: object
      properties:
        codes:
          type: array
          items:
            type: object
            properties:
              index:
                type: integer
              category_code:
                type: string
              sku_code:
                type: string
                description: "Auto-generated default task product code. Format: `NS` + two-letter uppercase category short code + 6-digit sequence (for example `NSKT000060`)."
    AssignTaskRequest:
      type: object
      description: |
        `assigned_by` is optional. When omitted, the backend uses the current authenticated actor.
      properties:
        designer_id:
          type: integer
          nullable: true
          description: Designer/assignee user id. Omit or send null on a single-task reassign to clear the assignee and return an InProgress task to PendingAssign.
        assigned_by:
          type: integer
          description: Optional override for compatibility. Defaults to the current authenticated actor.
        remark: { type: string }
    SubmitDesignRequest:
      type: object
      description: |
        `uploaded_by` is optional. When omitted, the backend uses the current authenticated actor.
        This contract supports two modes:
        1) compatibility single-asset submit (`asset_type` + `file_name`)
        2) batch submit (`assets[]`) that completes multiple upload sessions in one action.
        For batch multi-SKU delivery submits, each delivery item in `assets[]` must provide
        `target_sku_code`, and the value must match the upload session's captured `target_sku_code`.
      properties:
        uploaded_by:
          type: integer
          description: Optional override for compatibility. Defaults to the current authenticated actor.
        asset_type:
          type: string
          enum: [source, delivery]
        upload_request_id:
          type: string
        file_name: { type: string }
        mime_type:
          type: string
        file_size:
          type: integer
          format: int64
          nullable: true
        file_path:
          type: string
          nullable: true
        whole_hash:
          type: string
          nullable: true
        remark: { type: string }
        assets:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/SubmitDesignBatchAssetItem'
    SubmitDesignBatchAssetItem:
      type: object
      required: [upload_session_id]
      properties:
        upload_session_id:
          type: string
        asset_type:
          type: string
          enum: [source, delivery]
        asset_kind:
          type: string
          enum: [source, delivery]
          description: Canonical alias for `asset_type`.
        target_sku_code:
          type: string
          description: Required for delivery assets when the task is multi-SKU batch.
        file_hash:
          type: string
        upload_content_type:
          type: string
        remark:
          type: string
        oss_parts:
          type: array
          items:
            $ref: '#/components/schemas/OSSCompletePart'
        oss_upload_id:
          type: string
        oss_object_key:
          type: string
    SubmitDesignBatchResult:
      type: object
      properties:
        submitted_assets:
          type: array
          items:
            $ref: '#/components/schemas/CompleteTaskAssetUploadSessionResponseData'
    UpdateTaskBusinessInfoRequest:
      type: object
      description: |
        `operator_id` is optional. When omitted, the backend uses the current authenticated actor.
        Existing-product rebinding may pass `product_selection.erp_product` so the backend can
        cache or bind the chosen ERP Bridge product while preserving external id, sku, image,
        and price snapshot data under task-side `product_selection`.
        Filing follows backend trigger policy. `filed_at` and `trigger_filing` remain accepted
        as compatibility inputs and map to a forced filing trigger source.
        For original-product filing, `product_selection.erp_product` is the canonical source snapshot.
      properties:
        operator_id:
          type: integer
          description: Optional override for compatibility. Defaults to the current authenticated actor.
        product_name:
          type: string
          description: Clean product-name edit field. For new-product tasks this updates `tasks.product_name_snapshot` and is included in the next ERP filing payload.
        product_name_snapshot:
          type: string
          description: Compatibility alias for `product_name`.
        i_id:
          type: string
          description: Canonical Jushuitan product style/family i_id used by ERP filing. Prefer this over legacy category fields.
        product_i_id:
          type: string
          description: Compatibility alias for `i_id`.
        category: { type: string }
        category_id:
          type: integer
          nullable: true
        category_code: { type: string }
        spec_text: { type: string }
        material: { type: string }
        size_text: { type: string }
        design_requirement:
          type: string
          description: Editable demand text. For `original_product_development`, this is accepted as a compatibility alias and persisted to `change_request`; for `new_product_development` and `retouch_task`, it persists to `design_requirement`.
        change_request:
          type: string
          description: Editable original-product change request. For `new_product_development` and `retouch_task`, this is accepted as a compatibility alias of `design_requirement`.
        craft_text: { type: string }
        width:
          type: number
          format: double
          nullable: true
        height:
          type: number
          format: double
          nullable: true
        area:
          type: number
          format: double
          nullable: true
        quantity:
          type: integer
          format: int64
          nullable: true
        process: { type: string }
        product_selection:
          allOf: [{ $ref: '#/components/schemas/TaskProductSelectionContext' }]
          nullable: true
        cost_price:
          type: number
          format: double
          nullable: true
          description: Optional current effective cost. If `manual_cost_override=true`, this becomes the manual override value; otherwise the backend prefers system prefill when available.
        cost_rule_id:
          type: integer
          nullable: true
        cost_rule_name: { type: string }
        cost_rule_source: { type: string }
        manual_cost_override:
          type: boolean
          description: Business data flag only. It distinguishes user-entered override from system prefill and is not tied to auth/permissions.
        manual_cost_override_reason: { type: string }
        trigger_filing:
          type: boolean
          description: Legacy compatibility switch. Prefer backend auto-policy; this flag forces one filing evaluation.
        filed_at:
          type: string
          format: date-time
          nullable: true
          description: Legacy compatibility trigger timestamp. Backend maps this to a forced filing evaluation source.
        remark: { type: string }
    CreateCategoryRequest:
      type: object
      required: [category_code, category_name, category_type]
      properties:
        category_code: { type: string }
        category_name: { type: string }
        display_name: { type: string }
        parent_id:
          type: integer
          nullable: true
        level: { type: integer }
        search_entry_code:
          type: string
          description: Explicit first-level ERP search-entry code. Top-level categories must keep this equal to `category_code`.
        is_search_entry:
          type: boolean
          nullable: true
        category_type:
          $ref: '#/components/schemas/CategoryType'
        is_active:
          type: boolean
          nullable: true
        sort_order: { type: integer }
        source: { type: string }
        remark: { type: string }
    PatchCategoryRequest:
      type: object
      properties:
        category_code: { type: string }
        category_name: { type: string }
        display_name: { type: string }
        parent_id:
          type: integer
          nullable: true
        level: { type: integer }
        search_entry_code:
          type: string
        is_search_entry:
          type: boolean
          nullable: true
        category_type:
          $ref: '#/components/schemas/CategoryType'
        is_active:
          type: boolean
          nullable: true
        sort_order: { type: integer }
        source: { type: string }
        remark: { type: string }
    CreateCategoryERPMappingRequest:
      type: object
      required: [erp_match_type, erp_match_value]
      properties:
        category_id:
          type: integer
          nullable: true
        category_code: { type: string }
        search_entry_code: { type: string }
        erp_match_type:
          $ref: '#/components/schemas/CategoryERPMatchType'
        erp_match_value: { type: string }
        secondary_condition_key: { type: string }
        secondary_condition_value: { type: string }
        tertiary_condition_key: { type: string }
        tertiary_condition_value: { type: string }
        is_primary:
          type: boolean
          nullable: true
        is_active:
          type: boolean
          nullable: true
        priority: { type: integer }
        source: { type: string }
        remark: { type: string }
    PatchCategoryERPMappingRequest:
      type: object
      properties:
        category_id:
          type: integer
          nullable: true
        category_code: { type: string }
        search_entry_code: { type: string }
        erp_match_type:
          $ref: '#/components/schemas/CategoryERPMatchType'
        erp_match_value: { type: string }
        secondary_condition_key: { type: string }
        secondary_condition_value: { type: string }
        tertiary_condition_key: { type: string }
        tertiary_condition_value: { type: string }
        is_primary:
          type: boolean
          nullable: true
        is_active:
          type: boolean
          nullable: true
        priority: { type: integer }
        source: { type: string }
        remark: { type: string }
    CreateCostRuleRequest:
      type: object
      required: [rule_name, rule_type]
      properties:
        rule_name: { type: string }
        rule_version:
          type: integer
          nullable: true
          description: Optional explicit version for this row. If omitted while `supersedes_rule_id` is provided, the backend auto-increments from the referenced rule.
        category_id:
          type: integer
          nullable: true
        category_code: { type: string }
        product_family: { type: string }
        rule_type:
          $ref: '#/components/schemas/CostRuleType'
        base_price:
          type: number
          format: double
          nullable: true
        tax_multiplier:
          type: number
          format: double
          nullable: true
        min_area:
          type: number
          format: double
          nullable: true
        area_threshold:
          type: number
          format: double
          nullable: true
        surcharge_amount:
          type: number
          format: double
          nullable: true
        special_process_keyword: { type: string }
        special_process_price:
          type: number
          format: double
          nullable: true
        formula_expression: { type: string }
        priority: { type: integer }
        is_active:
          type: boolean
          nullable: true
        effective_from:
          type: string
          format: date-time
          nullable: true
        effective_to:
          type: string
          format: date-time
          nullable: true
        supersedes_rule_id:
          type: integer
          nullable: true
        governance_note:
          type: string
          description: Lightweight governance note only. This is not an approval workflow comment.
        source: { type: string }
        remark: { type: string }
    PatchCostRuleRequest:
      type: object
      properties:
        rule_name: { type: string }
        rule_version:
          type: integer
          nullable: true
        category_id:
          type: integer
          nullable: true
        category_code: { type: string }
        product_family: { type: string }
        rule_type:
          $ref: '#/components/schemas/CostRuleType'
        base_price:
          type: number
          format: double
          nullable: true
        tax_multiplier:
          type: number
          format: double
          nullable: true
        min_area:
          type: number
          format: double
          nullable: true
        area_threshold:
          type: number
          format: double
          nullable: true
        surcharge_amount:
          type: number
          format: double
          nullable: true
        special_process_keyword: { type: string }
        special_process_price:
          type: number
          format: double
          nullable: true
        formula_expression: { type: string }
        priority: { type: integer }
        is_active:
          type: boolean
          nullable: true
        effective_from:
          type: string
          format: date-time
          nullable: true
        effective_to:
          type: string
          format: date-time
          nullable: true
        supersedes_rule_id:
          type: integer
          nullable: true
        governance_note: { type: string }
        source: { type: string }
        remark: { type: string }
    CostRulePreviewRequest:
      type: object
      properties:
        category_id:
          type: integer
          nullable: true
        category_code: { type: string }
        width:
          type: number
          format: double
          nullable: true
        height:
          type: number
          format: double
          nullable: true
        area:
          type: number
          format: double
          nullable: true
        quantity:
          type: integer
          format: int64
          nullable: true
        process: { type: string }
        notes: { type: string }
    CostRulePreviewMatch:
      type: object
      properties:
        rule_id: { type: integer }
        rule_name: { type: string }
        rule_version: { type: integer }
        rule_type:
          $ref: '#/components/schemas/CostRuleType'
        priority: { type: integer }
        source: { type: string }
        governance_status:
          $ref: '#/components/schemas/CostRuleGovernanceStatus'
    CostRulePreviewResponse:
      type: object
      properties:
        matched_rule:
          allOf: [{ $ref: '#/components/schemas/CostRulePreviewMatch' }]
          nullable: true
        matched_rule_id:
          type: integer
          nullable: true
        matched_rule_version:
          type: integer
          nullable: true
        applied_rules:
          type: array
          items:
            $ref: '#/components/schemas/CostRulePreviewMatch'
        estimated_cost:
          type: number
          format: double
          nullable: true
        rule_source: { type: string }
        governance_status:
          $ref: '#/components/schemas/CostRuleGovernanceStatus'
        requires_manual_review: { type: boolean }
        explanation: { type: string }
    AssetDownloadInfo:
      type: object
      properties:
        download_mode:
          type: string
          description: Current OSS-only business runtime prefers `direct`; `proxy` is compatibility-only fallback.
        download_url: { type: string, nullable: true }
        access_hint: { type: string, nullable: true }
        preview_available:
          type: boolean
          description: Preview availability at response time.
        filename: { type: string }
        file_size:
          type: integer
          format: int64
        mime_type: { type: string }
        expires_at:
          type: string
          format: date-time
          nullable: true
          description: Expiry time for signed direct URL when present.
    ValidationViolation:
      type: object
      properties:
        field: { type: string }
        code: { type: string }
        message: { type: string }
    ErrorResponse:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              description: Unified error code for v1 routes. R1 contract-freeze routes additionally reserve the `DenyCode` enum.
              oneOf:
                - type: string
                - $ref: '#/components/schemas/DenyCode'
            message: { type: string }
            details:
              type: object
              additionalProperties: true
            violations:
              type: array
              items:
                $ref: '#/components/schemas/ValidationViolation'
            trace_id: { type: string }
    UpdateTaskProcurementRequest:
      type: object
      description: |
        `operator_id` is optional. When omitted, the backend uses the current authenticated actor.
      required: [status]
      properties:
        operator_id:
          type: integer
          description: Optional override for compatibility. Defaults to the current authenticated actor.
        status:
          $ref: '#/components/schemas/ProcurementStatus'
        procurement_price:
          type: number
          format: double
          nullable: true
        quantity:
          type: integer
          format: int64
          nullable: true
        supplier_name: { type: string }
        purchase_remark: { type: string }
        expected_delivery_at:
          type: string
          format: date-time
          nullable: true
        remark: { type: string }
    AdvanceTaskProcurementRequest:
      type: object
      description: |
        `operator_id` is optional. When omitted, the backend uses the current authenticated actor.
      required: [action]
      properties:
        operator_id:
          type: integer
          description: Optional override for compatibility. Defaults to the current authenticated actor.
        action:
          $ref: '#/components/schemas/ProcurementAction'
        remark: { type: string }
    PrepareWarehouseRequest:
      type: object
      description: |
        `operator_id` is optional. When omitted, the backend uses the current authenticated actor.
      properties:
        operator_id:
          type: integer
          description: Optional override for compatibility. Defaults to the current authenticated actor.
        remark: { type: string }
    CloseTaskRequest:
      type: object
      description: |
        `operator_id` is optional. When omitted, the backend uses the current authenticated actor.
      properties:
        operator_id:
          type: integer
          description: Optional override for compatibility. Defaults to the current authenticated actor.
        remark: { type: string }
    MockUploadTaskAssetRequest:
      type: object
      description: |
        `uploaded_by` is optional. When omitted, the backend uses the current authenticated actor.
      required: [asset_type, file_name]
      properties:
        uploaded_by:
          type: integer
          description: Optional override for compatibility. Defaults to the current authenticated actor.
        asset_type:
          type: string
          enum: [reference, source, delivery, preview, design_thumb]
        upload_request_id:
          type: string
        file_name: { type: string }
        mime_type:
          type: string
        file_size:
          type: integer
          format: int64
          nullable: true
        file_path:
          type: string
          nullable: true
        whole_hash:
          type: string
          nullable: true
        remark: { type: string }
    ProductListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ProductSearchResult'
        pagination:
          $ref: '#/components/schemas/PaginationMeta'
    ERPSyncRun:
      type: object
      properties:
        id: { type: integer }
        trigger_mode:
          type: string
          enum: [manual, scheduled]
        source_mode: { type: string, enum: [stub] }
        status:
          type: string
          enum: [success, noop, failed]
        total_received: { type: integer }
        total_upserted: { type: integer }
        error_message:
          type: string
          nullable: true
        started_at: { type: string, format: date-time }
        finished_at: { type: string, format: date-time }
        created_at: { type: string, format: date-time }
    ERPSyncRunResult:
      type: object
      properties:
        trigger_mode:
          type: string
          enum: [manual, scheduled]
        source_mode: { type: string, enum: [stub] }
        status:
          type: string
          enum: [success, noop, failed]
        total_received: { type: integer }
        total_upserted: { type: integer }
        error_message:
          type: string
          nullable: true
        started_at: { type: string, format: date-time }
        finished_at: { type: string, format: date-time }
    ERPSyncStatus:
      type: object
      properties:
        placeholder: { type: boolean }
        scheduler_enabled: { type: boolean }
        interval_seconds: { type: integer }
        source_mode: { type: string, enum: [stub] }
        stub_file:
          type: string
          description: Configured ERP sync source path as provided by runtime config. The current supported source is a local stub JSON input, not Bridge query ownership or formal external ERP connectivity.
        resolved_stub_file:
          type: string
          description: Runtime-resolved stub path after applying the current process working directory.
        stub_file_exists:
          type: boolean
          description: Whether `resolved_stub_file` currently exists as a readable file.
        latest_run:
          allOf: [{ $ref: '#/components/schemas/ERPSyncRun' }]
          nullable: true
    ERPSyncRunResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/ERPSyncRunResult'
    ERPSyncStatusResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/ERPSyncStatus'
    TaskListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/TaskListItem'
        pagination:
          $ref: '#/components/schemas/PaginationMeta'
    TaskQueryFilterDefinition:
      type: object
      description: Shared board/list filter contract. Queue `normalized_filters` map directly to `/v1/tasks` query semantics.
      properties:
        statuses:
          type: array
          items:
            type: string
        task_types:
          type: array
          items:
            type: string
            enum: [original_product_development, new_product_development, purchase_task]
        source_modes:
          type: array
          items:
            type: string
            enum: [existing_product, new_product]
        main_statuses:
          type: array
          items:
            $ref: '#/components/schemas/TaskMainStatus'
        sub_status_scope:
          $ref: '#/components/schemas/TaskSubStatusScope'
        sub_status_codes:
          type: array
          items:
            $ref: '#/components/schemas/TaskSubStatusCode'
        coordination_statuses:
          type: array
          items:
            $ref: '#/components/schemas/ProcurementCoordinationStatus'
        warehouse_prepare_ready:
          type: boolean
        warehouse_receive_ready:
          type: boolean
        warehouse_blocking_reason_codes:
          type: array
          items:
            type: string
            enum:
              - task_not_found
              - task_detail_missing
              - task_already_pending_warehouse
              - task_already_closed
              - task_awaiting_close
              - task_blocked
              - warehouse_already_received
              - warehouse_already_completed
              - missing_final_design_asset
              - audit_not_approved
              - missing_task_no
              - missing_sku
              - warehouse_not_received
              - warehouse_rejected_pending_resolution
              - warehouse_not_completed
              - pending_exception_resolution
              - filed_at_missing
              - category_missing
              - spec_text_missing
              - cost_price_missing
              - procurement_record_missing
              - procurement_price_missing
              - procurement_quantity_missing
              - procurement_not_ready
              - not_pending_close
        owner_departments:
          type: array
          items:
            type: string
        owner_org_teams:
          type: array
          items:
            type: string
    TaskQueryTemplate:
      type: object
      description: Direct board-to-list query template for `/v1/tasks`. Multi-value fields use comma-separated values.
      properties:
        status: { type: string }
        task_type: { type: string }
        source_mode: { type: string }
        workflow_lane: { type: string }
        main_status: { type: string }
        sub_status_code: { type: string }
        sub_status_scope:
          $ref: '#/components/schemas/TaskSubStatusScope'
        coordination_status: { type: string }
        warehouse_blocking_reason_code: { type: string }
        keyword: { type: string }
        owner_department: { type: string }
        owner_org_team: { type: string }
        creator_id: { type: integer }
        designer_id: { type: integer }
        need_outsource: { type: boolean }
        overdue: { type: boolean }
        warehouse_prepare_ready: { type: boolean }
        warehouse_receive_ready: { type: boolean }
    TaskBoardFiltersSchema:
      type: object
      properties:
        board_views:
          type: array
          items:
            $ref: '#/components/schemas/TaskBoardView'
        supported_global_filters:
          type: array
          items:
            type: string
        queue_condition_fields:
          type: array
          items:
            type: string
        task_list_endpoint:
          type: string
        task_list_passthrough_fields:
          type: array
          items:
            type: string
    TaskBoardQueueSummary:
      type: object
      properties:
        queue_key: { type: string }
        queue_name: { type: string }
        queue_description: { type: string }
        board_view:
          $ref: '#/components/schemas/TaskBoardView'
        suggested_roles:
          type: array
          description: Hint only. Suggested placeholder roles that usually operate this queue.
          items:
            $ref: '#/components/schemas/V7Role'
        suggested_actor_type:
          type: string
          description: Hint only. Does not enforce actor identity or assignment.
        default_visibility:
          type: string
          description: Hint only. Frontend presentation default, not a permission boundary.
        ownership_hint:
          type: string
          description: Hint only. Human-readable ownership guidance for workbench UX.
        filters:
          $ref: '#/components/schemas/TaskQueryFilterDefinition'
        normalized_filters:
          $ref: '#/components/schemas/TaskQueryFilterDefinition'
        query_template:
          $ref: '#/components/schemas/TaskQueryTemplate'
        count: { type: integer }
        sample_tasks:
          type: array
          items:
            $ref: '#/components/schemas/TaskListItem'
        policy_mode:
          $ref: '#/components/schemas/PolicyMode'
        visible_to_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        action_roles:
          type: array
          items:
            $ref: '#/components/schemas/ActionPolicySummary'
        policy_scope_summary:
          $ref: '#/components/schemas/PolicyScopeSummary'
    TaskBoardSummary:
      type: object
      properties:
        board_view:
          $ref: '#/components/schemas/TaskBoardView'
        board_name: { type: string }
        generated_at:
          type: string
          format: date-time
        filters_schema:
          $ref: '#/components/schemas/TaskBoardFiltersSchema'
        queues:
          type: array
          items:
            $ref: '#/components/schemas/TaskBoardQueueSummary'
        policy_mode:
          $ref: '#/components/schemas/PolicyMode'
        visible_to_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        action_roles:
          type: array
          items:
            $ref: '#/components/schemas/ActionPolicySummary'
        policy_scope_summary:
          $ref: '#/components/schemas/PolicyScopeSummary'
    TaskBoardSummaryResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/TaskBoardSummary'
    TaskBoardQueue:
      type: object
      properties:
        queue_key: { type: string }
        queue_name: { type: string }
        queue_description: { type: string }
        board_view:
          $ref: '#/components/schemas/TaskBoardView'
        suggested_roles:
          type: array
          description: Hint only. Suggested placeholder roles that usually operate this queue.
          items:
            $ref: '#/components/schemas/V7Role'
        suggested_actor_type:
          type: string
          description: Hint only. Does not enforce actor identity or assignment.
        default_visibility:
          type: string
          description: Hint only. Frontend presentation default, not a permission boundary.
        ownership_hint:
          type: string
          description: Hint only. Human-readable ownership guidance for workbench UX.
        filters:
          $ref: '#/components/schemas/TaskQueryFilterDefinition'
        normalized_filters:
          $ref: '#/components/schemas/TaskQueryFilterDefinition'
        query_template:
          $ref: '#/components/schemas/TaskQueryTemplate'
        count: { type: integer }
        tasks:
          type: array
          items:
            $ref: '#/components/schemas/TaskListItem'
        pagination:
          $ref: '#/components/schemas/PaginationMeta'
        policy_mode:
          $ref: '#/components/schemas/PolicyMode'
        visible_to_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        action_roles:
          type: array
          items:
            $ref: '#/components/schemas/ActionPolicySummary'
        policy_scope_summary:
          $ref: '#/components/schemas/PolicyScopeSummary'
    TaskBoardQueuesResponse:
      type: object
      properties:
        board_view:
          $ref: '#/components/schemas/TaskBoardView'
        board_name: { type: string }
        generated_at:
          type: string
          format: date-time
        filters_schema:
          $ref: '#/components/schemas/TaskBoardFiltersSchema'
        queues:
          type: array
          items:
            $ref: '#/components/schemas/TaskBoardQueue'
        policy_mode:
          $ref: '#/components/schemas/PolicyMode'
        visible_to_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        action_roles:
          type: array
          items:
            $ref: '#/components/schemas/ActionPolicySummary'
        policy_scope_summary:
          $ref: '#/components/schemas/PolicyScopeSummary'
    TaskBoardQueuesAPIResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/TaskBoardQueuesResponse'
    WorkbenchSortKey:
      type: string
      enum: [updated_at_desc]
    WorkbenchPreferences:
      type: object
      description: Effective saved workbench preferences resolved for the current session-backed user.
      properties:
        default_queue_key:
          type: string
          description: Optional preset queue to restore by default.
        pinned_queue_keys:
          type: array
          items:
            type: string
        default_filters:
          $ref: '#/components/schemas/TaskQueryTemplate'
        default_page_size:
          type: integer
          description: Effective page size for workbench queues. The response resolves the current default if no explicit value was saved.
        default_sort:
          $ref: '#/components/schemas/WorkbenchSortKey'
    WorkbenchPreferencesPatch:
      type: object
      description: Partial update payload for lightweight user-scoped saved workbench preferences. Empty string or `0` clears the saved queue/sort/page-size back to the current default.
      properties:
        default_queue_key:
          type: string
        pinned_queue_keys:
          type: array
          items:
            type: string
        default_filters:
          $ref: '#/components/schemas/TaskQueryTemplate'
        default_page_size:
          type: integer
          enum: [0, 10, 20, 50, 100]
        default_sort:
          type: string
          enum: ['', updated_at_desc]
    WorkbenchQueueConfig:
      type: object
      properties:
        queue_key: { type: string }
        queue_name: { type: string }
        queue_description: { type: string }
        board_view:
          $ref: '#/components/schemas/TaskBoardView'
        suggested_roles:
          type: array
          description: Hint only. Suggested placeholder roles that usually operate this queue.
          items:
            $ref: '#/components/schemas/V7Role'
        suggested_actor_type:
          type: string
          description: Hint only. Does not enforce actor identity or assignment.
        default_visibility:
          type: string
          description: Hint only. Frontend presentation default, not a permission boundary.
        ownership_hint:
          type: string
          description: Hint only. Human-readable ownership guidance for workbench UX.
        filters:
          $ref: '#/components/schemas/TaskQueryFilterDefinition'
        normalized_filters:
          $ref: '#/components/schemas/TaskQueryFilterDefinition'
        query_template:
          $ref: '#/components/schemas/TaskQueryTemplate'
    WorkbenchConfig:
      type: object
      properties:
        filters_schema:
          $ref: '#/components/schemas/TaskBoardFiltersSchema'
        supported_sorts:
          type: array
          items:
            $ref: '#/components/schemas/WorkbenchSortKey'
        supported_page_sizes:
          type: array
          items:
            type: integer
        queues:
          type: array
          items:
            $ref: '#/components/schemas/WorkbenchQueueConfig'
    WorkbenchPreferencesEnvelope:
      type: object
      properties:
        actor:
          $ref: '#/components/schemas/RequestActor'
        preferences:
          $ref: '#/components/schemas/WorkbenchPreferences'
        workbench_config:
          $ref: '#/components/schemas/WorkbenchConfig'
    WorkbenchPreferencesResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/WorkbenchPreferencesEnvelope'
    ExportType:
      type: string
      enum: [task_list, task_board_queue, procurement_summary, warehouse_receipts]
    ExportSourceQueryType:
      type: string
      enum: [task_query, task_board_queue, procurement_summary, warehouse_receipts]
    ExportJobStatus:
      type: string
      enum: [queued, running, ready, failed, cancelled]
    ExportJobProgressHint:
      type: string
      enum: [created, processing, download_ready, failed, cancelled]
    ExportJobStartMode:
      type: string
      enum: [explicit_internal_start]
      description: Current placeholder start-boundary mode for export jobs. This does not imply a real async runner platform.
    ExportJobExecutionMode:
      type: string
      enum: [manual_placeholder_runner]
      description: Current placeholder execution mode. Future real runners should replace this layer without changing handoff APIs.
    ExportJobDeliveryMode:
      type: string
      enum: [claim_read_refresh_handoff]
      description: Current planning-only delivery mode behind claim/read/refresh handoff APIs.
    ExportJobExecutionBoundary:
      type: object
      description: Planning-only execution-boundary read model. Clarifies which current layer starts execution, hands off dispatch, records attempts, and mints placeholder result state before any real runner platform is introduced.
      properties:
        boundary_key:
          type: string
        start_layer:
          type: string
        dispatch_layer:
          type: string
        attempt_layer:
          type: string
        result_generation_layer:
          type: string
        future_runner_replace_layer:
          type: string
        placeholder:
          type: boolean
        note:
          type: string
    ExportJobStorageBoundary:
      type: object
      description: Planning-only storage-boundary read model. Clarifies that current ready-state output is represented by `result_ref` metadata only, not real file persistence.
      properties:
        boundary_key:
          type: string
        result_source_layer:
          type: string
        storage_layer:
          type: string
        storage_ref_field:
          type: string
        future_storage_replace_layer:
          type: string
        placeholder:
          type: boolean
        note:
          type: string
    ExportJobDeliveryBoundary:
      type: object
      description: Planning-only delivery-boundary read model. Clarifies that claim/read/refresh currently deliver handoff metadata only and are the future replacement seam for a real download service.
      properties:
        boundary_key:
          type: string
        delivery_layer:
          type: string
        delivery_ref_field:
          type: string
        claim_action:
          type: string
        read_action:
          type: string
        refresh_action:
          type: string
        future_delivery_replace_layer:
          type: string
        placeholder:
          type: boolean
        note:
          type: string
    ExportJobRunnerAdapterKey:
      type: string
      enum: [manual_placeholder_adapter]
      description: Current placeholder runner-adapter identity. Future real scheduler/runner integration should replace this layer rather than the export-job lifecycle contract.
    ExportJobAttemptStatus:
      type: string
      enum: [running, succeeded, failed, cancelled]
      description: Status of one concrete export-job execution attempt, separate from the export job lifecycle.
    ExportJobDispatchStatus:
      type: string
      enum: [submitted, received, rejected, expired, not_executed]
      description: Status of one placeholder adapter-dispatch handoff, separate from both export job lifecycle and attempt lifecycle.
    ExportJobAdvanceAction:
      type: string
      enum: [start, mark_ready, fail, cancel, requeue]
    ExportSourceFilters:
      type: object
      properties:
        queue_key:
          type: string
          description: Required for `task_board_queue` export jobs so the persisted job can point back to one stable board queue definition.
        board_view:
          $ref: '#/components/schemas/TaskBoardView'
        task_id:
          type: integer
          nullable: true
        receiver_id:
          type: integer
          nullable: true
        status:
          type: string
          description: Warehouse-receipt status filter when `source_query_type=warehouse_receipts`.
    ExportResultRef:
      type: object
      description: Structured placeholder download-handoff metadata only. This is not a real NAS path, signed URL, or storage integration. Its semantics align with task-asset `storage_ref` metadata, but the models remain separate.
      properties:
        ref_type:
          type: string
        ref_key:
          type: string
        file_name:
          type: string
        mime_type:
          type: string
        expires_at:
          type: string
          format: date-time
          nullable: true
          description: Placeholder handoff expiry timestamp. Before expiry it can gate claim/read; after expiry refresh is required.
        is_placeholder:
          type: boolean
        note:
          type: string
    ExportJobEventSummary:
      type: object
      properties:
        event_id:
          type: string
        export_job_id:
          type: integer
        sequence:
          type: integer
        event_type:
          type: string
          description: |
            Export-job audit event type. Current runner-boundary events include
            `export_job.runner_initiated`, `export_job.started`, and attempt-result events
            such as `export_job.attempt_succeeded`, but event payload remains audit context
            only rather than a full runner log stream.
        from_status:
          $ref: '#/components/schemas/ExportJobStatus'
        to_status:
          $ref: '#/components/schemas/ExportJobStatus'
        actor_id:
          type: integer
        actor_type:
          type: string
          description: Placeholder actor source/type for the current phase.
        note:
          type: string
        created_at:
          type: string
          format: date-time
    ExportJobEvent:
      allOf:
        - $ref: '#/components/schemas/ExportJobEventSummary'
        - type: object
          properties:
            payload:
              description: Freeform audit context only. This is not a full runner log stream.
              nullable: true
    ExportJobDownloadHandoff:
      type: object
      description: |
        Structured placeholder download-handoff response for ready export jobs. This is not a real
        file-download service and does not return bytes, signed URLs, NAS paths, or object-storage handles.
        `is_expired` and `can_refresh` describe placeholder handoff lifecycle only.
      properties:
        export_job_id:
          type: integer
        status:
          $ref: '#/components/schemas/ExportJobStatus'
        download_ready:
          type: boolean
        claim_available:
          type: boolean
          description: Whether placeholder download handoff can currently be claimed.
        read_available:
          type: boolean
          description: Whether placeholder download handoff can currently be read.
        is_expired:
          type: boolean
          description: Whether the current placeholder handoff is expired.
        can_refresh:
          type: boolean
          description: Whether the current placeholder handoff can be refreshed in this phase.
        result_ref:
          $ref: '#/components/schemas/ExportResultRef'
        file_name:
          type: string
        mime_type:
          type: string
        is_placeholder:
          type: boolean
        expires_at:
          type: string
          format: date-time
          nullable: true
        note:
          type: string
        claimed_at:
          type: string
          format: date-time
          nullable: true
        claimed_by_actor_id:
          type: integer
          nullable: true
        claimed_by_actor_type:
          type: string
        last_read_at:
          type: string
          format: date-time
          nullable: true
        last_read_by_actor_id:
          type: integer
          nullable: true
        last_read_by_actor_type:
          type: string
    ExportJobDownloadHandoffResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/ExportJobDownloadHandoff'
    ExportJobAttempt:
      type: object
      description: |
        One placeholder execution attempt for an export job. This is not a real worker lease,
        scheduler callback, or distributed runner telemetry stream. It exists so export-job
        lifecycle and per-attempt execution state remain separately visible.
      properties:
        attempt_id:
          type: string
        export_job_id:
          type: integer
        dispatch_id:
          type: string
          description: Placeholder dispatch handoff consumed by this attempt, when available.
        attempt_no:
          type: integer
        trigger_source:
          type: string
          description: Current trigger source for this start attempt, such as explicit `/start` or backward-compatible `advance action=start`.
        execution_mode:
          $ref: '#/components/schemas/ExportJobExecutionMode'
        adapter_key:
          $ref: '#/components/schemas/ExportJobRunnerAdapterKey'
        status:
          $ref: '#/components/schemas/ExportJobAttemptStatus'
        started_at:
          type: string
          format: date-time
        finished_at:
          type: string
          format: date-time
          nullable: true
        error_message:
          type: string
        adapter_note:
          type: string
          description: Placeholder adapter-side note only. This is not full runner telemetry.
        blocks_new_attempt:
          type: boolean
          description: Lightweight hint derived from this attempt status only. It does not replace job-level start/attempt admission.
        next_attempt_admission_reason:
          type: string
          description: Lightweight reason hint derived from this attempt status only.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ExportJobDispatch:
      type: object
      description: |
        One placeholder adapter-dispatch handoff for an export job. This is not a real scheduler
        queue item, lease, worker callback, or distributed delivery contract; it only makes the
        dispatch boundary explicit ahead of any future real runner platform.
      properties:
        dispatch_id:
          type: string
        export_job_id:
          type: integer
        dispatch_no:
          type: integer
        trigger_source:
          type: string
        execution_mode:
          $ref: '#/components/schemas/ExportJobExecutionMode'
        adapter_key:
          $ref: '#/components/schemas/ExportJobRunnerAdapterKey'
        status:
          $ref: '#/components/schemas/ExportJobDispatchStatus'
        submitted_at:
          type: string
          format: date-time
        received_at:
          type: string
          format: date-time
          nullable: true
        finished_at:
          type: string
          format: date-time
          nullable: true
        expires_at:
          type: string
          format: date-time
          nullable: true
        status_reason:
          type: string
        adapter_note:
          type: string
        start_admissible:
          type: boolean
          description: Whether this dispatch status itself can be consumed directly by `/start`.
        start_admission_reason:
          type: string
          description: Dispatch-level start-admission reason derived from dispatch status.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ExportJobDispatchListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ExportJobDispatch'
    ExportJobAttemptListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ExportJobAttempt'
    ExportTemplate:
      type: object
      properties:
        key:
          type: string
        name:
          type: string
        description:
          type: string
        export_type:
          $ref: '#/components/schemas/ExportType'
        supported_source_query_types:
          type: array
          items:
            $ref: '#/components/schemas/ExportSourceQueryType'
        result_format:
          type: string
        placeholder_only:
          type: boolean
    ExportJobAdmissionDecision:
      type: object
      description: |
        Lightweight latest admission decision summary on export-job read models.
        This is a read-model interpretation only and not a persisted independent event stream.
      properties:
        decision_type:
          type: string
          enum: [dispatch, redispatch, start, attempt]
        allowed:
          type: boolean
        reason:
          type: string
        job_status:
          $ref: '#/components/schemas/ExportJobStatus'
        latest_dispatch_status:
          $ref: '#/components/schemas/ExportJobDispatchStatus'
        latest_attempt_status:
          $ref: '#/components/schemas/ExportJobAttemptStatus'
    ExportJob:
      type: object
      properties:
        export_job_id:
          type: integer
        template_key:
          type: string
        export_type:
          $ref: '#/components/schemas/ExportType'
        source_query_type:
          $ref: '#/components/schemas/ExportSourceQueryType'
        source_filters:
          $ref: '#/components/schemas/ExportSourceFilters'
        normalized_filters:
          $ref: '#/components/schemas/TaskQueryFilterDefinition'
        query_template:
          $ref: '#/components/schemas/TaskQueryTemplate'
        requested_by:
          $ref: '#/components/schemas/RequestActor'
        status:
          $ref: '#/components/schemas/ExportJobStatus'
        progress_hint:
          $ref: '#/components/schemas/ExportJobProgressHint'
        latest_status_at:
          type: string
          format: date-time
        download_ready:
          type: boolean
        can_start:
          type: boolean
          description: Whether the export job can currently cross the explicit placeholder runner-initiation boundary, including dispatch-side blocking checks.
        can_start_reason:
          type: string
          description: Machine-readable reason for current `can_start` admission decision.
        can_attempt:
          type: boolean
          description: Whether a new placeholder attempt can currently be created through the start boundary.
        can_attempt_reason:
          type: string
          description: Machine-readable reason for current `can_attempt` decision.
        can_retry:
          type: boolean
          description: Whether the export job has previous attempts and is currently back in `queued`, meaning a new placeholder retry attempt can be started now.
        can_dispatch:
          type: boolean
          description: Whether a new placeholder adapter-dispatch handoff can currently be submitted.
        can_dispatch_reason:
          type: string
          description: Machine-readable reason for current `can_dispatch` admission decision.
        can_redispatch:
          type: boolean
          description: Whether the export job already has historical dispatches and can submit another one now.
        can_redispatch_reason:
          type: string
          description: Machine-readable reason for current `can_redispatch` admission decision.
        dispatchability_reason:
          type: string
          description: Current dispatch-side admission reason alias for list/detail filtering and troubleshooting.
        attemptability_reason:
          type: string
          description: Current attempt-side admission reason alias for list/detail filtering and troubleshooting.
        latest_admission_decision:
          $ref: '#/components/schemas/ExportJobAdmissionDecision'
        start_mode:
          $ref: '#/components/schemas/ExportJobStartMode'
        execution_mode:
          $ref: '#/components/schemas/ExportJobExecutionMode'
        adapter_mode:
          $ref: '#/components/schemas/AdapterMode'
        dispatch_mode:
          $ref: '#/components/schemas/DispatchMode'
        storage_mode:
          $ref: '#/components/schemas/BoundaryStorageMode'
        delivery_mode:
          $ref: '#/components/schemas/ExportJobDeliveryMode'
        adapter_ref_summary:
          $ref: '#/components/schemas/AdapterRefSummary'
        resource_ref_summary:
          $ref: '#/components/schemas/ResourceRefSummary'
        handoff_ref_summary:
          $ref: '#/components/schemas/HandoffRefSummary'
        execution_boundary:
          $ref: '#/components/schemas/ExportJobExecutionBoundary'
        storage_boundary:
          $ref: '#/components/schemas/ExportJobStorageBoundary'
        delivery_boundary:
          $ref: '#/components/schemas/ExportJobDeliveryBoundary'
        is_expired:
          type: boolean
          description: Whether the current placeholder handoff is expired.
        can_refresh:
          type: boolean
          description: Whether the current placeholder handoff can be refreshed in this phase.
        result_ref:
          $ref: '#/components/schemas/ExportResultRef'
        dispatch_count:
          type: integer
          description: Number of persisted placeholder dispatch handoffs for this export job.
        latest_dispatch:
          $ref: '#/components/schemas/ExportJobDispatch'
        attempt_count:
          type: integer
          description: Number of persisted execution attempts for this export job.
        latest_attempt:
          $ref: '#/components/schemas/ExportJobAttempt'
        event_count:
          type: integer
          description: Lightweight lifecycle-audit summary count for the export job.
        latest_event:
          $ref: '#/components/schemas/ExportJobEventSummary'
        latest_dispatch_event:
          $ref: '#/components/schemas/ExportJobEventSummary'
        latest_runner_event:
          $ref: '#/components/schemas/ExportJobEventSummary'
        policy_mode:
          $ref: '#/components/schemas/PolicyMode'
        visible_to_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        action_roles:
          type: array
          items:
            $ref: '#/components/schemas/ActionPolicySummary'
        policy_scope_summary:
          $ref: '#/components/schemas/PolicyScopeSummary'
        platform_entry_boundary:
          allOf: [{ $ref: '#/components/schemas/PlatformEntryBoundary' }]
          nullable: true
        remark:
          type: string
        created_at:
          type: string
          format: date-time
        finished_at:
          type: string
          format: date-time
          nullable: true
        updated_at:
          type: string
          format: date-time
    CreateExportJobRequest:
      type: object
      required: [export_type, source_query_type]
      properties:
        export_type:
          $ref: '#/components/schemas/ExportType'
        template_key:
          type: string
          description: Optional static template key. When omitted, the backend chooses the default skeleton template for the selected `export_type`.
        source_query_type:
          $ref: '#/components/schemas/ExportSourceQueryType'
        source_filters:
          $ref: '#/components/schemas/ExportSourceFilters'
        normalized_filters:
          $ref: '#/components/schemas/TaskQueryFilterDefinition'
        query_template:
          $ref: '#/components/schemas/TaskQueryTemplate'
        remark:
          type: string
      description: |
        Creates a minimal export job skeleton. For task-query-derived exports, frontend should pass the current stable
        `query_template` from `/v1/tasks` or the board queue handoff, optionally with `normalized_filters`. For
        `task_board_queue` exports, `source_filters.queue_key` is required. For `warehouse_receipts`, use
        `source_filters.task_id` / `receiver_id` / `status` instead of task-query fields.
    ExportTemplateListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ExportTemplate'
    IntegrationConnectorKey:
      type: string
      enum: [erp_product_stub, erp_bridge_product_upsert, export_adapter_bridge]
    IntegrationCallDirection:
      type: string
      enum: [outbound, inbound]
    IntegrationCallStatus:
      type: string
      enum: [queued, sent, succeeded, failed, cancelled]
    IntegrationCallProgressHint:
      type: string
      enum: [queued, in_flight, succeeded, failed, cancelled]
    IntegrationExecutionMode:
      type: string
      enum: [manual_placeholder_adapter]
    IntegrationExecutionStatus:
      type: string
      enum: [prepared, dispatched, received, completed, failed, cancelled]
    IntegrationExecutionActionType:
      type: string
      enum: [start, retry, replay, compat]
    IntegrationConnector:
      type: object
      properties:
        key:
          $ref: '#/components/schemas/IntegrationConnectorKey'
        name:
          type: string
        description:
          type: string
        direction:
          $ref: '#/components/schemas/IntegrationCallDirection'
        placeholder_only:
          type: boolean
    IntegrationConnectorListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/IntegrationConnector'
    IntegrationCallLog:
      type: object
      description: Internal integration call log. It records the request envelope above execution attempts, exposes retry/replay admission hints, and is also used for the narrow ERP Bridge product-filing trace under connector `erp_bridge_product_upsert`.
      properties:
        call_log_id:
          type: integer
        connector_key:
          $ref: '#/components/schemas/IntegrationConnectorKey'
        operation_key:
          type: string
        direction:
          $ref: '#/components/schemas/IntegrationCallDirection'
        resource_type:
          type: string
        resource_id:
          type: integer
          nullable: true
        status:
          $ref: '#/components/schemas/IntegrationCallStatus'
        progress_hint:
          $ref: '#/components/schemas/IntegrationCallProgressHint'
        adapter_mode:
          $ref: '#/components/schemas/AdapterMode'
        dispatch_mode:
          $ref: '#/components/schemas/DispatchMode'
        requested_by:
          $ref: '#/components/schemas/RequestActor'
        request_payload:
          description: Placeholder request payload snapshot only.
          nullable: true
        response_payload:
          description: Placeholder response payload snapshot only.
          nullable: true
        error_message:
          type: string
        latest_status_at:
          type: string
          format: date-time
        started_at:
          type: string
          format: date-time
          nullable: true
        finished_at:
          type: string
          format: date-time
          nullable: true
        execution_count:
          type: integer
        latest_execution:
          $ref: '#/components/schemas/IntegrationExecution'
        can_retry:
          type: boolean
        can_replay:
          type: boolean
        retry_count:
          type: integer
        replay_count:
          type: integer
        latest_retry_action:
          $ref: '#/components/schemas/IntegrationExecutionActionSummary'
        latest_replay_action:
          $ref: '#/components/schemas/IntegrationExecutionActionSummary'
        retryability_reason:
          type: string
        replayability_reason:
          type: string
        adapter_ref_summary:
          $ref: '#/components/schemas/AdapterRefSummary'
        handoff_ref_summary:
          $ref: '#/components/schemas/HandoffRefSummary'
        policy_mode:
          $ref: '#/components/schemas/PolicyMode'
        visible_to_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        action_roles:
          type: array
          items:
            $ref: '#/components/schemas/ActionPolicySummary'
        policy_scope_summary:
          $ref: '#/components/schemas/PolicyScopeSummary'
        remark:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    IntegrationCallLogResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/IntegrationCallLog'
    IntegrationCallLogListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/IntegrationCallLog'
        pagination:
          $ref: '#/components/schemas/PaginationMeta'
    IntegrationExecution:
      type: object
      description: One placeholder integration execution attempt beneath one call log. `action_type` distinguishes manual start, retry, replay, and compatibility actions on the same execution boundary. This is not an external worker, callback stream, or retry platform.
      properties:
        execution_id:
          type: string
        call_log_id:
          type: integer
        connector_key:
          $ref: '#/components/schemas/IntegrationConnectorKey'
        execution_no:
          type: integer
        execution_mode:
          $ref: '#/components/schemas/IntegrationExecutionMode'
        action_type:
          $ref: '#/components/schemas/IntegrationExecutionActionType'
        adapter_mode:
          $ref: '#/components/schemas/AdapterMode'
        dispatch_mode:
          $ref: '#/components/schemas/DispatchMode'
        trigger_source:
          type: string
        status:
          $ref: '#/components/schemas/IntegrationExecutionStatus'
        latest_status_at:
          type: string
          format: date-time
        started_at:
          type: string
          format: date-time
        finished_at:
          type: string
          format: date-time
          nullable: true
        error_message:
          type: string
        adapter_note:
          type: string
        retryable:
          type: boolean
        adapter_ref_summary:
          $ref: '#/components/schemas/AdapterRefSummary'
        handoff_ref_summary:
          $ref: '#/components/schemas/HandoffRefSummary'
        policy_mode:
          $ref: '#/components/schemas/PolicyMode'
        visible_to_roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
        action_roles:
          type: array
          items:
            $ref: '#/components/schemas/ActionPolicySummary'
        policy_scope_summary:
          $ref: '#/components/schemas/PolicyScopeSummary'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    IntegrationExecutionActionSummary:
      type: object
      description: Latest retry/replay action summary derived from persisted execution attempts. It reuses the execution boundary instead of introducing a second action-history subsystem.
      properties:
        action_type:
          $ref: '#/components/schemas/IntegrationExecutionActionType'
        execution_id:
          type: string
        execution_no:
          type: integer
        trigger_source:
          type: string
        status:
          $ref: '#/components/schemas/IntegrationExecutionStatus'
        retryable:
          type: boolean
        latest_status_at:
          type: string
          format: date-time
        finished_at:
          type: string
          format: date-time
          nullable: true
    IntegrationExecutionResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/IntegrationExecution'
    IntegrationExecutionListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/IntegrationExecution'
    CreateIntegrationCallLogRequest:
      type: object
      required: [connector_key, operation_key, direction]
      properties:
        connector_key:
          $ref: '#/components/schemas/IntegrationConnectorKey'
        operation_key:
          type: string
        direction:
          $ref: '#/components/schemas/IntegrationCallDirection'
        resource_type:
          type: string
        resource_id:
          type: integer
          nullable: true
        request_payload:
          nullable: true
        remark:
          type: string
    AdvanceIntegrationCallLogRequest:
      type: object
      required: [status]
      properties:
        status:
          $ref: '#/components/schemas/IntegrationCallStatus'
        response_payload:
          nullable: true
        error_message:
          type: string
        remark:
          type: string
    CreateIntegrationExecutionRequest:
      type: object
      properties:
        execution_mode:
          $ref: '#/components/schemas/IntegrationExecutionMode'
        trigger_source:
          type: string
        adapter_note:
          type: string
    AdvanceIntegrationExecutionRequest:
      type: object
      required: [status]
      properties:
        status:
          $ref: '#/components/schemas/IntegrationExecutionStatus'
        response_payload:
          nullable: true
        error_message:
          type: string
        adapter_note:
          type: string
        retryable:
          type: boolean
    IntegrationExecutionActionRequest:
      type: object
      properties:
        execution_mode:
          $ref: '#/components/schemas/IntegrationExecutionMode'
        adapter_note:
          type: string
    CreateExportJobDispatchRequest:
      type: object
      properties:
        trigger_source:
          type: string
          description: Optional placeholder handoff source. Defaults to a manual internal dispatch source.
        expires_at:
          type: string
          format: date-time
          nullable: true
          description: Optional placeholder dispatch expiry timestamp.
        remark:
          type: string
    AdvanceExportJobDispatchRequest:
      type: object
      required: [action]
      properties:
        action:
          type: string
          enum: [receive, reject, expire, mark_not_executed]
        reason:
          type: string
        remark:
          type: string
    ExportJobResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/ExportJob'
    ExportJobListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ExportJob'
        pagination:
          $ref: '#/components/schemas/PaginationMeta'
    ExportJobEventListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ExportJobEvent'
    AdvanceExportJobRequest:
      type: object
      required: [action]
      properties:
        action:
          $ref: '#/components/schemas/ExportJobAdvanceAction'
        result_file_name:
          type: string
          description: Optional placeholder handoff file name override used when `action=mark_ready`.
        result_mime_type:
          type: string
          description: Optional placeholder MIME type override used when `action=mark_ready`.
        expires_at:
          type: string
          format: date-time
          description: Optional placeholder download-handoff expiry used when `action=mark_ready`.
        failure_reason:
          type: string
          description: Optional failure note used when `action=fail`.
        remark:
          type: string
      description: |
        Internal/admin lifecycle-advance skeleton for export jobs. This endpoint is for manual lifecycle progression only
        and is not a real async runner, scheduler, or file-generation platform. `action=start` remains
        supported for backward compatibility, but `POST /v1/export-jobs/{id}/start` is now the formal
        placeholder runner-initiation boundary.
    OutsourceOrderListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/OutsourceOrder'
        pagination:
          $ref: '#/components/schemas/PaginationMeta'
    WarehouseReceiptListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/WarehouseReceipt'
        pagination:
          $ref: '#/components/schemas/PaginationMeta'
    CodeRule:
      type: object
      properties:
        id: { type: integer }
        rule_type:
          type: string
          enum: [task_no, new_sku, outsource_no, handover_no]
        rule_name: { type: string }
        prefix: { type: string }
        date_format: { type: string }
        site_code: { type: string }
        biz_code: { type: string }
        seq_length: { type: integer }
        reset_cycle:
          type: string
          enum: [none, daily, monthly]
        is_enabled: { type: boolean }
        config_json: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    CodePreview:
      type: object
      properties:
        rule_id: { type: integer }
        preview: { type: string }
        is_preview: { type: boolean }
    APIReadiness:
      type: string
      enum: [ready_for_frontend, internal_placeholder, mock_placeholder_only]
    OSSCompletePart:
      type: object
      properties:
        part_number:
          type: integer
        etag:
          type: string
    TaskModuleState:
      type: string
      description: "Source: V1_MODULE_ARCHITECTURE §3 / §7 / V1_CUSTOMIZATION_WORKFLOW §2."
      enum: [active, pending_claim, in_progress, submitted, approved, rejected, closed, forcibly_closed, pending, preparing, received, completed]
    TaskModuleAction:
      type: string
      description: "Source: V1_MODULE_ARCHITECTURE §6 Layer 3 / R1 prompt §1.2."
      enum: [claim, submit, approve, reject, reassign, pool_reassign, asset_upload_session_create, update_reference_files, update_basic_info, update_deadline, update_priority, close_task, cancel_task]
    DenyCode:
      type: string
      description: "Minimum deny-code registry frozen in R1. Source: V1_MODULE_ARCHITECTURE §6.2 / R1 prompt §1.3."
      enum: [not_implemented, task_not_found, module_not_instantiated, module_out_of_scope, module_state_mismatch, module_action_role_denied, module_claim_conflict, module_blueprint_missing_team, task_already_claimed, asset_gone, draft_not_found, draft_quota_exceeded, org_move_requires_superadmin, ws_upgrade_failed]
    Actor:
      type: object
      description: "Source: V1_MODULE_ARCHITECTURE §5.2 / V1_INFORMATION_ARCHITECTURE §7.2."
      properties:
        user_id:
          type: integer
        name:
          type: string
        username:
          type: string
          nullable: true
        department:
          type: string
          nullable: true
        team_code:
          type: string
          nullable: true
        roles:
          type: array
          items:
            type: string
        frontend_actions:
          type: array
          items:
            type: string
        managed_departments:
          type: array
          items:
            type: string
        managed_teams:
          type: array
          items:
            type: string
    TaskDraftPayload:
      type: object
      description: "Draft payload mirrors `POST /v1/tasks`. Source: V1_INFORMATION_ARCHITECTURE §3.5.9."
      additionalProperties: true
    TaskDraft:
      type: object
      description: "Source: V1_INFORMATION_ARCHITECTURE §3.5.9."
      properties:
        draft_id:
          type: integer
        owner_user_id:
          type: integer
        task_type:
          type: string
        payload:
          $ref: '#/components/schemas/TaskDraftPayload'
        expires_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    BatchItem:
      type: object
      description: Batch Excel preview row. When the uploaded workbook contains pictures anchored to the row, the server uploads them through the task reference-file flow and returns the resulting `reference_file_refs`; frontend should preview and submit those refs unchanged in `POST /v1/tasks.batch_items[]`.
      properties:
        product_name:
          type: string
        product_short_name:
          type: string
        category_code:
          type: string
        product_i_id:
          type: string
          description: Per-row ERP product i_id. If present, parse-excel validates exact membership through `GET /v1/erp/iids` semantics before returning a clean preview.
        material_mode:
          type: string
          enum: [preset, other]
        design_requirement:
          type: string
        new_sku:
          type: string
        purchase_sku:
          type: string
        cost_price_mode:
          type: string
          enum: [manual, template]
        quantity:
          type: integer
          format: int64
          nullable: true
        base_sale_price:
          type: number
          format: double
          nullable: true
        variant_json:
          type: object
          nullable: true
          additionalProperties: true
        reference_file_refs:
          type: array
          description: Reference file refs uploaded by the server from images pasted into this Excel row. Submit unchanged in the matching `batch_items[]` row during task creation.
          items:
            $ref: '#/components/schemas/ReferenceFileRef'
    ParseViolation:
      type: object
      required: [row, code, message]
      properties:
        row:
          type: integer
        column:
          type: string
        code:
          type: string
        message:
          type: string
    BatchCreateParseResult:
      type: object
      description: "Source: V1_INFORMATION_ARCHITECTURE §3.5.4."
      properties:
        task_type:
          type: string
          enum: [new_product_development, purchase_task]
        preview:
          type: array
          items:
            $ref: '#/components/schemas/BatchItem'
        violations:
          type: array
          items:
            $ref: '#/components/schemas/ParseViolation'
    AssetLifecycleState:
      type: string
      description: "Source: V1_ASSET_OWNERSHIP §7."
      enum: [active, closed_retained, archived, auto_cleaned, deleted]
    Asset:
      allOf:
        - $ref: '#/components/schemas/DesignAsset'
        - type: object
          properties:
            source_module_key:
              type: string
            lifecycle_state:
              $ref: '#/components/schemas/AssetLifecycleState'
    ERPProductSnapshot:
      type: object
      description: "Source: V1_CUSTOMIZATION_WORKFLOW §3.1.2."
      properties:
        code:
          type: string
        product_name:
          type: string
        snapshot:
          type: object
          additionalProperties: true
    DesignSourceEntry:
      type: object
      description: "Source: V1_CUSTOMIZATION_WORKFLOW §3.2.2."
      properties:
        id:
          type: integer
        file_name:
          type: string
        owner_team_code:
          type: string
        preview_url:
          type: string
          nullable: true
        version_no:
          type: integer
          nullable: true
        origin_task_id:
          type: integer
          nullable: true
        created_at:
          type: string
          format: date-time
    SearchResultGroup:
      type: object
      description: |
        Source: V1_INFORMATION_ARCHITECTURE §4.2.
        Decision (R1.7-D): all four arrays are item-schema fixed; `users[]` is always
        `[]` for callers other than super_admin / hr_admin regardless of match count
        (IA §4.3 row-level policy; R1.7-D Q1=A1 + Q2=U1).
      properties:
        tasks:
          type: array
          items:
            type: object
            required: [id, task_no]
            properties:
              id:
                type: integer
                format: int64
              task_no:
                type: string
              title:
                type: string
                nullable: true
              task_status:
                type: string
                nullable: true
                description: Derived task status (see V1_MODULE_ARCHITECTURE §5).
              priority:
                type: string
                nullable: true
                description: One of `low` / `normal` / `high` / `critical`.
              task_type:
                type: string
                nullable: true
                description: Task business type, such as `new_product_development`.
              sku_code:
                type: string
                nullable: true
              primary_sku_code:
                type: string
                nullable: true
              i_id:
                type: string
                nullable: true
                description: JST product style/family i_id resolved from task detail or ERP filing snapshot.
              owner_department:
                type: string
                nullable: true
              owner_team:
                type: string
                nullable: true
              owner_org_team:
                type: string
                nullable: true
              creator_id:
                type: integer
                format: int64
                nullable: true
              creator_name:
                type: string
                nullable: true
              designer_id:
                type: integer
                format: int64
                nullable: true
              designer_name:
                type: string
                nullable: true
              created_at:
                type: string
                format: date-time
                nullable: true
              deadline_at:
                type: string
                format: date-time
                nullable: true
              highlight:
                type: string
                nullable: true
                description: Matched snippet (optional in v1 MySQL LIKE mode).
        assets:
          type: array
          items:
            type: object
            required: [asset_id, file_name]
            properties:
              asset_id:
                type: integer
                format: int64
              file_name:
                type: string
              source_module_key:
                type: string
                nullable: true
                description: One of `basic_info` / `design` / `audit` / `customization` / `warehouse`.
              task_id:
                type: integer
                format: int64
                nullable: true
        products:
          type: array
          items:
            type: object
            required: [erp_code, product_name]
            properties:
              erp_code:
                type: string
              product_name:
                type: string
              i_id:
                type: string
                nullable: true
              category:
                type: string
                nullable: true
        users:
          type: array
          description: |
            Always `[]` unless caller role ∈ {super_admin, hr_admin} (IA §4.3; R1.7-D U1).
          items:
            type: object
            required: [user_id, username]
            properties:
              user_id:
                type: integer
                format: int64
              username:
                type: string
              department_name:
                type: string
                nullable: true
    NotificationType:
      type: string
      description: "Source: V1_INFORMATION_ARCHITECTURE §8.1 (`task_mentioned` excluded in v1). Current values: `task_assigned_to_me`, `task_rejected`, `task_pending_audit`, `claim_conflict`, `pool_reassigned`, and `task_cancelled`."
      enum: [task_assigned_to_me, task_rejected, task_pending_audit, claim_conflict, pool_reassigned, task_cancelled]
    Notification:
      type: object
      description: |
        Source: V1_INFORMATION_ARCHITECTURE §8.

        `payload` is a notification-type-specific JSON object. Stable routing fields are ids/codes;
        display-name fields may be included as denormalized snapshots for frontend rendering, but
        frontend must tolerate them being absent and fall back to id/code based labels.

        Current payload contract:

        - `task_assigned_to_me`: `task_id`; optional `task_no`, `task_type`, `module_key`,
          `action`, `assigned_by`, `assigned_by_name`, `designer_id`,
          `previous_designer_id`, `previous_handler_id`, `reason`, `remark`,
          `batch_request_id`.
        - `task_rejected`: `task_id`, `reject_reason`; optional `task_no`, `module_key`,
          `rejected_by`, `rejected_by_name`.
        - `task_pending_audit`: `task_id`, `module_key`, `pool_team_code`; optional
          `task_no`, `task_type`, `submitted_by`, `submitted_by_name`.
        - `claim_conflict`: `task_id`, `module_key`; optional `task_no`, `winner_user_id`,
          `winner_user_name`.
        - `pool_reassigned`: `task_id`, `module_key`; optional `task_no`, `team_code`,
          `team_name`, `reassigned_by`, `reassigned_by_name`.
        - `task_cancelled`: `task_id`, `cancel_reason`, `cancelled_by`; optional `task_no`,
          `cancelled_by_name`, `module_key`.
      properties:
        id:
          type: integer
        notification_type:
          $ref: '#/components/schemas/NotificationType'
        payload:
          type: object
          description: Notification-type-specific payload. See `Notification` schema description for the per-type field list.
          additionalProperties: true
        is_read:
          type: boolean
        read_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
    OrgMoveRequestState:
      type: string
      description: "Source: V1_INFORMATION_ARCHITECTURE §5.2."
      enum: [pending_super_admin_confirm, approved, rejected]
    OrgMoveRequest:
      type: object
      description: "Source: V1_INFORMATION_ARCHITECTURE §5.2."
      properties:
        id:
          type: integer
        user_id:
          type: integer
        source_department_id:
          type: integer
        target_department_id:
          type: integer
          nullable: true
        state:
          $ref: '#/components/schemas/OrgMoveRequestState'
        reason:
          type: string
          nullable: true
        reject_reason:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    MyOrgProfile:
      type: object
      description: "Source: V1_INFORMATION_ARCHITECTURE §7.2 (我的组织 · 只读). R4-SA-B."
      properties:
        department:
          type: string
          nullable: true
          description: Canonical department name from `/v1/org/options`.
        team:
          type: string
          nullable: true
          description: Primary team (single value in v1; multi-team membership deferred to R5+).
        managed_departments:
          type: array
          items: { type: string }
          description: Departments this user manages (populated for DeptAdmin / DesignDirector / SuperAdmin).
        managed_teams:
          type: array
          items: { type: string }
          description: Teams this user manages (populated for TeamLead / DeptAdmin managed scope).
        roles:
          type: array
          items:
            $ref: '#/components/schemas/V7Role'
    UpdateMyProfileRequest:
      type: object
      description: "Source: V1_INFORMATION_ARCHITECTURE §7.2 (账户信息 · 编辑). R4-SA-B. All fields optional; server applies PATCH semantics."
      properties:
        display_name:
          type: string
        mobile:
          type: string
        email:
          type: string
          nullable: true
        avatar:
          type: string
          nullable: true
          description: |
            R1.7-B placeholder. The server accepts but currently ignores this field (no `users.avatar_url` column in v1 DDL). Full persistence is scheduled for R5+.
    ChangePasswordRequest:
      type: object
      description: "Source: V1_INFORMATION_ARCHITECTURE §7.2 (安全 · 改密). R4-SA-B."
      required: [old_password, new_password, confirm]
      properties:
        old_password:
          type: string
          minLength: 1
        new_password:
          type: string
          minLength: 8
        confirm:
          type: string
          minLength: 1
          description: Must equal `new_password`; server enforces equality.
    CreateOrgMoveRequestPayload:
      type: object
      description: "Source: V1_INFORMATION_ARCHITECTURE §5.2 (DeptAdmin 发起跨部门移出). R4-SA-B."
      required: [user_id, target_department_id, reason]
      properties:
        user_id:
          type: integer
          description: The user being moved. Must currently belong to the source department identified in the path.
        target_department_id:
          type: integer
          nullable: true
          description: Destination department; nullable means unassigned-pool.
        reason:
          type: string
          minLength: 1
    RejectReasonRequest:
      type: object
      description: "Source: V1_INFORMATION_ARCHITECTURE §5.2 (SuperAdmin 驳回 org-move-request). R4-SA-B."
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1
    L1Card:
      type: object
      description: |
        Source: V1_INFORMATION_ARCHITECTURE §1 一级菜单「报表」 + V1_MODULE_ARCHITECTURE §12 U 表.
        R1.7-D decision (Q6=C1): v1 直查 `task_module_events` + `tasks`,不建物化表;
        R6+ 视性能再升级到汇总表。
      properties:
        key:
          type: string
          description: Stable card key (e.g. `tasks_in_progress`, `tasks_completed_today`).
        title:
          type: string
        value:
          type: number
        unit:
          type: string
          nullable: true
        delta:
          type: number
          nullable: true
          description: Change vs previous period; null when no baseline available.
    AssetReasonRequest:
      type: object
      description: "Source: V1_ASSET_OWNERSHIP §5.4 (delete) / §7.3 (archive). R4-SA-A."
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1
    AssetVersion:
      type: object
      description: "Single asset version entry. Source: V1_ASSET_OWNERSHIP §4 / §5.2."
      properties:
        version_id:
          type: integer
        version_no:
          type: integer
        storage_key:
          type: string
          nullable: true
          description: Null when lifecycle_state ∈ {auto_cleaned, deleted}.
        file_size:
          type: integer
          format: int64
          nullable: true
        mime_type:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
        created_by:
          $ref: '#/components/schemas/Actor'
    AssetDetail:
      type: object
      description: "Source: service/asset_center.AssetDetail — detail endpoint returns asset + version list."
      properties:
        id:
          type: integer
        task_id:
          type: integer
        asset_no:
          type: string
        scope_sku_code:
          type: string
        asset_type:
          type: string
        current_version_id:
          type: integer
          nullable: true
        source_module_key:
          type: string
        lifecycle_state:
          $ref: '#/components/schemas/AssetLifecycleState'
        archive_status:
          type: string
        upload_status:
          $ref: '#/components/schemas/DesignAssetUploadStatus'
        storage_key:
          type: string
          nullable: true
        file_name:
          type: string
        original_filename:
          type: string
        file_size:
          type: integer
          format: int64
          nullable: true
        mime_type:
          type: string
        task_no:
          type: string
        task_status:
          type: string
        owner_team_code:
          type: string
        created_by:
          type: integer
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        versions:
          type: array
          items:
            $ref: '#/components/schemas/AssetVersion'
        archived_at:
          type: string
          format: date-time
          nullable: true
        archived_by:
          allOf: [{ $ref: '#/components/schemas/Actor' }]
          nullable: true
        cleaned_at:
          type: string
          format: date-time
          nullable: true
        deleted_at:
          type: string
          format: date-time
          nullable: true
paths:
  /v1/auth/register:
    post:
      summary: Register workflow user
      description: |
        Frontend-ready registration endpoint for the current minimal auth mainline. Registration now
        accepts explicit profile fields (`name`, `department`, optional `team`, `mobile`, optional
        `email`, `account`, `password`, optional admin key). Department must match the backend org
        master exposed by `/v1/org/options`. If `team` is provided it must belong to the selected department. `mobile`
        is validated and kept unique. If the provided admin key matches the configured department rule,
        the new user is granted `DepartmentAdmin`. The response includes `user.frontend_access`.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [account, name, department, mobile, password]
              properties:
                username: { type: string, description: Compatibility alias of account }
                account: { type: string }
                display_name: { type: string }
                name: { type: string }
                department:
                  $ref: '#/components/schemas/Department'
                team: { type: string }
                group: { type: string, description: Compatibility alias of team }
                mobile: { type: string }
                phone: { type: string, description: Compatibility alias of mobile }
                email: { type: string }
                password: { type: string }
                admin_key:
                  type: string
                  description: |
                    Department admin registration key. If a valid department admin key is provided,
                    the user is registered as a department admin (role: dept_admin) for the specified
                    department. If omitted or invalid, the user is registered as a regular member.
                    Super admin accounts are NOT created through self-registration; they are managed
                    via auth_identity.json configuration (super_admins section).
                secret_key: { type: string, description: Compatibility alias of admin_key }
      responses:
        '201':
          description: User registered and logged in
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AuthResult'
        '400':
          description: Invalid registration payload, invalid department/team/mobile/email, or duplicate account/mobile
  /v1/auth/register-options:
    get:
      summary: Get registration department/team options
      description: |
        Returns the current enabled backend org-master department list plus enabled teams per department
        for frontend register-form initialization. Departments without active teams return an empty `teams` array.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      responses:
        '200':
          description: Registration options
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/RegistrationOptions'
  /v1/org/options:
    get:
      summary: Get organization options
      description: |
        Returns the backend org master source used by user-management, task org validation, owner-team
        compatibility bridging, and frontend org-assignment flows.
        The canonical response shape is top-level `departments[]`, where each department carries a nested `teams[]`
        array. That nested department tree is authoritative for `PATCH /v1/users/{id}` department/team updates and for
        org values accepted by task create.
        `teams_by_department` remains a deprecated compatibility mirror in v1.8 only; responses that still include it
        emit `Deprecation: version="v1.8"`.
        User responses return both `team` and compatibility alias `group` with the same value.
        Read access is available to company-level managers, department managers, and legacy org/role compatibility admins.
        The v1.0 official baseline exposed here is exactly:
        `人事部` -> `人事管理组`;
        `运营部` -> `淘系一组`, `淘系二组`, `天猫一组`, `天猫二组`, `拼多多南京组`, `拼多多池州组`;
        `设计研发部` -> `默认组`;
        `定制美工部` -> `默认组`;
        `审核部` -> `普通审核组`, `定制审核组`;
        `云仓部` -> `默认组`;
        plus the `未分配` / `未分配池` system bucket.
        Legacy operations groups 1-7 and legacy compatibility departments are intentionally filtered out by the `enabled=1` projection.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Admin, SuperAdmin, HRAdmin, DepartmentAdmin, OrgAdmin, RoleAdmin]
      responses:
        '200':
          description: Organization options
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/OrgOptions'
        '403':
          description: Permission denied
  /v1/org/departments:
    post:
      summary: Create organization department
      description: Creates one enabled department in backend org master. Newly created departments appear in `/v1/org/options` and become valid user/task org values immediately after creation.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [HRAdmin, SuperAdmin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOrgDepartmentRequest'
      responses:
        '201':
          description: Department created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/OrgDepartment'
        '400':
          description: Invalid request payload or department already exists
        '403':
          description: Permission denied
  /v1/org/departments/{id}:
    put:
      summary: Update organization department
      description: Updates one backend org department. Current runtime semantic is enable/disable only; disabling is rejected while users are still assigned or enabled child teams still exist.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [HRAdmin, SuperAdmin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateOrgDepartmentRequest'
      responses:
        '200':
          description: Department updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/OrgDepartment'
        '400':
          description: Invalid request payload or department cannot be disabled yet
        '403':
          description: Permission denied
        '404':
          description: Department not found
  /v1/org/teams:
    post:
      summary: Create organization team
      description: Creates one enabled team in backend org master under the specified department. Newly created teams appear in `/v1/org/options` and become valid user/task org values immediately after creation.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [HRAdmin, SuperAdmin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOrgTeamRequest'
      responses:
        '201':
          description: Team created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/OrgTeam'
        '400':
          description: Invalid request payload, team already exists, or department is invalid
        '403':
          description: Permission denied
  /v1/org/teams/{id}:
    put:
      summary: Update organization team
      description: Updates one backend org team. Current runtime semantic is enable/disable only; disabling is rejected while users are still assigned to that team.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [HRAdmin, SuperAdmin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateOrgTeamRequest'
      responses:
        '200':
          description: Team updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/OrgTeam'
        '400':
          description: Invalid request payload or team cannot be disabled yet
        '403':
          description: Permission denied
        '404':
          description: Team not found
  /v1/auth/login:
    post:
      summary: Login workflow user
      description: |
        Login endpoint for local session-token auth. Returns a bearer token backed by persisted
        `user_sessions` rows. This contract does not introduce SSO, org sync, or external identity
        providers. The response includes `session_id` plus the current frontend permission contract
        under `user.frontend_access`.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username, password]
              properties:
                username:
                  type: string
                  description: Workflow user login name. Runtime handler binds this as the primary field.
                account:
                  type: string
                  description: Compatibility alias of `username`.
                password:
                  type: string
      responses:
        '200':
          description: Logged in
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AuthResult'
        '401':
          description: Invalid account or password
  /v1/auth/me:
    get:
      summary: Get current authenticated user
      description: |
        Returns the current bearer-session user. Debug headers are not accepted as a substitute for
        login on this route. `data.frontend_access` is the explicit frontend-ready contract for page,
        menu, action, scope, role, department, and team gating.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        session_required: true
      responses:
        '200':
          description: Current user
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WorkflowUser'
        '401':
          description: Authentication required
  /v1/auth/password:
    put:
      summary: Change current user password
      description: |
        Session-backed current-user password change endpoint. Requires `old_password` verification and
        validates `new_password` with the same minimum password rules used during registration.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [old_password, new_password]
              properties:
                old_password: { type: string }
                new_password: { type: string }
      responses:
        '200':
          description: Password changed
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      message: { type: string }
        '400':
          description: Invalid new password
        '401':
          description: Authentication required or old password incorrect
  /v1/roles:
    get:
      summary: List role catalog
      description: Minimal role/permission catalog for the current auth/org scope. Read access is available to management roles, including DepartmentAdmin, plus legacy org/role compatibility admins.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Admin, SuperAdmin, HRAdmin, DepartmentAdmin, OrgAdmin, RoleAdmin]
      responses:
        '200':
          description: Available workflow roles
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/RoleCatalogEntry'
        '403':
          description: Permission denied
  /v1/access-rules:
    get:
      summary: List protected route access rules
      description: Super-admin or HR inspection endpoint for the current route-to-role authorization contract, including whether a route is session-only or still debug-compatible placeholder scope.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
      responses:
        '200':
          description: Protected route access rules
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/RouteAccessRule'
        '403':
          description: Permission denied
  /v1/users:
    get:
      summary: List workflow users
      description: |
        Management-scoped user-management read endpoint. Returns user department, team, role, and frontend access state for frontend integration, with server-side pagination and filtering.
        `DepartmentAdmin` reads are forced to own department.
        `TeamLead` reads are forced to own team inside own department.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-B
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Admin, SuperAdmin, HRAdmin, DepartmentAdmin, TeamLead, OrgAdmin, RoleAdmin]
      parameters:
        - in: query
          name: keyword
          schema: { type: string }
        - in: query
          name: status
          schema:
            $ref: '#/components/schemas/UserStatus'
        - in: query
          name: role
          schema:
            $ref: '#/components/schemas/V7Role'
        - in: query
          name: department
          schema:
            type: string
          description: Must match `/v1/org/options`.
        - in: query
          name: team
          schema:
            type: string
          description: Must match the selected department in `/v1/org/options`.
        - in: query
          name: page
          schema: { type: integer }
        - in: query
          name: page_size
          schema: { type: integer }
      responses:
        '200':
          description: Users
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/WorkflowUser'
                  pagination:
                    $ref: '#/components/schemas/PaginationMeta'
    post:
      summary: Create workflow user
      description: |
        Managed user creation endpoint. Validates org fields against `/v1/org/options`, validates roles against the workflow role catalog,
        sets the initial password hash, and returns the created user with `frontend_access`. If `status` is omitted the user is created as `active`.
        `DepartmentAdmin` can create users only inside own department and only with department-compatible business roles.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-B
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [HRAdmin, SuperAdmin, DepartmentAdmin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateManagedUserRequest'
      responses:
        '201':
          description: User created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WorkflowUser'
  /v1/users/designers:
    get:
      summary: List designers
      description: |
        Returns users with Designer role for task creation/assignment dropdowns. Prefer this over filtering the full user list.
        Round C (v1.5) widens the route guard so Ops task creators plus HR/SuperAdmin can look up designers cross-department.
        `DepartmentAdmin` is intentionally NOT in the guard list: cross-department designer lookup is an Ops-style capability only. DepartmentAdmin remains scoped by `authorizeUserListFilter` on the canonical `/v1/users` path.
        As of v1.6 (Round D), this endpoint uses a dedicated assignment-candidate-pool service path (`IdentityService.ListAssignableDesigners`) that bypasses the standard user-list authorization filter. By default it remains restricted to `role=Designer` + `status=active` and does NOT accept department/team/keyword or pagination parameters. Round N adds `workflow_lane` to select the candidate-pool lane while preserving no-parameter backward compatibility. The route guard listed in `x-rbac-placeholder.required_roles` is the sole access control for this method; the service path performs no additional department/team scoping. Response envelope is `{data, pagination}` where `pagination.page_size` and `pagination.total` both reflect the full returned list length (no server-side pagination).
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, CustomizationOperator, Admin, HRAdmin, SuperAdmin]
      parameters:
        - in: query
          name: workflow_lane
          required: false
          description: "Selects the assignment candidate pool lane. `normal` (default, back-compat) returns active users with role=Designer. `customization` returns active users with role=CustomizationOperator. `all` returns the union."
          schema:
            type: string
            enum: [normal, customization, all]
            default: normal
      responses:
        '200':
          description: Designer list (id, username, display_name)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id: { type: integer }
                        username: { type: string }
                        display_name: { type: string }
                  pagination:
                    type: object
                    properties:
                      page: { type: integer, example: 1 }
                      page_size: { type: integer }
                      total: { type: integer }
  /v1/users/{id}:
    get:
      summary: Get workflow user
      description: |
        Management-scoped user detail read endpoint.
        `DepartmentAdmin` can read only users in own department.
        `TeamLead` can read only users in own team.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Admin, SuperAdmin, HRAdmin, DepartmentAdmin, TeamLead, OrgAdmin, RoleAdmin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: User detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WorkflowUser'
        '404':
          description: User not found
    patch:
      summary: Update workflow user
      description: |
        Partial update for user profile and org affiliation.
        Org-field contract:
        - `department` + `team` are the canonical write fields.
        - `group` is a compatibility alias of `team` (when both are provided they must be equal).
        - To remove a user from a formal group, use the unassigned-pool semantic:
          - set `department` to the unassigned department from `/v1/org/options`
          - set `team` (or `group`) to its unassigned pool team.
        - Compatibility alias: `team/group = "ungrouped"` is normalized by backend into the configured unassigned pool.
        `DepartmentAdmin` can move users across teams inside own department and assign unassigned users into own department, but cannot move users across departments or mutate managed scope.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-B
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [HRAdmin, SuperAdmin, DepartmentAdmin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Partial user update. Field-level authorization is enforced per V1_INFORMATION_ARCHITECTURE §5.4.
              properties:
                display_name: { type: string }
                status:
                  $ref: '#/components/schemas/UserStatus'
                employment_type:
                  $ref: '#/components/schemas/EmploymentType'
                department:
                  $ref: '#/components/schemas/Department'
                team:
                  type: string
                group:
                  type: string
                  description: Compatibility alias of team.
                email:
                  type: string
                  nullable: true
                mobile:
                  type: string
                avatar:
                  type: string
                  nullable: true
                  description: |
                    R1.7-B placeholder. Server currently ignores this field (no `users.avatar_url` column in v1 DDL). Full persistence is scheduled for R5+.
                roles:
                  type: array
                  description: |
                    Role assignment per V1_INFORMATION_ARCHITECTURE §5.4. DeptAdmin cannot grant SuperAdmin or DeptAdmin; HRAdmin cannot grant SuperAdmin.
                  items:
                    $ref: '#/components/schemas/V7Role'
                managed_departments:
                  type: array
                  items:
                    type: string
                managed_teams:
                  type: array
                  items:
                    type: string
                team_codes:
                  type: array
                  description: |
                    R1.7-B placeholder. v1 persists only the single `team` field plus `managed_teams[]`; multi-team membership deferred to R5+. The server accepts but currently ignores this array (no-op). IA §5.4 reference maintained for forward-compat.
                  items:
                    type: string
                primary_team_code:
                  type: string
                  nullable: true
                  description: |
                    R1.7-B placeholder. v1 semantically equals the `team` field; server accepts but currently ignores this field (writes must go through `team`). Full persistence of the multi-team model is scheduled for R5+.
      responses:
        '200':
          description: User updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WorkflowUser'
        '403':
          description: Forbidden (field-level authorization per §5.4 denied, e.g., DeptAdmin granting DeptAdmin or SuperAdmin; TeamLead attempting role change; cross-department mutation without org-move-request)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      summary: Delete workflow user
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-B
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '204': { description: User deleted }
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/users/{id}/password:
    put:
      summary: Reset workflow user password
      description: |
        Managed password reset endpoint. Replaces the target user's local password hash and returns the user record.
        `DepartmentAdmin` can reset passwords only for users in own department.
        Existing session tokens are not revoked by this minimal reset operation.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [HRAdmin, SuperAdmin, DepartmentAdmin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResetUserPasswordRequest'
      responses:
        '200':
          description: Password reset
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WorkflowUser'
  /v1/users/{id}/roles:
    post:
      summary: Add workflow user roles
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [HRAdmin, SuperAdmin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                roles:
                  type: array
                  items:
                    $ref: '#/components/schemas/V7Role'
      responses:
        '200':
          description: User roles added
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WorkflowUser'
    put:
      summary: Replace workflow user roles
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [HRAdmin, SuperAdmin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                roles:
                  type: array
                  items:
                    $ref: '#/components/schemas/V7Role'
      responses:
        '200':
          description: User roles replaced
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WorkflowUser'
  /v1/users/{id}/roles/{role}:
    delete:
      summary: Remove one workflow user role
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [HRAdmin, SuperAdmin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: role
          required: true
          schema:
            $ref: '#/components/schemas/V7Role'
      responses:
        '200':
          description: User role removed
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WorkflowUser'
  /v1/permission-logs:
    get:
      summary: List permission access logs
      description: |
        Combined route-access and identity/role-change audit log. Besides route-level authorization
        decisions, this endpoint also records register, login, login failure, password change, role
        assignment, and role removal actions. Read access is available to super admins and HR users.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
      parameters:
        - in: query
          name: actor_id
          schema: { type: integer }
        - in: query
          name: actor_username
          schema: { type: string }
        - in: query
          name: action_type
          schema: { type: string }
        - in: query
          name: target_user_id
          schema: { type: integer }
        - in: query
          name: target_username
          schema: { type: string }
        - in: query
          name: granted
          schema: { type: boolean }
        - in: query
          name: method
          schema: { type: string }
        - in: query
          name: route_path
          schema: { type: string }
        - in: query
          name: page
          schema: { type: integer }
        - in: query
          name: page_size
          schema: { type: integer }
      responses:
        '200':
          description: Permission logs
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/PermissionLog'
                  pagination:
                    $ref: '#/components/schemas/PaginationMeta'
  /v1/operation-logs:
    get:
      summary: List aggregated operation logs
      description: |
        Aggregated operation-log query endpoint for frontend integration. Combines recent task
        events, export-job events, and integration call logs into one timeline-style read model.
        Product policy is `HRAdmin` and `SuperAdmin`; legacy `Admin` remains accepted as compatibility-only access.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Admin, SuperAdmin, HRAdmin]
      parameters:
        - in: query
          name: source
          schema:
            $ref: '#/components/schemas/OperationLogSource'
        - in: query
          name: event_type
          schema: { type: string }
        - in: query
          name: page
          schema: { type: integer }
        - in: query
          name: page_size
          schema: { type: integer }
      responses:
        '200':
          description: Aggregated operation logs
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/OperationLogEntry'
                  pagination:
                    $ref: '#/components/schemas/PaginationMeta'
  /v1/audit-logs:
    get:
      summary: List audit records (cross-task)
      description: |
        Cross-task audit record list for audit log view. Filters by task_no, auditor, action, start/end date.
        Returns audit_records enriched with task_no and auditor_name.
        Read access is limited to audit roles and management roles.
        `DepartmentAdmin` results are filtered to own department.
        `TeamLead` results are filtered to own team inside own department.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Admin, SuperAdmin, HRAdmin, DepartmentAdmin, TeamLead, OrgAdmin, RoleAdmin, Audit_A, Audit_B, CustomizationReviewer]
      parameters:
        - in: query
          name: taskNo
          schema: { type: string }
          description: Contains match on task_no
        - in: query
          name: auditor
          schema: { type: string }
          description: Contains match on auditor display_name
        - in: query
          name: action
          schema: { type: string, enum: [claim, approve, reject, transfer, handover, takeover] }
        - in: query
          name: start
          schema: { type: string, format: date }
          description: YYYY-MM-DD, records with created_at >= start 00:00:00
        - in: query
          name: end
          schema: { type: string, format: date }
          description: YYYY-MM-DD, records with created_at <= end 23:59:59
        - in: query
          name: page
          schema: { type: integer }
        - in: query
          name: page_size
          schema: { type: integer }
      responses:
        '200':
          description: Audit records list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id: { type: integer }
                        task_id: { type: integer }
                        task_no: { type: string }
                        stage: { type: string }
                        action: { type: string }
                        auditor_id: { type: integer }
                        auditor_name: { type: string }
                        comment: { type: string }
                        created_at: { type: string, format: date-time }
  /v1/server-logs:
    get:
      summary: List server logs
      description: |
        Admin-only server log query with filtering (level, keyword, since, until) and masking of sensitive data.
        5xx HTTP responses are automatically recorded. Use POST /v1/server-logs/clean to purge old entries.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      parameters:
        - in: query
          name: level
          schema: { type: string, enum: [info, warn, error] }
        - in: query
          name: keyword
          schema: { type: string }
        - in: query
          name: since
          schema: { type: string, format: date-time }
        - in: query
          name: until
          schema: { type: string, format: date-time }
        - in: query
          name: page
          schema: { type: integer }
        - in: query
          name: page_size
          schema: { type: integer }
      responses:
        '200':
          description: Server logs (sensitive fields masked)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ServerLog'
                  pagination:
                    $ref: '#/components/schemas/PaginationMeta'
  /v1/server-logs/clean:
    post:
      summary: Clean old server logs
      description: Admin-only. Deletes server logs older than the specified hours. Reason is required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                older_than_hours: { type: integer, default: 24 }
                reason: { type: string }
      responses:
        '200':
          description: Number of deleted rows
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted: { type: integer }
  /v1/erp/products:
    get:
      summary: Search ERP Bridge products
      description: |
        Frontend-ready MAIN facade over Bridge(8081). **Primary chain**: Bridge `remote`/`hybrid` calls Jushuitan OpenWeb POST `ERP_REMOTE_SKU_QUERY_PATH` (live ERP). Local `products` is **not** the search source of truth; hybrid may fall back to local `products` only on transient upstream failure (5xx/timeout), never on OpenWeb business errors or auth misconfiguration. Server logs: `erp_bridge_product_search` (`remote_ok` vs `fallback_local_products`).
        **8080 `products` table**: sync replica / mapping cache / business read-model (ERP_SYNC_SOURCE_MODE=jst via OpenWeb provider), not the picker primary index.
        Current search boundary:
        - keyword / q: supported; implemented as the current bridge's contains-match style lookup rather than semantic/global search
        - sku_code: supported as an exact filter after MAIN normalization
        - category_id / category_name: supported as exact filters after MAIN category validation and local refinement
        - browse: supported
        - sorting: not supported
        - search suggestion / association: not supported
        - global search: not supported
        Contract guarantees for result-page integration:
        - product_id is the stable lookup key emitted by this list and accepted by /v1/erp/products/{id}
        - sku_code, category_name, category_code, product_short_name, image_url may be empty strings when upstream data is absent
        - empty image_url means frontend should render its own placeholder image
        - when category_id / category_name is invalid or does not exist, MAIN returns an empty list instead of falling back to browse
        - when a result is chosen, frontend should pass the returned object into task product_selection.erp_product; backend will cache/bind it to a local products.id
      tags: [ERPBridge, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
      parameters:
        - in: query
          name: q
          description: Keyword search string. This is the primary original-product selection entry and should be preferred over bridge category-only browsing.
          schema: { type: string }
        - in: query
          name: keyword
          description: Additive compatibility alias for `q`. When `q` is empty, backend normalizes this into the effective keyword query.
          schema: { type: string }
        - in: query
          name: sku_code
          description: Exact sku-code filter. MAIN may reuse a code-like upstream id as sku_code when bridge does not emit a dedicated field.
          schema: { type: string }
        - in: query
          name: category_id
          description: Exact category filter using the normalized category id from /v1/erp/categories.
          schema: { type: string }
        - in: query
          name: category_name
          description: Exact category-name filter. MAIN validates the value against /v1/erp/categories; unknown values return an empty list.
          schema: { type: string }
        - in: query
          name: category
          description: Compatibility alias for `category_name`.
          schema: { type: string }
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: page_size
          schema: { type: integer, default: 20, maximum: 100 }
      responses:
        '200':
          description: ERP product list normalized by MAIN for stable frontend result-page integration
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ERPBridgeProduct'
                  pagination:
                    $ref: '#/components/schemas/PaginationMeta'
                  normalized_filters:
                    $ref: '#/components/schemas/ERPBridgeProductSearchFilter'
        '404':
          description: ERP Bridge upstream product path not found
        '500':
          description: ERP Bridge unavailable or upstream returned a non-200 response. Error details may now include timeout/retry-hint diagnostics for internal observability.
  /v1/erp/iids:
    get:
      summary: List ERP product i_id options
      description: |
        Returns distinct Jushuitan product style/family `i_id` values from the local ERP sync replica for frontend selection.
        New task creation should use this endpoint to select `i_id`; `category_code` is backend-owned compatibility metadata and should not be a required frontend input.
      tags: [ERPBridge, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
      parameters:
        - in: query
          name: q
          schema: { type: string }
          description: Optional fuzzy search against i_id, category, category_name, product_name, or sku_code.
        - in: query
          name: keyword
          schema: { type: string }
          description: Compatibility alias for `q`.
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: page_size
          schema: { type: integer, default: 50, maximum: 200 }
      responses:
        '200':
          description: Distinct ERP i_id option list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ERPIIDOption'
                  pagination:
                    $ref: '#/components/schemas/PaginationMeta'
                  normalized_filters:
                    $ref: '#/components/schemas/ERPIIDListFilter'
  /v1/erp/products/{id}:
    get:
      summary: Get ERP Bridge product detail
      description: |
        Returns one normalized ERP product detail through the MAIN facade.
        Detail lookup behavior:
        - accepts the product_id emitted by GET /v1/erp/products
        - first tries direct bridge detail lookup
        - when direct bridge detail returns 404, MAIN performs a compatible search-based fallback so list -> detail -> task binding remains stable for result-page integration
        - returns 404 only when MAIN cannot resolve the list-emitted lookup key anymore
      tags: [ERPBridge, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        '200':
          description: ERP Bridge product detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ERPBridgeProduct'
        '404':
          description: ERP Bridge product not found
        '500':
          description: ERP Bridge unavailable or upstream returned a non-200 response
  /v1/erp/categories:
    get:
      summary: List ERP Bridge categories
      description: |
        Normalized categories for **exact** product filters on /v1/erp/products. Source is local configurable mapping layer (categories table; current 31 rows are sample data, not production category center). Business classification primary semantic = style code (i_id); JST category field is ERP raw field. Do NOT use jst_inventory or any large sync table scan.
        For **global** category pickers (rules, filters, dropdowns), prefer **GET /v1/categories** or **GET /v1/categories/search** as the primary API. See docs/TRUTH_SOURCE_ALIGNMENT.md.
      tags: [ERPBridge, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
      responses:
        '200':
          description: ERP Bridge category list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ERPBridgeCategory'
        '500':
          description: ERP Bridge unavailable or upstream returned a non-200 response
  /v1/erp/warehouses:
    get:
      summary: List ERP warehouses (wms_co_id)
      description: Returns Bridge-aligned warehouse dimension catalog for MAIN filtering and warehouse-scoped ERP writes.
      tags: [ERPBridge, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
      responses:
        '200':
          description: ERP warehouse list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ERPWarehouse'
        '500':
          description: ERP Bridge unavailable
  /v1/erp/users:
    get:
      summary: List JST company users (Bridge-side, pre-wiring)
      description: |
        Bridge-side query for JST getcompanyusers. Maps to `/open/webapi/userapi/company/getcompanyusers`.
        Pre-wiring only: does NOT change main auth/permission logic. Admin/ERP role required.
      tags: [ERPBridge, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      parameters:
        - name: current_page
          in: query
          schema: { type: integer, default: 1 }
        - name: page_size
          in: query
          schema: { type: integer, default: 50 }
        - name: page_action
          in: query
          schema: { type: integer }
        - name: enabled
          in: query
          schema: { type: boolean }
        - name: version
          in: query
          schema: { type: integer }
      responses:
        '200':
          description: JST user list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/JSTUserListResponse'
        '500':
          description: Bridge or upstream unavailable
  /v1/admin/jst-users:
    get:
      summary: List JST users (Admin, via Bridge)
      description: Pre-wiring. Query JST company users through Bridge. Admin only.
      tags: [Admin, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      parameters:
        - name: current_page
          in: query
          schema: { type: integer }
        - name: page_size
          in: query
          schema: { type: integer }
      responses:
        '200':
          description: JST user list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/JSTUserListResponse'
  /v1/admin/jst-users/import-preview:
    post:
      summary: Preview JST user import (Admin)
      tags: [Admin, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/JSTUserImportPreviewResult'
  /v1/admin/jst-users/import:
    post:
      summary: Import JST users (Admin)
      tags: [Admin, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/JSTUserImportResult'
  /v1/erp/products/upsert:
    post:
      summary: Upsert ERP Bridge product (Bridge-side write endpoint)
      description: |
        Bridge-side write endpoint that accepts ordinary product-profile upsert payloads from MAIN.
        MAIN calls this internally via `ERP_BRIDGE_BASE_URL` when `filed_at` is set
        on an existing-product task. In the current v0.4 architecture, Bridge uses a
        `localERPBridgeClient` that persists directly to the shared MySQL database
        rather than calling an external ERP API.
        As of 2026-03-17, Bridge also supports a config-driven remote mode:
        `ERP_REMOTE_MODE=local|remote|hybrid`.
        - `local`: keep local DB writeback
        - `remote`: upsert is sent to configured external ERP API
        - `hybrid`: remote first, optional local fallback by env switch
        OpenWeb official mapping used by Bridge remote mode:
        - `POST /v1/erp/products/upsert` -> `/open/webapi/itemapi/itemsku/itemskubatchupload`
        - `POST /v1/erp/products/style/update` -> `/open/webapi/itemapi/itemskuim/itemupload`
        - OpenWeb signature parameters are:
          `app_key`, `access_token`, `timestamp`, `charset`, `version`, `biz`
          (`sign` excluded from signing), sorted by key and signed as:
          `md5(app_secret + key1value1key2value2...)` (lowercase hex)
        Combined-SKU upload endpoint `/open/item/combinesku/upload` is intentionally
        not auto-selected in current Bridge contract because this payload currently has
        no explicit "combined SKU" discriminator; Bridge keeps ordinary-SKU mapping
        deterministic to avoid accidental wrong-endpoint writes.
        Current live production mode is `hybrid`; do not assume every write endpoint has
        remote success without upstream response evidence for that endpoint.
        This route was added in ITERATION_070 to fix a 404 gap that prevented all
        MAIN -> Bridge filing calls from succeeding.
        Requires session authentication (Bearer token forwarded from MAIN).
      tags: [ERPBridge, InternalBridge]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ERPProductUpsertPayload'
      responses:
        '200':
          description: Product upserted successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ERPProductUpsertResult'
        '400':
          description: Invalid payload or missing required fields (sku_id required)
        '401':
          description: Authentication required
        '500':
          description: Internal error during upsert
  /v1/erp/products/style/update:
    post:
      summary: Update ERP item style (Bridge-side write endpoint)
      description: |
        Bridge-side style update endpoint for i_id-centered changes, especially for original-product flows
        where SKU remains unchanged but style-level data (picture/style fields) needs update.
        OpenWeb remote mapping:
        `POST /v1/erp/products/style/update` -> `/open/webapi/itemapi/itemskuim/itemupload`.
      tags: [ERPBridge, InternalBridge]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ERPItemStyleUpdatePayload'
      responses:
        '200':
          description: Item style updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ERPItemStyleUpdateResult'
        '400':
          description: Invalid payload (`i_id` is required)
        '401':
          description: Authentication required
        '500':
          description: Internal error during style update
  /v1/erp/sync-logs:
    get:
      summary: List ERP Bridge sync logs
      description: |
        Lists Bridge-side ERP write sync logs used for mutation observability.
        This route is served by 8081 Bridge and primarily backed by
        integration call logs for:
        - `erp_bridge_product_upsert`
        - `erp_bridge_item_style_update`
        - `erp_bridge_product_shelve_batch`
        - `erp_bridge_product_unshelve_batch`
        - `erp_bridge_inventory_virtual_qty`
      tags: [ERPBridge, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
      parameters:
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: page_size
          schema: { type: integer, default: 20, maximum: 100 }
        - in: query
          name: status
          schema: { type: string }
        - in: query
          name: connector
          schema: { type: string }
        - in: query
          name: operation
          schema: { type: string }
        - in: query
          name: resource_type
          schema: { type: string }
      responses:
        '200':
          description: ERP sync logs
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ERPSyncLog'
                  pagination:
                    $ref: '#/components/schemas/PaginationMeta'
        '500':
          description: Internal error while reading sync logs
  /v1/erp/sync-logs/{id}:
    get:
      summary: Get ERP Bridge sync log detail
      tags: [ERPBridge, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        '200':
          description: ERP sync log detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ERPSyncLog'
        '404':
          description: Sync log not found
  /v1/erp/products/shelve/batch:
    post:
      summary: Shelve products in batch through Bridge
      description: |
        Bridge-owned ERP batch shelve mutation boundary for MAIN integration.
        OpenWeb official mapping in remote/hybrid mode:
        `POST /v1/erp/products/shelve/batch` -> `/open/webapi/wmsapi/openshelve/skubatchshelve`.
        Current live boundary (`v0.4`, ITERATION_077): remote request is sent, but
        upstream currently rejects with business response `code=100, msg=涓婃灦浠撲綅涓嶈兘涓虹┖`
        for tested payloads; in `hybrid` mode Bridge falls back to local write path.
      tags: [ERPBridge, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ERPProductBatchMutationPayload'
      responses:
        '200':
          description: Shelve mutation accepted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ERPProductBatchMutationResult'
        '400':
          description: Invalid batch payload
  /v1/erp/products/unshelve/batch:
    post:
      summary: Unshelve products in batch through Bridge
      description: |
        Bridge-owned ERP batch unshelve mutation boundary for MAIN integration.
        OpenWeb official mapping in remote/hybrid mode:
        `POST /v1/erp/products/unshelve/batch` -> `/open/webapi/wmsapi/openoffshelve/skubatchoffshelve`.
        Current live boundary (`v0.4`, ITERATION_077): remote request is sent, but
        upstream currently rejects with business response `code=100, msg=鎸囧畾绠变笉瀛樺湪`
        for tested payloads; in `hybrid` mode Bridge falls back to local write path.
      tags: [ERPBridge, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ERPProductBatchMutationPayload'
      responses:
        '200':
          description: Unshelve mutation accepted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ERPProductBatchMutationResult'
        '400':
          description: Invalid batch payload
  /v1/erp/inventory/virtual-qty:
    post:
      summary: Update virtual inventory qty through Bridge
      description: |
        Bridge-owned ERP virtual inventory mutation boundary for MAIN integration.
        OpenWeb official mapping in remote/hybrid mode:
        `POST /v1/erp/inventory/virtual-qty` -> `/open/webapi/itemapi/iteminventory/batchupdatewmsvirtualqtys`.
        Current live boundary (`v0.4`, ITERATION_077): remote request is sent, but
        upstream raw body in tested cases returns `code=0, msg=鏈幏鍙栧埌鏈夋晥鐨勪紶鍏ユ暟鎹? data=null`.
        Bridge classifies this as business rejection and in `hybrid` mode falls back
        to local write path.
      tags: [ERPBridge, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ERPVirtualInventoryUpdatePayload'
      responses:
        '200':
          description: Virtual inventory mutation accepted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ERPVirtualInventoryUpdateResult'
        '400':
          description: Invalid payload
  /internal/jst/ping:
    get:
      summary: JST sync service ping (8082)
      description: |
        Resident JST sync service health boundary.
        This endpoint is owned by the long-running 8082 JST sync service and is
        documented here for cross-service contract visibility; it is not served by
        the 8080/8081 `cmd/server` runtime.
      tags: [JSTSync8082, InternalPlaceholder]
      x-runtime-role: jst_sync_8082
      responses:
        '200':
          description: JST sync service reachable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSTSyncPingResponse'
  /jst/sync/inc:
    post:
      summary: Trigger JST incremental sync (8082)
      description: |
        Incremental JST sync control endpoint owned by the resident 8082 service.
        8081 Bridge should consume 8082 outputs/contracts when JST source sync is required.
      tags: [JSTSync8082, InternalPlaceholder]
      x-runtime-role: jst_sync_8082
      responses:
        '200':
          description: Incremental sync accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSTIncrementalSyncResponse'
  /v1/products/search:
    get:
      summary: Search local cached ERP products
      description: |
        Compatibility-only local-cache product search.
        New integrations must use `GET /v1/erp/products`.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/erp/products
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: candidate_for_v1_0_removal
      tags: [Products, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, Audit_A, Audit_B, Warehouse]
      parameters:
        - in: query
          name: keyword
          schema: { type: string }
        - in: query
          name: category
          description: Legacy fuzzy filter against `products.category`. Kept for compatibility.
          schema: { type: string }
        - in: query
          name: category_id
          description: Resolve the selected category to its local first-level `search_entry_code`, then apply active local category-to-ERP mappings.
          schema: { type: integer }
        - in: query
          name: category_code
          description: Resolve the selected category to its local first-level `search_entry_code`, then apply active local category-to-ERP mappings.
          schema: { type: string }
        - in: query
          name: search_entry_code
          description: Explicit first-level search-entry code for local ERP positioning. When omitted but `category_id` or `category_code` is provided, it is derived from the category center.
          schema: { type: string }
        - in: query
          name: mapping_match
          description: Controls which active local mapping rules are consumed. `primary` is the default when mapped search is used; `all` allows non-primary active rules too.
          schema:
            type: string
            enum: [primary, all]
        - in: query
          name: secondary_key
          description: Optional lightweight reserved second-level mapping key. Must be paired with `secondary_value`.
          schema: { type: string }
        - in: query
          name: secondary_value
          description: Optional lightweight reserved second-level mapping value. Must be paired with `secondary_key`.
          schema: { type: string }
        - in: query
          name: tertiary_key
          description: Optional lightweight reserved third-level mapping key. Must be paired with `tertiary_value`.
          schema: { type: string }
        - in: query
          name: tertiary_value
          description: Optional lightweight reserved third-level mapping value. Must be paired with `tertiary_key`.
          schema: { type: string }
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: page_size
          schema: { type: integer, default: 20 }
      responses:
        '200':
          description: Product list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductListResponse'
        '400':
          description: Invalid mapped-search query
  /v1/products/sync/status:
    get:
      summary: Get ERP sync placeholder status
      description: Internal placeholder endpoint for ERP sync visibility. This remains a MAIN-owned sync and runtime control surface and does not imply Bridge ownership. `source_mode=stub` means MAIN reads a local stub JSON source. The response reports the runtime-resolved stub path plus existence state so noop-vs-source-path diagnosis is explicit. Not ready for frontend. Uses current debug-header role enforcement.
      tags: [Products, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [ERP, Admin]
      responses:
        '200':
          description: ERP sync status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERPSyncStatusResponse'
  /v1/products/sync/run:
    post:
      summary: Run ERP sync placeholder manually
      description: Internal placeholder endpoint that synchronously reads the stub ERP source and upserts products. This remains a MAIN-owned sync and runtime control surface and does not imply Bridge ownership. Not ready for frontend. Uses current debug-header role enforcement.
      tags: [Products, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [ERP, Admin]
      responses:
        '200':
          description: ERP sync run summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERPSyncRunResponse'
  /v1/products/{id}:
    get:
      summary: Get product by ID
      description: Compatibility-only local-cache product detail. New integrations must use `GET /v1/erp/products/{id}`.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/erp/products/{id}
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: candidate_for_v1_0_removal
      tags: [Products, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, Audit_A, Audit_B, Warehouse]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Product detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Product'
        '404':
          description: Product not found
  /v1/categories:
    get:
      summary: List categories
      description: Returns paginated category-center skeleton entries. These records now explicitly model first-level ERP search-entry semantics through `search_entry_code` and `is_search_entry`. Coded-style total category codes such as `HBJ` remain valid first-level search-entry categories.
      tags: [CategoryCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: query
          name: keyword
          schema: { type: string }
        - in: query
          name: category_type
          schema:
            $ref: '#/components/schemas/CategoryType'
        - in: query
          name: parent_id
          schema: { type: integer }
        - in: query
          name: level
          schema: { type: integer }
        - in: query
          name: is_active
          schema: { type: boolean }
        - in: query
          name: source
          schema: { type: string }
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: page_size
          schema: { type: integer, default: 20 }
      responses:
        '200':
          description: Paginated categories
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Category'
                  pagination:
                    $ref: '#/components/schemas/PaginationMeta'
    post:
      summary: Create category
      description: Creates one category-center skeleton entry. Top-level categories must keep `search_entry_code == category_code` and `is_search_entry=true`, making the total category code the explicit first-level ERP search entry.
      tags: [CategoryCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCategoryRequest'
      responses:
        '201':
          description: Category created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Category'
        '400':
          description: Invalid category payload
  /v1/categories/search:
    get:
      summary: Search categories
      description: Lightweight selection/search endpoint for category-center skeleton entries. Suitable for task filing and for locating first-level ERP search-entry categories before later second/third-level refinement is introduced.
      tags: [CategoryCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      parameters:
        - in: query
          name: keyword
          schema: { type: string }
        - in: query
          name: category_type
          schema:
            $ref: '#/components/schemas/CategoryType'
        - in: query
          name: is_active
          schema: { type: boolean }
        - in: query
          name: limit
          schema: { type: integer, default: 20 }
      responses:
        '200':
          description: Matching categories
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Category'
  /v1/categories/{id}:
    get:
      summary: Get category by ID
      tags: [CategoryCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Category detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Category'
        '404':
          description: Category not found
    patch:
      summary: Patch category
      description: Updates one category-center skeleton entry while preserving explicit first-level ERP search-entry semantics.
      tags: [CategoryCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchCategoryRequest'
      responses:
        '200':
          description: Category updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Category'
        '400':
          description: Invalid category payload
        '404':
          description: Category not found
  /v1/category-mappings:
    get:
      summary: List category-to-ERP mappings
      description: Returns paginated category-to-ERP positioning skeleton records. These mappings define how one category or first-level search entry should later be used to narrow ERP product lookup, without performing real ERP lookup in this phase.
      tags: [CategoryMappings, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      parameters:
        - in: query
          name: keyword
          schema: { type: string }
        - in: query
          name: category_id
          schema: { type: integer }
        - in: query
          name: category_code
          schema: { type: string }
        - in: query
          name: search_entry_code
          schema: { type: string }
        - in: query
          name: erp_match_type
          schema:
            $ref: '#/components/schemas/CategoryERPMatchType'
        - in: query
          name: is_active
          schema: { type: boolean }
        - in: query
          name: is_primary
          schema: { type: boolean }
        - in: query
          name: source
          schema: { type: string }
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: page_size
          schema: { type: integer, default: 20 }
      responses:
        '200':
          description: Paginated category-to-ERP mappings
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CategoryERPMapping'
                  pagination:
                    $ref: '#/components/schemas/PaginationMeta'
    post:
      summary: Create category-to-ERP mapping
      description: Creates one category-to-ERP positioning skeleton record. `search_entry_code` represents the explicit first-level ERP lookup entry, while secondary and tertiary condition fields are reserved for later refinement.
      tags: [CategoryMappings, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCategoryERPMappingRequest'
      responses:
        '201':
          description: Category-to-ERP mapping created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CategoryERPMapping'
        '400':
          description: Invalid category mapping payload
  /v1/category-mappings/search:
    get:
      summary: Search category-to-ERP mappings
      description: Lightweight selection/search endpoint for category-to-ERP positioning skeleton records. Suitable for admin lookup and for later task/product-search orchestration.
      tags: [CategoryMappings, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      parameters:
        - in: query
          name: keyword
          schema: { type: string }
        - in: query
          name: category_code
          schema: { type: string }
        - in: query
          name: search_entry_code
          schema: { type: string }
        - in: query
          name: erp_match_type
          schema:
            $ref: '#/components/schemas/CategoryERPMatchType'
        - in: query
          name: is_active
          schema: { type: boolean }
        - in: query
          name: limit
          schema: { type: integer, default: 20 }
      responses:
        '200':
          description: Matching category-to-ERP mappings
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CategoryERPMapping'
  /v1/category-mappings/{id}:
    get:
      summary: Get category-to-ERP mapping by ID
      tags: [CategoryMappings, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Category-to-ERP mapping detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CategoryERPMapping'
        '404':
          description: Category-to-ERP mapping not found
    patch:
      summary: Patch category-to-ERP mapping
      description: Updates one category-to-ERP positioning skeleton record while preserving the explicit first-level ERP search-entry contract.
      tags: [CategoryMappings, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchCategoryERPMappingRequest'
      responses:
        '200':
          description: Category-to-ERP mapping updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CategoryERPMapping'
        '400':
          description: Invalid category mapping payload
        '404':
          description: Category-to-ERP mapping not found
  /v1/cost-rules:
    get:
      summary: List cost rules
      description: Returns paginated governed cost-rule entries with effective-window metadata and lineage fields such as `version_chain_summary`, `previous_version`, `next_version`, and `supersession_depth`. This module is a governed rule skeleton, not a full formula engine or approval platform.
      tags: [CostRules, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      parameters:
        - in: query
          name: category_id
          schema: { type: integer }
        - in: query
          name: category_code
          schema: { type: string }
        - in: query
          name: product_family
          schema: { type: string }
        - in: query
          name: rule_type
          schema:
            $ref: '#/components/schemas/CostRuleType'
        - in: query
          name: is_active
          schema: { type: boolean }
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: page_size
          schema: { type: integer, default: 20 }
      responses:
        '200':
          description: Paginated cost rules
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CostRule'
                  pagination:
                    $ref: '#/components/schemas/PaginationMeta'
    post:
      summary: Create cost rule
      description: Creates one governed cost-rule skeleton entry. Use `manual_quote` for categories or formulas that are not machine-calculable yet. `rule_version` / `supersedes_rule_id` / `governance_note` harden governance only; they do not introduce a separate rule-version subsystem or approval flow.
      tags: [CostRules, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCostRuleRequest'
      responses:
        '201':
          description: Cost rule created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CostRule'
        '400':
          description: Invalid cost rule payload
  /v1/cost-rules/{id}:
    get:
      summary: Get cost rule by ID
      description: Returns one governed cost-rule row with lineage read-model fields such as `previous_version`, `next_version`, `version_chain_summary`, and `supersession_depth`. This is configuration governance, not a separate approval workflow or formula platform.
      tags: [CostRules, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Cost rule detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CostRule'
        '404':
          description: Cost rule not found
    patch:
      summary: Patch cost rule
      description: Updates one governed cost-rule skeleton entry while keeping the model extensible for later rule growth, later deactivation, and later successor-version planning. This remains configuration governance, not a formula DSL editor or approval system.
      tags: [CostRules, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchCostRuleRequest'
      responses:
        '200':
          description: Cost rule updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CostRule'
        '400':
          description: Invalid cost rule payload
        '404':
          description: Cost rule not found
  /v1/cost-rules/{id}/history:
    get:
      summary: Get cost rule lineage history
      description: Returns the selected governed rule row together with its lineage chain so consumers can distinguish the current row from predecessor and successor versions. This endpoint is read-only and does not introduce approval flow, formula authoring, or finance integration.
      tags: [CostRules, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Cost rule lineage history
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CostRuleHistoryReadModel'
        '404':
          description: Cost rule not found
  /v1/cost-rules/preview:
    post:
      summary: Preview cost rule estimate
      description: Minimal preview contract for the governed cost-rule skeleton. Fixed price, area-threshold surcharge, minimum billable area, and special-process surcharge can return estimates. `manual_quote`, missing required size or area inputs, and unsupported size-based formulas return `requires_manual_review=true`. The response includes `matched_rule_id`, `matched_rule_version`, `rule_source`, and `governance_status`. `PATCH /v1/tasks/{id}/business-info` reuses the same pricing semantics for persisted task-side prefill snapshots.
      tags: [CostRules, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CostRulePreviewRequest'
      responses:
        '200':
          description: Preview result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CostRulePreviewResponse'
        '400':
          description: Invalid preview request
  /v1/task-create/asset-center/upload-sessions:
    post:
      summary: Create task-create reference upload session
      description: |
        Compatibility-only pre-task reference upload flow retained for rollback-safe migration.
        Obsolete for frontend rollout. New frontend integration must use `POST /v1/tasks/reference-upload`.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/tasks/reference-upload
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTaskReferenceUploadSessionRequest'
      responses:
        '201':
          description: Task-create reference upload session created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CreateTaskReferenceUploadSessionResponseData'
        '400':
          description: Invalid request payload
  /v1/task-create/asset-center/upload-sessions/{session_id}:
    get:
      summary: Get task-create reference upload session
      description: Compatibility-only status read for the old task-create upload-session flow. Obsolete for frontend rollout; new integration must use `POST /v1/tasks/reference-upload`.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/tasks/reference-upload
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops]
      parameters:
        - in: path
          name: session_id
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Upload session
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/UploadSession'
        '404':
          description: Upload session not found
  /v1/task-create/asset-center/upload-sessions/{session_id}/complete:
    post:
      summary: Complete task-create reference upload session
      description: |
        Compatibility-only completion for the old task-create upload-session flow.
        Obsolete for frontend rollout. Finalizes one old pre-task reference upload session and returns both the legacy
        `reference_file_ref` id and the normalized `ref_object` that can be passed into `POST /v1/tasks`.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/tasks/reference-upload
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops]
      parameters:
        - in: path
          name: session_id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompleteTaskReferenceUploadSessionRequest'
      responses:
        '200':
          description: Upload session completed and reference_file_ref returned
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CompleteTaskReferenceUploadSessionResponseData'
        '400':
          description: Invalid request payload
        '404':
          description: Upload session not found
        '409':
          description: Upload session already terminal
  /v1/task-create/asset-center/upload-sessions/{session_id}/abort:
    post:
      summary: Abort task-create reference upload session
      description: Compatibility-only abort for the old task-create upload-session flow. Obsolete for frontend rollout; new integration must use `POST /v1/tasks/reference-upload`.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/tasks/reference-upload
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops]
      parameters:
        - in: path
          name: session_id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelTaskAssetUploadSessionRequest'
      responses:
        '200':
          description: Upload session aborted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/UploadSession'
        '400':
          description: Invalid request payload
        '404':
          description: Upload session not found
        '409':
          description: Completed upload session cannot be aborted
  /v1/tasks/reference-upload:
    post:
      summary: Upload task-create reference file
      description: |
        Canonical frontend entry for task-create reference upload.
        Accepts one `multipart/form-data` file field named `file`, writes it to OSS through backend-controlled direct storage flow when available
        (and uses upload-service proxy only as compatibility fallback),
        records a completed legal reference source, and returns one normalized `reference_file_ref` object.
        The returned object should be appended directly into `POST /v1/tasks.reference_file_refs`.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                created_by:
                  type: integer
                  nullable: true
                remark:
                  type: string
                file_hash:
                  type: string
                file:
                  type: string
                  format: binary
      responses:
        '201':
          description: Reference file uploaded and normalized ref object returned
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ReferenceFileRef'
        '400':
          description: Invalid upload request
        '401':
          description: Authentication required
  /v1/assets:
    get:
      summary: List assets
      description: |
        Canonical resource catalog for asset lookup.
        Supports task-linked lookup plus minimal resource filters for task, SKU scope, archive state, and upload state.
        For PSD preview flows, callers can filter by `source_asset_id` to find backend-owned preview/thumb derivatives.
      tags: [Assets, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, CustomizationOperator, CustomizationReviewer, Ops, Audit_A, Audit_B, Warehouse, Admin]
      parameters:
        - in: query
          name: task_id
          schema:
            type: integer
        - in: query
          name: asset_kind
          schema:
            type: string
            enum: [reference, source, delivery, preview, design_thumb]
        - in: query
          name: source_asset_id
          schema:
            type: integer
        - in: query
          name: scope_sku_code
          schema:
            type: string
        - in: query
          name: archive_status
          schema:
            type: string
        - in: query
          name: upload_status
          schema:
            type: string
      responses:
        '200':
          description: Asset list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DesignAsset'
  /v1/assets/{asset_id}:
    get:
      summary: Get asset
      description: Returns one asset resource by id including full version list. Source V1_ASSET_OWNERSHIP §5.2.
      tags: [Assets, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-A
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, CustomizationOperator, CustomizationReviewer, Ops, Audit_A, Audit_B, Warehouse, Admin]
      parameters:
        - in: path
          name: asset_id
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Asset detail with version list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AssetDetail'
        '404':
          description: Asset not found
    delete:
      summary: Delete asset
      description: Hard-delete (OSS DeleteObject + soft-delete DB row). SuperAdmin only. Source V1_ASSET_OWNERSHIP §5.4.
      tags: [Assets, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-A
      parameters:
        - in: path
          name: asset_id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssetReasonRequest'
      responses:
        '204': { description: Asset deleted }
        '403':
          description: Not SuperAdmin
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Asset not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/assets/{asset_id}/download:
    get:
      summary: Get asset download info
      description: Returns backend-authorized download metadata for one asset resource. Canonical runtime prefers browser-direct byte access.
      tags: [Assets, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-A
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, CustomizationOperator, CustomizationReviewer, Ops, Audit_A, Audit_B, Warehouse, Admin]
      parameters:
        - in: path
          name: asset_id
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Asset download metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AssetDownloadInfo'
        '404':
          description: Asset not found
  /v1/assets/{asset_id}/preview:
    get:
      summary: Get asset preview info
      description: |
        Returns preview metadata for one asset resource.
        For source formats that OSS IMG can process directly (`jpg/png/bmp/gif/webp/tiff/heic/avif`),
        this endpoint returns a signed private-bucket URL with `x-oss-process` preview transform.
        For source formats that are not directly previewable (such as PSD/PSB), this endpoint resolves
        backend-derived `preview/design_thumb` assets linked by `source_asset_id` when available.
        When preview metadata is not currently available for the asset, runtime returns HTTP 409 with
        `error.code=INVALID_STATE_TRANSITION` and message `asset preview is not available`.
      tags: [Assets, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, CustomizationOperator, CustomizationReviewer, Ops, Audit_A, Audit_B, Warehouse, Admin]
      parameters:
        - in: path
          name: asset_id
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Asset preview metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AssetDownloadInfo'
        '404':
          description: Asset not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Preview metadata not available for current asset state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/assets/upload-sessions:
    post:
      summary: Create asset upload session
      description: |
        Canonical frontend entry for asset upload session creation. Backend decides whether
        to use single-part or multipart upload and returns the upload strategy plus
        completion/cancel endpoints. Reference uploads are allowed while a task is still
        `PendingAssign`, so operations users can fill in reference material before a
        designer self-claims or is assigned.
      tags: [Assets, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, CustomizationOperator, CustomizationReviewer, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAssetUploadSessionRequestCanonical'
      responses:
        '201':
          description: Upload session created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CreateTaskAssetUploadSessionResponseData'
        '400':
          description: Invalid request payload
        '403':
          description: Permission denied
  /v1/assets/upload-sessions/{session_id}:
    get:
      summary: Get asset upload session
      description: Returns the current upload-session state by session id.
      tags: [Assets, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, CustomizationOperator, CustomizationReviewer, Ops, Audit_A, Audit_B, Warehouse, Admin]
      parameters:
        - in: path
          name: session_id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Upload session
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/UploadSession'
        '404':
          description: Upload session not found
  /v1/assets/upload-sessions/{session_id}/complete:
    post:
      summary: Complete asset upload session
      description: Completes one asset upload session after the frontend uploads bytes to OSS using the returned plan.
      tags: [Assets, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, CustomizationOperator, CustomizationReviewer, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: session_id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompleteTaskAssetUploadSessionRequest'
      responses:
        '200':
          description: Completed upload session
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CompleteTaskAssetUploadSessionResponseData'
        '400':
          description: Invalid request payload
        '404':
          description: Upload session not found
        '409':
          description: Upload session already terminal or asset type mismatch
  /v1/assets/upload-sessions/{session_id}/cancel:
    post:
      summary: Cancel asset upload session
      description: Cancels one asset upload session and aborts the remote OSS session when needed.
      tags: [Assets, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, CustomizationOperator, CustomizationReviewer, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: session_id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelTaskAssetUploadSessionRequest'
      responses:
        '200':
          description: Cancelled upload session
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/UploadSession'
        '400':
          description: Invalid request payload
        '404':
          description: Upload session not found
        '409':
          description: Completed upload session cannot be cancelled
  /v1/tasks/prepare-product-codes:
    post:
      summary: Prepare task product codes
      description: |
        Allocates unique default product codes for task-create UIs.
        Default format is fixed to `NS + {CATEGORY_CODE} + {6-digit sequence}`.
        This endpoint does not require frontend code-rule/template selection and is available for
        `new_product_development` and `purchase_task`.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Admin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PrepareTaskProductCodesRequest'
      responses:
        '200':
          description: Codes prepared
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/PrepareTaskProductCodesResponse'
        '400':
          description: Invalid task type/category/count payload
  /v1/tasks:
    post:
      summary: Create task
      description: |
        Creates one task.
        For `original_product_development`, narrow by category or `search_entry_code`,
        call `GET /v1/erp/products`, choose one product, and submit that result through `product_selection`.
        Legacy `product_id`, `sku_code`, and `product_name_snapshot` fields remain accepted for compatibility.
        Current create rules:
        - `original_product_development` is existing-product only.
        - when `product_id` is null, backend resolves ERP/local binding before create-tx using this priority:
          `product_id` -> `product_selection.erp_product.product_id` -> `product_selection.erp_product.sku_code` -> top-level `sku_code`.
        - ERP-side codes are treated as bridge binding keys and are normalized to a local `products.id`; they are not used as local primary keys directly.
        - frontend should not send `source_mode`; backend infers it from `task_type`.
        - `new_product_development` infers `source_mode=new_product` and auto-generates `sku_code` when omitted.
        - `purchase_task` no longer depends on design/audit assumptions at entry; creation initializes a draft procurement record so read models expose procurement state immediately.
        - `retouch_task` is a design-only image retouch task. It infers `source_mode=new_product`, does not require product binding, does not enter audit, and is completed immediately after the retouch/design worker submits the retouched image.
        - customization workflow is decoupled from ERP order-detail APIs; no ERP order-info matching/sync dependency is required at runtime.
        - `customization_required=true` is the canonical way to create a customization-lane task; that task enters `PendingCustomizationReview` immediately and does not pass through the normal design workbench first.
        - legacy `is_outsource` / `need_outsource` create intent is folded into the same customization lane for compatibility, but new integrations must not use those fields as workflow selectors.
        - customization-lane create now also creates one primary `customization_job` immediately so `/v1/customization-jobs` visibility exists before review approval.
        - customization classification is business-configurable through `customization_level_code` and `customization_level_name`; do not assume fixed `A/B/C` levels.
        - default task product-code rule is backend-only and fixed to `NS + category_short_code(2 uppercase letters) + 6-digit sequence`; frontend no longer configures code-rules/rule-templates for task `sku_code` generation.
        - category short code generation priority is backend-owned: explicit map first (e.g. `KT_STANDARD -> KT`), otherwise first two alphabet letters from `category_code` (uppercased), then deterministic fallback to two letters.
        - sequence allocation for default task product-code uses `(prefix, category_short_code)` scope so different `category_code` values that collapse to one short code still remain unique.
        - `batch_sku_mode=multiple` is supported only for `new_product_development` and `purchase_task`; `original_product_development` returns `400 INVALID_REQUEST` with machine-readable `error.details.violations`.
        - batch Excel for `new_product_development` only requires each row's `产品名称` and `设计要求`; SKU/category internals are backend-owned.
        - batch mode writes one mother task plus multiple `task_sku_items` in one transaction and keeps `sku_code` / `primary_sku_code` aligned to the first child SKU for compatibility.
        - create now also appends `task.created`, and multi-SKU creates additionally append `task.batch_items_created`.
        - `reference_images` is no longer accepted. If present, backend returns `400 INVALID_REQUEST` and requires the reference-upload flow.
        - `reference_file_refs` must be objects returned by `POST /v1/tasks/reference-upload` or the compatibility task-create asset-center flow; forged, missing, incomplete, or unauthorized refs return `400 INVALID_REQUEST` with `invalid_reference_file_refs`.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R3
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTaskRequest'
      responses:
        '201':
          description: Task created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TaskReadModel'
        '400':
          description: Validation error. Create-task validation may include machine-readable `error.details.violations` entries describing field-level contract mismatches, including unsupported batch mode, duplicate `batch_items`, mixed top-level single-SKU fields, and invalid batch item fields.
    get:
      summary: List tasks
      description: Returns the frontend-oriented task list with projected `workflow`, aggregated `warehouse_status`, stable `product_selection` summary, `procurement_summary`, canonical actor/source fields `requester_id/requester_name`, `creator_id/creator_name`, `designer_id/designer_name`, `current_handler_id/current_handler_name`, and task org ownership fields `owner_team`, `owner_department`, and `owner_org_team`. Default ordering is latest updated first (`updated_at DESC, id DESC`). For `purchase_task`, `procurement_summary` carries procurement-to-warehouse coordination state plus lightweight product-selection provenance. Board queue `query_template` payloads are designed to be consumed directly by this endpoint. `workflow_lane` is the canonical list/workbench split selector for distinguishing the normal lane from the customization lane. Main task-flow list reads are globally visible to task-facing authenticated roles; use query filters for workbench/pool/tab slicing. Mutating actions such as assign/reassign, upload, submit, audit, filing, procurement, warehouse, close, and cancel remain action-gated by role, status, handler/assignee, and organization scope.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R3
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, CustomizationOperator, CustomizationReviewer, Audit_A, Audit_B, Warehouse, Outsource, Admin, SuperAdmin, HRAdmin, OrgAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: query
          name: status
          description: Raw `task_status` filter. Supports comma-separated multi-value queries.
          schema:
            type: array
            items: { type: string }
          style: form
          explode: false
        - in: query
          name: task_type
          description: Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              type: string
              enum: [original_product_development, new_product_development, purchase_task]
          style: form
          explode: false
        - in: query
          name: source_mode
          description: Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              type: string
              enum: [existing_product, new_product]
          style: form
          explode: false
        - in: query
          name: workflow_lane
          description: Filters list/workbench reads by canonical workflow lane. Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              type: string
              enum: [normal, customization]
          style: form
          explode: false
        - in: query
          name: main_status
          description: Filters by projected `workflow.main_status`. Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              $ref: '#/components/schemas/TaskMainStatus'
          style: form
          explode: false
        - in: query
          name: sub_status_code
          description: Filters by projected `workflow.sub_status.*.code`. If `sub_status_scope` is omitted, the code is matched against all sub-status lanes. Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              $ref: '#/components/schemas/TaskSubStatusCode'
          style: form
          explode: false
        - in: query
          name: sub_status_scope
          description: Narrows `sub_status_code` matching to one sub-status lane.
          schema:
            $ref: '#/components/schemas/TaskSubStatusScope'
        - in: query
          name: coordination_status
          description: Filters by derived `procurement_summary.coordination_status`. Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              $ref: '#/components/schemas/ProcurementCoordinationStatus'
          style: form
          explode: false
        - in: query
          name: warehouse_prepare_ready
          description: Filters by derived warehouse handoff readiness.
          schema: { type: boolean }
        - in: query
          name: warehouse_receive_ready
          description: Filters by derived warehouse receive readiness.
          schema: { type: boolean }
        - in: query
          name: warehouse_blocking_reason_code
          description: Filters tasks that currently contain any of the given `workflow.warehouse_blocking_reasons.code` values. Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              type: string
          style: form
          explode: false
        - in: query
          name: creator_id
          schema: { type: integer }
        - in: query
          name: designer_id
          schema: { type: integer }
        - in: query
          name: need_outsource
          schema: { type: boolean }
        - in: query
          name: overdue
          description: When `true`, filters `deadline_at < now` and excludes `Completed`/`Archived`/`Cancelled`; when `false`, returns the complement set.
          schema: { type: boolean }
        - in: query
          name: keyword
          description: Matches `task_no`, `sku_code`, or `product_name_snapshot`.
          schema: { type: string }
        - in: query
          name: owner_department
          description: Filters by canonical task owner department. Supports comma-separated multi-value queries.
          schema:
            type: array
            items: { type: string }
          style: form
          explode: false
        - in: query
          name: owner_org_team
          description: Filters by canonical task owner org team. Supports comma-separated multi-value queries.
          schema:
            type: array
            items: { type: string }
          style: form
          explode: false
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: page_size
          schema: { type: integer, default: 20 }
      responses:
        '200':
          description: Task list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskListResponse'
  /v1/tasks/{id}:
    get:
      summary: Get task read model
      description: Returns the root task record plus the current workflow snapshot, nullable `procurement`, frontend-friendly `procurement_summary`, full `product_selection` provenance, canonical actor/source fields `requester_id/requester_name`, `creator_id/creator_name`, `designer_id/designer_name`, `current_handler_id/current_handler_name`, compatibility alias `assignee_id/assignee_name`, task org ownership fields `owner_team`, `owner_department`, and `owner_org_team`, and cost-governance read models. `reference_file_refs` is the task-level reference-image summary field; for batch tasks, SKU-specific refs are returned on `sku_items[].reference_file_refs`. `design_assets` and `asset_versions` are the formal design-asset detail fields, and batch-task SKU scope is expressed by `scope_sku_code`. `matched_rule_governance` exposes matched-rule lineage context, `override_summary` is the lightweight current summary, `governance_audit_summary` points to the read-only override timeline, and `override_governance_boundary` exposes the current governance-boundary summary fields. `task_event_logs` remain the general task event layer. Use `/v1/tasks/{id}/detail` for the full aggregate page and `/v1/tasks/{id}/cost-overrides` for the read-only governance audit timeline. For `purchase_task`, `procurement_summary` carries arrival and warehouse handoff state plus lightweight product-selection provenance. Main task-flow read detail is globally visible to task-facing authenticated roles; mutating actions remain separately action-gated by role, status, handler/assignee, and organization scope.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, CustomizationOperator, CustomizationReviewer, Audit_A, Audit_B, Warehouse, Outsource, Admin, SuperAdmin, HRAdmin, OrgAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Task read model
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TaskReadModel'
        '403':
          description: "`PERMISSION_DENIED` with `deny_code` such as `task_out_of_department_scope` or `task_out_of_team_scope`."
        '404':
          description: Task not found
  /v1/tasks/{id}/product-info:
    get:
      summary: Get per-task product information
      description: Returns task-scoped product/business fields used by frontend product panel.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, Audit_A, Audit_B, Warehouse, Outsource, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Task product info
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      product_id: { type: integer, nullable: true }
                      sku_code: { type: string }
                      product_name: { type: string }
                      product_name_snapshot: { type: string }
                      i_id:
                        type: string
                        description: Canonical JST product style/family i_id resolved from task product info.
                      product_i_id:
                        type: string
                        description: Compatibility alias for `i_id`.
                      product_selection:
                        $ref: '#/components/schemas/TaskProductSelectionContext'
                      category: { type: string }
                      category_id: { type: integer, nullable: true }
                      category_code: { type: string }
                      category_name: { type: string }
                      material: { type: string }
                      size_text: { type: string }
                      spec_text: { type: string }
                      design_requirement:
                        type: string
                        description: Editable demand text. For original-product tasks this is a compatibility alias of `change_request`.
                      change_request:
                        type: string
                        description: Editable original-product change request. For new-product and retouch tasks prefer `design_requirement`.
                      reference_link: { type: string }
                      reference_file_refs:
                        type: array
                        items:
                          $ref: '#/components/schemas/ReferenceFileRef'
                      note: { type: string }
    patch:
      summary: Patch per-task product information
      description: Partial update of task-scoped product fields; omitted fields remain unchanged. This write path now also requires both an allowed role and a matching minimum org scope over canonical task ownership.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                operator_id: { type: integer, nullable: true }
                product_name:
                  type: string
                  nullable: true
                  description: Clean product-name edit field. Updates task product snapshot and participates in ERP filing.
                product_name_snapshot:
                  type: string
                  nullable: true
                  description: Compatibility alias for `product_name`.
                i_id:
                  type: string
                  nullable: true
                  description: Canonical Jushuitan product style/family i_id. Prefer this over legacy category/category_code fields.
                product_i_id:
                  type: string
                  nullable: true
                  description: Compatibility alias for `i_id`.
                product_selection:
                  $ref: '#/components/schemas/TaskProductSelectionContext'
                category: { type: string, nullable: true }
                category_id: { type: integer, nullable: true }
                category_code: { type: string, nullable: true }
                spec_text: { type: string, nullable: true }
                design_requirement:
                  type: string
                  nullable: true
                  description: Editable demand text. For original-product tasks backend maps this as an alias to `change_request`; for new-product and retouch tasks it writes `design_requirement`.
                change_request:
                  type: string
                  nullable: true
                  description: Editable original-product change request. For new-product and retouch tasks backend accepts it as a compatibility alias of `design_requirement`.
                material: { type: string, nullable: true }
                size_text: { type: string, nullable: true }
                reference_link: { type: string, nullable: true }
                reference_file_refs:
                  type: array
                  nullable: true
                  items:
                    $ref: '#/components/schemas/ReferenceFileRef'
                note: { type: string, nullable: true }
                trigger_filing:
                  type: boolean
                  nullable: true
                  description: Optional active sync switch. When true, backend forces ERP filing evaluation immediately; otherwise new-product tasks auto-file on product-info/business-info patch per backend policy.
                remark: { type: string, nullable: true }
      responses:
        '200':
          description: Updated task detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TaskDetail'
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
  /v1/tasks/{id}/cost-info:
    get:
      summary: Get per-task cost information
      description: Returns task-scoped cost fields and governance light metadata.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, Audit_A, Audit_B, Warehouse, Outsource, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Task cost info
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      cost_price: { type: number, format: double, nullable: true }
                      estimated_cost: { type: number, format: double, nullable: true }
                      cost_rule_id: { type: integer, nullable: true }
                      cost_rule_name: { type: string }
                      cost_rule_source: { type: string }
                      matched_rule_version: { type: integer, nullable: true }
                      prefill_source: { type: string }
                      prefill_at: { type: string, format: date-time, nullable: true }
                      requires_manual_review: { type: boolean }
                      manual_cost_override: { type: boolean }
                      manual_cost_override_reason: { type: string }
                      override_actor: { type: string }
                      override_at: { type: string, format: date-time, nullable: true }
    patch:
      summary: Patch per-task cost information
      description: Partial update of task-scoped cost fields; omitted fields remain unchanged. This write path now also requires both an allowed role and a matching minimum org scope over canonical task ownership.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                operator_id: { type: integer, nullable: true }
                cost_price: { type: number, format: double, nullable: true }
                cost_rule_id: { type: integer, nullable: true }
                cost_rule_name: { type: string, nullable: true }
                cost_rule_source: { type: string, nullable: true }
                manual_cost_override: { type: boolean, nullable: true }
                manual_cost_override_reason: { type: string, nullable: true }
                remark: { type: string, nullable: true }
      responses:
        '200':
          description: Updated task detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TaskDetail'
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
  /v1/tasks/{id}/cost-quote/preview:
    post:
      summary: Preview cost quote for one task
      description: Runs cost-rule preview using task defaults plus optional request overrides.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                operator_id: { type: integer, nullable: true }
                category_id: { type: integer, nullable: true }
                category_code: { type: string, nullable: true }
                width: { type: number, format: double, nullable: true }
                height: { type: number, format: double, nullable: true }
                area: { type: number, format: double, nullable: true }
                quantity: { type: integer, nullable: true }
                process: { type: string, nullable: true }
                notes: { type: string, nullable: true }
      responses:
        '200':
          description: Cost preview result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CostRulePreviewResponse'
  /v1/tasks/{id}/business-info:
    patch:
      summary: Update task business-info and generic cost fields
      description: |
        Maintains PRD V2.0 front-loaded category/spec/cost/filed information used by warehouse
        handoff and close-readiness checks. When category plus minimal
        width/height/area/quantity/process inputs are present, the backend also triggers skeleton
        cost preview and persists `estimated_cost`, rule provenance, governed
        `matched_rule_version`, and manual-review state. Existing-product tasks may also persist or
        rebind `product_selection` here so the selected product stays traceable back to local
        mapped-search provenance and optional ERP Bridge external snapshot fields.
        Filing now uses backend state-machine auto triggers and idempotent payload comparison.
        Legacy `trigger_filing` and `filed_at` remain compatibility forced triggers.
        Bridge remains the ERP/JST adapter and mutation executor; MAIN decides business boundary
        and records filing traces/status. `cost_price` is the
        current effective internal cost, while `manual_cost_override` distinguishes business-side
        override from system prefill; `prefill_source`, `prefill_at`, `override_actor`, and
        `override_at` provide governance trace, and override state changes append a dedicated
        `cost_override_events` audit record. This
        remains a narrow filing/master-data boundary only, not a broad ERP docking, approval flow,
        finance system, procurement/WMS integration, or raw ERP mutation API family on MAIN.
        Historical tasks are not auto-recomputed by later rule changes; new rule changes affect
        future preview/prefill only. Procurement preparation is maintained separately via
        `/v1/tasks/{id}/procurement`.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTaskBusinessInfoRequest'
      responses:
        '200':
          description: Task detail updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TaskDetail'
        '400':
          description: Invalid request payload
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '500':
          description: Internal error
        '404':
          description: Task not found
  /v1/tasks/{id}/filing-status:
    get:
      summary: Get task filing status view
      description: Returns filing state-machine status, missing fields, and retry hints for frontend display.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin, Designer, Audit_A, Audit_B]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Filing status
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TaskFilingStatusView'
        '404':
          description: Task not found
  /v1/tasks/{id}/filing/retry:
    post:
      summary: Retry task filing
      description: Forces one filing retry attempt using current task payload snapshot and updates filing status fields. This write path now also requires both an allowed role and a matching minimum org scope over canonical task ownership.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RetryTaskFilingRequest'
      responses:
        '200':
          description: Filing retried
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TaskFilingStatusView'
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task not found
  /v1/tasks/{id}/procurement:
    patch:
      summary: Update purchase-task procurement draft data
      description: Creates or updates the dedicated procurement record used by `purchase_task` readiness, coordination summaries, and structured procurement sub-status. Status remains explicit and mutable, and `/v1/tasks/{id}/procurement/advance` provides the minimal lifecycle transition action. This write path requires both an allowed role and a matching minimum org or handler scope over canonical task ownership.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTaskProcurementRequest'
      responses:
        '200':
          description: Procurement record updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ProcurementRecord'
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task not found
        '409':
          description: Task is not a purchase task
  /v1/tasks/{id}/procurement/advance:
    post:
      summary: Advance purchase-task procurement lifecycle
      description: |
        Performs minimal procurement lifecycle transitions for `purchase_task`:
        `prepare`, `start`, `complete`, or `reopen`.
        This write path now also requires both an allowed role and a matching minimum org/handler
        scope over canonical task ownership.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdvanceTaskProcurementRequest'
      responses:
        '200':
          description: Procurement lifecycle advanced
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ProcurementRecord'
        '400':
          description: Invalid action or missing required draft data
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task not found
        '409':
          description: Task is not a purchase task or the transition is invalid
  /v1/tasks/{id}/detail:
    get:
      summary: Get task aggregate detail (V1.1-A1 fast-path)
      description: |
        Returns the task aggregate produced by `task_aggregator.DetailService` fast path.
        Top-level `data` contains: `task`, nullable `task_detail`, `modules[]`, `events[]`
        (service caps recent events at 50), `reference_file_refs[]`, `sku_items[]`, and
        `asset_versions[]`. For batch tasks, `sku_items[]` is present on this detail endpoint so
        frontend can render per-SKU tabs without a second read. Design upload versions preserve
        batch scope through `asset_versions[].scope_sku_code`, copied from upload-session
        `target_sku_code`. Rich snapshot sections
        such as `procurement_summary`, full top-level `product_selection`, `matched_rule_governance`,
        `design_assets`, and `governance_audit_summary` are not returned by this endpoint in
        v1.21; use dedicated routes such as
        `/v1/tasks/{id}/procurement`, `/v1/tasks/{id}/asset-center/*`, and
        `/v1/tasks/{id}/cost-overrides` for those read models.
        Main task-flow aggregate detail is globally visible to task-facing authenticated roles;
        all mutating actions remain separately action-gated by role, status, handler/assignee,
        and organization scope.
      tags: [TaskDetail, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: V1.1-A1
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, CustomizationOperator, CustomizationReviewer, Audit_A, Audit_B, Warehouse, Outsource, Admin, SuperAdmin, HRAdmin, OrgAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Aggregate detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TaskAggregateDetailV2'
        '404':
          description: Task not found
  /v1/tasks/{id}/cost-overrides:
    get:
      summary: Get task cost-override governance audit timeline
      description: Returns the dedicated read-only cost override and governance audit timeline for one task. This timeline records override-specific audit facts such as previous estimated cost, override cost, matched rule and version context, actor and time, and release events. `override_governance_boundary` reuses the same boundary summary object exposed by task, detail, and procurement reads. `task_event_logs` remain the general task event stream and coexist with this governance-specific audit layer. This endpoint is not an approval flow, finance system, accounting contract, or ERP writeback contract.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Task cost override governance audit timeline
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TaskCostOverrideAuditTimeline'
        '404':
          description: Task not found
  /v1/tasks/{id}/cost-overrides/{event_id}/review:
    post:
      summary: Upsert cost-override review placeholder boundary
      description: Adds or updates the approval-side placeholder boundary for one dedicated `cost_override_events` row. This is a skeleton governance handoff only; it is not a real approval workflow, identity approval chain, or permission model.
      tags: [Tasks, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Ops, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: event_id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TaskCostOverrideReviewRequest'
      responses:
        '200':
          description: Updated placeholder review boundary
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TaskCostOverrideGovernanceBoundary'
        '404':
          description: Task or override event not found
  /v1/tasks/{id}/cost-overrides/{event_id}/finance-mark:
    post:
      summary: Upsert cost-override finance placeholder boundary
      description: Adds or updates the finance-side placeholder boundary for one dedicated `cost_override_events` row. This is a future finance-handoff skeleton only; it is not a real finance system, ledger, reconciliation, settlement, invoice, or ERP writeback interface.
      tags: [Tasks, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [ERP, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: event_id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TaskCostOverrideFinanceMarkRequest'
      responses:
        '200':
          description: Updated placeholder finance boundary
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TaskCostOverrideGovernanceBoundary'
        '404':
          description: Task or override event not found
  /v1/tasks/{id}/assign:
    post:
      summary: Assign task to designer
      description: |
        `POST /v1/tasks/{id}/assign` now carries two bounded semantics under the same route:
        - `PendingAssign`: assign is allowed for the existing operation/management path within the allowed org scope. A Designer may also self-claim an unassigned task by sending their own user id as `designer_id`; success sets `designer_id` and `current_handler_id`, then moves the task to `InProgress`.
        - `InProgress`: the same route acts as reassign. Allowed actors are requester/initiator (`requester_id` or `creator_id`), the current owning-group `TeamLead`, and scoped management roles (`DepartmentAdmin`, `DesignDirector`, `RoleAdmin`, `HRAdmin`, `SuperAdmin`, `Admin`). Ordinary Ops users without those conditions are denied.
        - Audit / warehouse / close states remain denied with machine-readable `PERMISSION_DENIED` details such as `task_not_reassignable`.
        - `purchase_task` cannot be assigned or reassigned to a designer.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignTaskRequest'
      responses:
        '200':
          description: Task assigned
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Task'
        '403':
          description: "`PERMISSION_DENIED` with machine-readable task-action details such as `missing_required_role`, `task_out_of_department_scope`, `task_out_of_team_scope`, `task_not_reassignable`, or `task_reassign_requires_requester_or_manager`."
        '404':
          description: Task not found
        '409':
          description: Invalid task state such as attempting designer assignment on `purchase_task`
  /v1/tasks/batch/assign:
    post:
      summary: Batch assign tasks to designer
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/BatchTaskActionResult'
  /v1/tasks/batch/remind:
    post:
      summary: Batch remind task handlers
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/BatchTaskActionResult'
  /v1/tasks/{id}/submit-design:
      post:
        summary: Submit task design asset
        description: |
          Supports two submit modes:
          1) Compatibility single-asset submit (`asset_type` + `file_name`) which creates one `task_assets` record.
          2) Batch submit (`assets[]`) which completes multiple upload sessions in a single action and persists canonical
             `design_assets`/`asset_versions` with SKU scope.
          Re-entry is allowed from `RejectedByAuditA` and `RejectedByAuditB`. Delivery upload-session completion advances
          normal design task status to `PendingAuditA` when current status is one of `PendingAssign`, `Assigned`, `InProgress`,
          `RejectedByAuditA`, or `RejectedByAuditB`, and for multi-SKU batch tasks the gate waits until required SKU-scoped
          delivery assets are complete.
          For `retouch_task`, delivery submission completes the task directly (`task_status=Completed`), clears `current_handler_id`,
          marks the `retouch` module completed, and never triggers audit.
          `purchase_task` cannot submit design. This action uses minimum role plus org or handler gating.
      tags: [TaskAssets, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmitDesignRequest'
      responses:
        '201':
          description: Design submitted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    oneOf:
                      - $ref: '#/components/schemas/TaskAsset'
                      - $ref: '#/components/schemas/SubmitDesignBatchResult'
        '400':
          description: Invalid request
        '403':
          description: "`PERMISSION_DENIED` with `deny_code` such as `task_not_assigned_to_actor`, `task_out_of_department_scope`, or `task_status_not_actionable`."
        '404':
          description: Task not found
        '409':
          description: Missing designer assignment or invalid task state
  /v1/tasks/{id}/assets:
    get:
      summary: List task-linked design assets
      description: |
        Canonical task-scoped design-asset list path.
        Returns the same resource model as `GET /v1/assets?task_id={id}` and keeps task detail pages on
        one explicit task context route while `/v1/assets` remains the canonical cross-task resource namespace.
      x-workflow-api-status: canonical
      x-workflow-new-usage-allowed: true
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, CustomizationOperator, CustomizationReviewer, Ops, Audit_A, Audit_B, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Design asset center list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DesignAsset'
        '404':
          description: Task not found
  /v1/tasks/{id}/assets/timeline:
    get:
      summary: List legacy task asset timeline
      description: Returns the append-only task-asset timeline ordered by `version_no ASC`. This is a compatibility-only standalone refresh view, obsolete for frontend rollout, and not the primary design-asset aggregation surface for new frontend upload integration.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/tasks/{id}/assets
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: candidate_for_v1_0_removal
      tags: [TaskAssets, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, CustomizationOperator, CustomizationReviewer, Ops, Audit_A, Audit_B, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Task asset timeline
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/TaskAsset'
        '404':
          description: Task not found
  /v1/tasks/{id}/assets/{asset_id}/versions:
    get:
      summary: List versions under one design asset
      description: Compatibility-only alias for `GET /v1/tasks/{id}/asset-center/assets/{asset_id}/versions`. Obsolete for frontend rollout.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/tasks/{id}/asset-center/assets/{asset_id}/versions
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Audit_A, Audit_B]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: asset_id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Design asset versions
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DesignAssetVersion'
        '404':
          description: Task or asset not found
  /v1/tasks/{id}/assets/{asset_id}/download:
    get:
      summary: Get latest version download info for one design asset
      description: Compatibility-only alias for `GET /v1/tasks/{id}/asset-center/assets/{asset_id}/download`. Obsolete for frontend rollout.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/tasks/{id}/asset-center/assets/{asset_id}/download
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Audit_A, Audit_B]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: asset_id
          required: true
          schema: { type: integer }
        - in: header
          name: X-Network-Probe-Reachable
          required: false
          schema: { type: boolean }
        - in: header
          name: X-Network-Probe-Method
          required: false
          schema: { type: string }
        - in: header
          name: X-Network-Probe-URL
          required: false
          schema: { type: string }
        - in: header
          name: X-Network-Probe-Checked-At
          required: false
          schema: { type: string, format: date-time }
        - in: header
          name: X-Network-Probe-Status-Code
          required: false
          schema: { type: integer }
        - in: header
          name: X-Network-Probe-Error
          required: false
          schema: { type: string }
        - in: header
          name: X-Network-Probe-Attestation
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Asset download metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AssetDownloadInfo'
        '404':
          description: Task or asset not found
  /v1/tasks/{id}/assets/{asset_id}/versions/{version_id}/download:
    get:
      summary: Get specific version download info for one design asset
      description: Compatibility-only alias for `GET /v1/tasks/{id}/asset-center/assets/{asset_id}/versions/{version_id}/download`. Obsolete for frontend rollout.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/tasks/{id}/asset-center/assets/{asset_id}/versions/{version_id}/download
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Audit_A, Audit_B]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: asset_id
          required: true
          schema: { type: integer }
        - in: path
          name: version_id
          required: true
          schema: { type: integer }
        - in: header
          name: X-Network-Probe-Reachable
          required: false
          schema: { type: boolean }
        - in: header
          name: X-Network-Probe-Method
          required: false
          schema: { type: string }
        - in: header
          name: X-Network-Probe-URL
          required: false
          schema: { type: string }
        - in: header
          name: X-Network-Probe-Checked-At
          required: false
          schema: { type: string, format: date-time }
        - in: header
          name: X-Network-Probe-Status-Code
          required: false
          schema: { type: integer }
        - in: header
          name: X-Network-Probe-Error
          required: false
          schema: { type: string }
        - in: header
          name: X-Network-Probe-Attestation
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Asset version download metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AssetDownloadInfo'
        '404':
          description: Task, asset, or version not found
  /v1/tasks/{id}/assets/upload-sessions:
    post:
      summary: Create upload session
      description: |
        Compatibility-only alias for `POST /v1/assets/upload-sessions`.
        Obsolete for frontend rollout; new integration must use the top-level asset session contract.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/assets/upload-sessions
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTaskAssetUploadSessionRequest'
      responses:
        '201':
          description: Upload session created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CreateTaskAssetUploadSessionResponseData'
        '400':
          description: Invalid request payload
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task or asset not found
  /v1/tasks/{id}/assets/upload-sessions/{session_id}:
    get:
      summary: Get upload session status
      description: |
        Compatibility-only alias for `GET /v1/assets/upload-sessions/{session_id}`.
        Obsolete for frontend rollout.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/assets/upload-sessions/{session_id}
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Audit_A, Audit_B]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: session_id
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Upload session
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/UploadSession'
        '404':
          description: Task or upload session not found
  /v1/tasks/{id}/assets/upload-sessions/{session_id}/complete:
    post:
      summary: Complete upload session and record asset version
      description: |
        Compatibility-only alias for `POST /v1/assets/upload-sessions/{session_id}/complete`.
        Obsolete for frontend rollout.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/assets/upload-sessions/{session_id}/complete
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: session_id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompleteTaskAssetUploadSessionRequest'
      responses:
        '200':
          description: Upload session completed and asset version recorded
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CompleteTaskAssetUploadSessionResponseData'
        '400':
          description: Invalid request payload
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task or upload session not found
        '409':
          description: Upload session already terminal or asset type mismatch
  /v1/tasks/{id}/assets/upload-sessions/{session_id}/abort:
    post:
      summary: Abort upload session
      description: |
        Compatibility-only alias for `POST /v1/assets/upload-sessions/{session_id}/cancel`.
        Obsolete for frontend rollout.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/assets/upload-sessions/{session_id}/cancel
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: session_id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelTaskAssetUploadSessionRequest'
      responses:
        '200':
          description: Upload session aborted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/UploadSession'
        '400':
          description: Invalid request payload
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task or upload session not found
        '409':
          description: Completed upload session cannot be aborted
  /v1/tasks/{id}/assets/upload:
    post:
      summary: Create small-file direct upload handoff (legacy path)
      description: |
        **Deprecated browser contract:** `multipart/form-data` uploads (`file` + `file_role`)
        are **not supported** on this path and return **410** with `UPLOAD_ENDPOINT_DEPRECATED`.
        **Supported:** `application/json` body identical to
        `POST /v1/assets/upload-sessions` (backend-selected upload strategy).
        **Design drafts (source / delivery / preview):** use the unified asset upload-session contract,
        upload bytes using the returned remote plan, then call `.../complete`.
        Preferred entrypoint for new code: `/v1/assets/upload-sessions` instead of this legacy URL.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: deprecated
      x-workflow-successor-path: /v1/assets/upload-sessions
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTaskAssetUploadSessionRequest'
      responses:
        '201':
          description: Small upload handoff created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CreateTaskAssetUploadSessionResponseData'
        '400':
          description: Invalid request payload
        '404':
          description: Task or asset not found
        '410':
          description: |
            `UPLOAD_ENDPOINT_DEPRECATED` when `Content-Type` is `multipart/form-data` or
            `application/x-www-form-urlencoded`. Use asset-center upload session JSON handoff + OSS
            upload + complete (see response `details`).
  /v1/tasks/{id}/asset-center/assets:
    get:
      summary: List design assets in task asset center
      description: |
        Compatibility-only alias for `GET /v1/tasks/{id}/assets`.
        Keep for migration safety only; new frontend integration must use the canonical
        task-linked lookup path `GET /v1/tasks/{id}/assets`.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/tasks/{id}/assets
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Audit_A, Audit_B]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Design asset center list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DesignAsset'
        '404':
          description: Task not found
  /v1/tasks/{id}/asset-center/assets/{asset_id}/versions:
    get:
      summary: List versions under one design asset
      description: Compatibility-only alias for `GET /v1/tasks/{id}/assets/{asset_id}/versions`. Obsolete for frontend rollout.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/tasks/{id}/assets/{asset_id}/versions
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Audit_A, Audit_B]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: asset_id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Design asset versions
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DesignAssetVersion'
        '404':
          description: Task or asset not found
  /v1/tasks/{id}/asset-center/assets/{asset_id}/download:
    get:
      summary: Get latest version download info for one design asset
      description: Compatibility-only alias for `GET /v1/tasks/{id}/assets/{asset_id}/download`. Obsolete for frontend rollout.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/tasks/{id}/assets/{asset_id}/download
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Audit_A, Audit_B]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: asset_id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Asset download metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AssetDownloadInfo'
        '404':
          description: Task or asset not found
  /v1/tasks/{id}/asset-center/assets/{asset_id}/versions/{version_id}/download:
    get:
      summary: Get specific version download info for one design asset
      description: Compatibility-only alias for `GET /v1/tasks/{id}/assets/{asset_id}/versions/{version_id}/download`. Obsolete for frontend rollout.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/tasks/{id}/assets/{asset_id}/versions/{version_id}/download
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Audit_A, Audit_B]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: asset_id
          required: true
          schema: { type: integer }
        - in: path
          name: version_id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Asset version download metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AssetDownloadInfo'
        '404':
          description: Task, asset, or version not found
  /v1/tasks/{id}/asset-center/upload-sessions:
    post:
      summary: Create upload session
      description: Compatibility-only alias for `POST /v1/assets/upload-sessions`. Use the top-level asset session route for new frontend work.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/assets/upload-sessions
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTaskAssetUploadSessionRequest'
      responses:
        '201':
          description: Upload session created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CreateTaskAssetUploadSessionResponseData'
        '400':
          description: Invalid request payload
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task or asset not found
  /v1/tasks/{id}/asset-center/upload-sessions/small:
    post:
      summary: Create small-file upload session
      description: Compatibility-only alias for `POST /v1/assets/upload-sessions`. Use the top-level asset session route for new frontend work.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/assets/upload-sessions
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTaskAssetUploadSessionRequest'
      responses:
        '201':
          description: Upload session created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CreateTaskAssetUploadSessionResponseData'
        '400':
          description: Invalid request payload
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task or asset not found
  /v1/tasks/{id}/asset-center/upload-sessions/multipart:
    post:
      summary: Create multipart upload session
      description: Compatibility-only alias for `POST /v1/assets/upload-sessions`. Use the top-level asset session route for new frontend work.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/assets/upload-sessions
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTaskAssetUploadSessionRequest'
      responses:
        '201':
          description: Multipart upload session created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CreateTaskAssetUploadSessionResponseData'
        '400':
          description: Invalid request payload
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task or asset not found
  /v1/tasks/{id}/asset-center/upload-sessions/{session_id}:
    get:
      summary: Get upload session status
      description: Compatibility-only alias for `GET /v1/assets/upload-sessions/{session_id}`. Returns the MAIN-side upload-session business view.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/assets/upload-sessions/{session_id}
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Audit_A, Audit_B]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: session_id
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Upload session
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/UploadSession'
        '404':
          description: Task or upload session not found
  /v1/tasks/{id}/asset-center/upload-sessions/{session_id}/complete:
    post:
      summary: Complete upload session and record asset version
      description: |
        Compatibility-only alias for `POST /v1/assets/upload-sessions/{session_id}/complete`.
        For multi-SKU batch delivery submissions, whole-task status does not advance on the first successful bucket.
        `PendingAuditA` is entered only after all SKU items required by the batch have a completed delivery asset.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/assets/upload-sessions/{session_id}/complete
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: session_id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompleteTaskAssetUploadSessionRequest'
      responses:
        '200':
          description: Upload session completed and asset version recorded
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CompleteTaskAssetUploadSessionResponseData'
        '400':
          description: Invalid request payload
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task or upload session not found
        '409':
          description: Upload session already terminal or asset type mismatch
  /v1/tasks/{id}/asset-center/upload-sessions/{session_id}/cancel:
    post:
      summary: Cancel upload session
      description: Compatibility-only alias for `POST /v1/assets/upload-sessions/{session_id}/cancel`. Cancels the MAIN business session and aborts the remote OSS upload session when needed.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/assets/upload-sessions/{session_id}/cancel
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: remove_after_frontend_migration
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: session_id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelTaskAssetUploadSessionRequest'
      responses:
        '200':
          description: Upload session cancelled
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/UploadSession'
        '400':
          description: Invalid request payload
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task or upload session not found
        '409':
          description: Completed upload session cannot be cancelled
  /v1/tasks/{id}/asset-center/upload-sessions/{session_id}/abort:
    post:
      summary: Abort upload session
      description: Compatibility-only alias for `POST /v1/assets/upload-sessions/{session_id}/cancel`. Obsolete for frontend rollout.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/assets/upload-sessions/{session_id}/cancel
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: candidate_for_v1_0_removal
      tags: [TaskAssetCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Designer, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: session_id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelTaskAssetUploadSessionRequest'
      responses:
        '200':
          description: Upload session aborted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/UploadSession'
        '400':
          description: Invalid request payload
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task or upload session not found
        '409':
          description: Completed upload session cannot be aborted
  /v1/tasks/{id}/warehouse/prepare:
    post:
      summary: Prepare task for warehouse handoff
      description: Evaluates PRD-aligned warehouse readiness. Purchase tasks may enter the warehouse path without design/audit, but they must complete procurement arrival before warehouse handoff; design task types still require final asset and approved audit path. This action now uses minimum role + org scope gating over canonical task ownership.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PrepareWarehouseRequest'
      responses:
        '200':
          description: Task moved to `PendingWarehouseReceive`
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Task'
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task not found
        '409':
          description: Warehouse blocking reasons prevent handoff
  /v1/tasks/{id}/assets/mock-upload:
    post:
      summary: Mock upload task asset
      description: Creates a `task_assets` record without changing task status. Intended for prototype reference or attachment areas. This route can optionally bind a placeholder `upload_request_id` and emit structured `storage_ref` metadata, but it remains mock or placeholder only and is not a stable real-upload contract.
      tags: [TaskAssets, MockPlaceholderOnly]
      x-api-readiness: mock_placeholder_only
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Designer, Ops]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MockUploadTaskAssetRequest'
      responses:
        '201':
          description: Task asset created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TaskAsset'
        '400':
          description: Invalid request
        '404':
          description: Task not found
  /v1/assets/files/{path}:
    get:
      summary: Proxy OSS-backed business file
      description: |
        Compatibility-only proxy byte-serving route for OSS-backed business files. Canonical browser download should use the URL returned by `/v1/assets/{id}/download` or `/v1/assets/{id}/preview`.
        Path is the storage_key (e.g. tasks/task-create-reference/assets/.../filename.png).
      tags: [ReadyForFrontend]
      parameters:
        - in: path
          name: path
          required: true
          schema: { type: string }
          description: Storage key (may contain slashes)
      responses:
        '200':
          description: File bytes (Content-Type from upstream)
        '404':
          description: File not found
        '502':
          description: Upstream file request failed
  /v1/assets/upload-requests:
    get:
      summary: List asset upload requests
      description: |
        Internal placeholder management view for upload requests. Supports paginated filtering by
        owner boundary, task-asset type, and lifecycle status so upload/storage placeholder records
        can be inspected without introducing a real upload allocator, signed-URL session, or object
        storage/NAS integration.
      tags: [InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Ops, Designer, Audit_A, Audit_B, Warehouse, Outsource, Admin]
      parameters:
        - in: query
          name: owner_type
          schema:
            $ref: '#/components/schemas/AssetOwnerType'
        - in: query
          name: owner_id
          schema:
            type: integer
        - in: query
          name: task_asset_type
          schema:
            type: string
            enum: [reference, source, delivery, preview, design_thumb]
        - in: query
          name: status
          schema:
            $ref: '#/components/schemas/UploadRequestStatus'
        - in: query
          name: page
          schema:
            type: integer
            minimum: 1
        - in: query
          name: page_size
          schema:
            type: integer
            minimum: 1
            maximum: 100
      responses:
        '200':
          description: Paginated upload requests
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/UploadRequest'
                  pagination:
                    $ref: '#/components/schemas/PaginationMeta'
        '400':
          description: Invalid filter values
    post:
      summary: Create asset upload request
      description: |
        Internal placeholder upload-intent boundary for task assets. This endpoint does not upload file bytes,
        does not return signed URLs, and does not connect to NAS/object storage. It only records one placeholder
        upload request that later task-asset write actions may bind into a `storage_ref`. Shared
        `adapter_mode`, `dispatch_mode`, `storage_mode`, `adapter_ref_summary`, and
        `handoff_ref_summary` fields keep storage and upload language aligned with export and integration.
        Lifecycle advancement remains a separate internal placeholder route.
      tags: [InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Ops, Designer, Audit_A, Audit_B, Warehouse, Outsource, Admin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUploadRequestRequest'
      responses:
        '201':
          description: Upload request created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/UploadRequest'
        '400':
          description: Invalid request payload or unsupported owner boundary
        '404':
          description: Owner not found
  /v1/assets/upload-requests/{id}:
    get:
      summary: Get asset upload request
      description: Returns one persisted placeholder upload request. `can_bind`, `can_cancel`, and `can_expire` expose internal placeholder lifecycle readiness while the payload remains storage and upload metadata only.
      tags: [InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Ops, Designer, Audit_A, Audit_B, Warehouse, Outsource, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Upload request
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/UploadRequest'
        '404':
          description: Upload request not found
  /v1/assets/upload-requests/{id}/advance:
    post:
      summary: Advance asset upload request
      description: |
        Internal placeholder lifecycle action for upload requests. Supports `cancel` and `expire`
        while keeping `bound` reserved for later task-asset binding. This endpoint does not upload bytes,
        does not allocate storage, and does not replace task-asset writes as the actual binding path.
      tags: [InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Ops, Designer, Audit_A, Audit_B, Warehouse, Outsource, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdvanceUploadRequestRequest'
      responses:
        '200':
          description: Upload request advanced
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/UploadRequest'
        '400':
          description: Invalid request payload or invalid upload-request lifecycle transition
        '404':
          description: Upload request not found
  /v1/tasks/{id}/close:
    post:
      summary: Close task explicitly
      description: |
        Requires the task to be in explicit `PendingClose` mainline state.
        This action now uses minimum role + canonical-owner org gating instead of role name alone.
        `Admin`/`SuperAdmin`/`RoleAdmin`/`HRAdmin` may cross org scope but still cannot bypass the `PendingClose` status gate.
        `Ops`, `Warehouse`, and scoped management roles must still match the task canonical owner department/team scope.
        On readiness failure, `error.details` returns:
        - `task_type`
        - `workflow`
        - `closable`
        - `cannot_close_reasons`
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Warehouse, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CloseTaskRequest'
      responses:
        '200':
          description: Task closed
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TaskReadModel'
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task not found
        '409':
          description: Task is not ready to close; see `error.details.workflow` and `error.details.cannot_close_reasons`
  /v1/tasks/{id}/audit/claim:
    post:
      summary: Claim task for audit
      description: "`auditor_id` is optional and defaults to the current authenticated actor. This action uses minimum role plus org scope gating over canonical task ownership. `Audit_A` may only claim `PendingAuditA`; `Audit_B` may only claim `PendingAuditB`; when a current handler already exists, non-management actors must match that handler instead of taking over globally."
      tags: [Audit, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Audit_A, Audit_B, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [stage]
              properties:
                auditor_id:
                  type: integer
                  description: Optional override for compatibility. Defaults to the current authenticated actor.
                stage:
                  type: string
                  enum: [A, B, outsource_review]
      responses:
        '200':
          description: Claimed
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task not found
        '409':
          description: Invalid task state
  /v1/tasks/{id}/audit/approve:
      post:
        summary: Approve audit and move task to next status
        description: "`auditor_id` is optional and defaults to the current authenticated actor. Approval clears the current audit handler so the next audit or warehouse stage must be explicitly claimed or received. This action uses minimum role plus org or handler gating over canonical task ownership. `Audit_A` can only approve stage A (`PendingAuditA`), `Audit_B` can only approve stage B (`PendingAuditB`), and non-management actors must be the current handler."
      tags: [Audit, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Audit_A, Audit_B, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [stage, next_status]
              properties:
                auditor_id:
                  type: integer
                  description: Optional override for compatibility. Defaults to the current authenticated actor.
                stage:
                  type: string
                  enum: [A, B, outsource_review]
                next_status:
                  type: string
                  enum: [PendingAuditB, PendingWarehouseReceive, PendingOutsource]
                comment: { type: string }
                issue_types:
                  type: array
                  items:
                    type: string
                    enum: [color_error, size_error, text_error, layout_error, quality_issue, other]
      responses:
        '200':
          description: Approved
        '403':
          description: "`PERMISSION_DENIED` with `deny_code` such as `task_out_of_department_scope`, `task_out_of_team_scope`, `task_not_assigned_to_actor`, or `task_status_not_actionable`."
        '404':
          description: Task not found
        '409':
          description: Invalid task state or transition
  /v1/tasks/{id}/audit/reject:
      post:
        summary: Reject audit
        description: "`auditor_id` is optional and defaults to the current authenticated actor. Audit rejection routes the task back to the designer handler so rework is explicit. This action uses minimum role plus org or handler gating over canonical task ownership. `Audit_A` can only reject stage A, `Audit_B` can only reject stage B, and non-management actors must be the current handler."
      tags: [Audit, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Audit_A, Audit_B, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [stage, comment]
              properties:
                auditor_id:
                  type: integer
                  description: Optional override for compatibility. Defaults to the current authenticated actor.
                stage:
                  type: string
                  enum: [A, B]
                comment: { type: string }
                issue_types:
                  type: array
                  items:
                    type: string
                    enum: [color_error, size_error, text_error, layout_error, quality_issue, other]
                affects_launch: { type: boolean }
      responses:
        '200':
          description: Rejected
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task not found
        '409':
          description: Invalid task state
  /v1/tasks/{id}/audit/transfer:
    post:
      summary: Transfer audit responsibility
      description: "`from_auditor_id` is optional and defaults to the current authenticated actor. This action uses minimum role plus org or handler gating over canonical task ownership. `Audit_A` can only transfer stage A, `Audit_B` can only transfer stage B, and non-management actors must currently own the handler slot."
      tags: [Audit, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Audit_A, Audit_B, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [to_auditor_id, stage]
              properties:
                from_auditor_id:
                  type: integer
                  description: Optional override for compatibility. Defaults to the current authenticated actor.
                to_auditor_id: { type: integer }
                stage:
                  type: string
                  enum: [A, B, outsource_review]
                comment: { type: string }
      responses:
        '200':
          description: Transferred
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task not found
        '409':
          description: Invalid task state
  /v1/tasks/{id}/audit/handover:
    post:
      summary: Create audit handover
      tags: [Audit, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Audit handover
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AuditHandover'
  /v1/tasks/{id}/audit/handovers:
    get:
      summary: List audit handovers for task
      description: Ordered by `created_at DESC`. This endpoint keeps the pre-Step-05 `data`-only list shape.
      tags: [Audit, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Audit_A, Audit_B, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Handover list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/AuditHandover'
        '404':
          description: Task not found
  /v1/tasks/{id}/audit/takeover:
      post:
        summary: Take over pending handover
        description: "`auditor_id` is optional and defaults to the current authenticated actor. Takeover restores explicit audit ownership by setting the task handler to the takeover auditor. This action uses minimum role plus org scope gating over canonical task ownership. `Audit_A` can only take over stage A handovers, `Audit_B` can only take over stage B handovers, and management roles remain state-gated."
      tags: [Audit, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Audit_A, Audit_B, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [handover_id]
              properties:
                handover_id: { type: integer }
                auditor_id:
                  type: integer
                  description: Optional override for compatibility. Defaults to the current authenticated actor.
      responses:
        '200':
          description: Taken over
        '403':
          description: "`PERMISSION_DENIED` with task-action `deny_code` details when the actor is outside the allowed org scope."
        '404':
          description: Task or handover not found
        '409':
          description: Invalid handover or task state
  /v1/tasks/{id}/outsource:
    post:
      summary: Create outsource order for task
      description: Compatibility-only legacy late-branch entry retained for historical tasks. New integrations must create customization-lane tasks with `customization_required=true` instead of using this route.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/tasks
      x-workflow-new-usage-allowed: false
      tags: [Outsource, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Outsource, Ops, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [operator_id, vendor_name, outsource_type]
              properties:
                operator_id: { type: integer }
                vendor_name: { type: string }
                outsource_type: { type: string }
                delivery_requirement: { type: string }
                settlement_note: { type: string }
      responses:
        '201':
          description: Outsource created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/OutsourceOrder'
        '404':
          description: Task not found
        '409':
          description: Task not in PendingOutsource
  /v1/outsource-orders:
    get:
      summary: List outsource orders
      description: Compatibility-only legacy list for historical late-branch outsource records. New integrations should use `/v1/customization-jobs` for the unified customization lane. `vendor` is matched with fuzzy `LIKE` against `vendor_name`.
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: compatibility
      x-workflow-successor-path: /v1/customization-jobs
      x-workflow-new-usage-allowed: false
      tags: [Outsource, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Outsource, Ops, Admin]
      parameters:
        - in: query
          name: task_id
          schema: { type: integer }
        - in: query
          name: status
          schema: { type: string }
        - in: query
          name: vendor
          schema: { type: string }
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: page_size
          schema: { type: integer, default: 20 }
      responses:
        '200':
          description: Outsource order list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OutsourceOrderListResponse'
  /v1/warehouse/receipts:
    get:
      summary: List warehouse receipts
      description: Returns paginated warehouse receipts. `receiver_id` filters against the current receipt owner/receiver.
      tags: [Warehouse, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Warehouse, Ops, Admin]
      parameters:
        - in: query
          name: task_id
          schema: { type: integer }
        - in: query
          name: status
          schema:
            type: string
            enum: [received, rejected, completed]
        - in: query
          name: receiver_id
          schema: { type: integer }
        - in: query
          name: workflow_lane
          schema:
            type: string
            enum: [normal, customization]
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: page_size
          schema: { type: integer, default: 20 }
      responses:
        '200':
          description: Warehouse receipt list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WarehouseReceiptListResponse'
  /v1/task-board/summary:
    get:
      summary: Get task-board queue summary
      description: Frontend-ready aggregate entry for role-based workbenches. Returns preset queues with queue identifiers, queue conditions, counts, sample tasks, `normalized_filters`, `/v1/tasks`-ready `query_template` metadata, and lightweight ownership-hint fields built on top of projected `workflow`, task-item `product_selection` summary, and `procurement_summary.coordination_status`. Queue aggregation uses a shared board-level candidate pool and preserves the stable external queue contract. Ownership hints are advisory only and do not introduce enforced queue ownership persistence.
      tags: [TaskBoard, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, CustomizationOperator, CustomizationReviewer, Audit_A, Audit_B, Warehouse, Admin]
      parameters:
        - in: query
          name: board_view
          description: Restricts the response to one role-oriented board. Defaults to `all`.
          schema:
            $ref: '#/components/schemas/TaskBoardView'
        - in: query
          name: queue_key
          description: When present, returns only one preset queue inside the board summary.
          schema: { type: string }
        - in: query
          name: keyword
          schema: { type: string }
        - in: query
          name: task_type
          description: Applies the same task-list filter semantics as `/v1/tasks`. Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              type: string
              enum: [original_product_development, new_product_development, purchase_task]
          style: form
          explode: false
        - in: query
          name: source_mode
          description: Applies the same task-list filter semantics as `/v1/tasks`. Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              type: string
              enum: [existing_product, new_product]
          style: form
          explode: false
        - in: query
          name: status
          description: Applies the same task-list filter semantics as `/v1/tasks`. Supports comma-separated multi-value queries.
          schema:
            type: array
            items: { type: string }
          style: form
          explode: false
        - in: query
          name: main_status
          description: Applies the same task-list filter semantics as `/v1/tasks`. Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              $ref: '#/components/schemas/TaskMainStatus'
          style: form
          explode: false
        - in: query
          name: sub_status_code
          description: Applies the same task-list filter semantics as `/v1/tasks`. Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              $ref: '#/components/schemas/TaskSubStatusCode'
          style: form
          explode: false
        - in: query
          name: sub_status_scope
          description: Applies the same task-list filter semantics as `/v1/tasks`.
          schema:
            $ref: '#/components/schemas/TaskSubStatusScope'
        - in: query
          name: coordination_status
          description: Applies the same task-list filter semantics as `/v1/tasks`. Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              $ref: '#/components/schemas/ProcurementCoordinationStatus'
          style: form
          explode: false
        - in: query
          name: creator_id
          schema: { type: integer }
        - in: query
          name: designer_id
          schema: { type: integer }
        - in: query
          name: need_outsource
          schema: { type: boolean }
        - in: query
          name: overdue
          schema: { type: boolean }
        - in: query
          name: warehouse_prepare_ready
          schema: { type: boolean }
        - in: query
          name: warehouse_receive_ready
          schema: { type: boolean }
        - in: query
          name: warehouse_blocking_reason_code
          description: Applies the same task-list filter semantics as `/v1/tasks`. Supports comma-separated multi-value queries.
          schema:
            type: array
            items: { type: string }
          style: form
          explode: false
        - in: query
          name: preview_size
          description: Number of sample tasks per queue. Defaults to `3`, max `10`.
          schema: { type: integer, default: 3 }
      responses:
        '200':
          description: Task-board queue summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskBoardSummaryResponse'
        '400':
          description: Invalid board query
  /v1/task-board/queues:
    get:
      summary: Get task-board queue tasks
      description: Frontend-ready aggregate queue endpoint. Returns preset queues with queue conditions, total counts, paginated task lists, `normalized_filters`, `/v1/tasks`-ready `query_template` metadata, and lightweight ownership-hint fields so workbenches can render inbox or task-board columns directly and drill into list view without rebuilding queue logic. Task items in these queues carry the same `product_selection` summary used by `/v1/tasks`, while detail endpoints keep the full provenance object. Queue aggregation uses a shared board-level candidate pool and preserves the stable external queue contract. Ownership hints are advisory only and do not introduce enforced queue ownership persistence.
      tags: [TaskBoard, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, CustomizationOperator, CustomizationReviewer, Audit_A, Audit_B, Warehouse, Admin]
      parameters:
        - in: query
          name: board_view
          description: Restricts the response to one role-oriented board. Defaults to `all`.
          schema:
            $ref: '#/components/schemas/TaskBoardView'
        - in: query
          name: queue_key
          description: When present, returns only one preset queue.
          schema: { type: string }
        - in: query
          name: keyword
          schema: { type: string }
        - in: query
          name: task_type
          description: Applies the same task-list filter semantics as `/v1/tasks`. Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              type: string
              enum: [original_product_development, new_product_development, purchase_task]
          style: form
          explode: false
        - in: query
          name: source_mode
          description: Applies the same task-list filter semantics as `/v1/tasks`. Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              type: string
              enum: [existing_product, new_product]
          style: form
          explode: false
        - in: query
          name: status
          description: Applies the same task-list filter semantics as `/v1/tasks`. Supports comma-separated multi-value queries.
          schema:
            type: array
            items: { type: string }
          style: form
          explode: false
        - in: query
          name: main_status
          description: Applies the same task-list filter semantics as `/v1/tasks`. Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              $ref: '#/components/schemas/TaskMainStatus'
          style: form
          explode: false
        - in: query
          name: sub_status_code
          description: Applies the same task-list filter semantics as `/v1/tasks`. Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              $ref: '#/components/schemas/TaskSubStatusCode'
          style: form
          explode: false
        - in: query
          name: sub_status_scope
          description: Applies the same task-list filter semantics as `/v1/tasks`.
          schema:
            $ref: '#/components/schemas/TaskSubStatusScope'
        - in: query
          name: coordination_status
          description: Applies the same task-list filter semantics as `/v1/tasks`. Supports comma-separated multi-value queries.
          schema:
            type: array
            items:
              $ref: '#/components/schemas/ProcurementCoordinationStatus'
          style: form
          explode: false
        - in: query
          name: creator_id
          schema: { type: integer }
        - in: query
          name: designer_id
          schema: { type: integer }
        - in: query
          name: need_outsource
          schema: { type: boolean }
        - in: query
          name: overdue
          schema: { type: boolean }
        - in: query
          name: warehouse_prepare_ready
          schema: { type: boolean }
        - in: query
          name: warehouse_receive_ready
          schema: { type: boolean }
        - in: query
          name: warehouse_blocking_reason_code
          description: Applies the same task-list filter semantics as `/v1/tasks`. Supports comma-separated multi-value queries.
          schema:
            type: array
            items: { type: string }
          style: form
          explode: false
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: page_size
          schema: { type: integer, default: 20 }
      responses:
        '200':
          description: Task-board queue tasks
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskBoardQueuesAPIResponse'
        '400':
          description: Invalid board query
  /v1/workbench/preferences:
    get:
      summary: Get saved workbench preferences
      description: Returns user-scoped saved workbench preferences plus frontend bootstrap config for preset queues. This frontend-ready route now requires a bearer session.
      tags: [Workbench, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, CustomizationOperator, CustomizationReviewer, Audit_A, Audit_B, Warehouse, Admin]
      responses:
        '200':
          description: Workbench preferences and config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkbenchPreferencesResponse'
        '401':
          description: Session-backed user required
    patch:
      summary: Save workbench preferences
      description: Saves lightweight workbench preferences for the current session-backed user. This persists queue/default-filter/page-size/sort hints only and does not introduce full inbox ownership persistence.
      tags: [Workbench, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, CustomizationOperator, CustomizationReviewer, Audit_A, Audit_B, Warehouse, Admin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkbenchPreferencesPatch'
      responses:
        '200':
          description: Updated workbench preferences and config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkbenchPreferencesResponse'
        '400':
          description: Invalid preference payload
        '401':
          description: Session-backed user required
  /v1/export-templates:
    get:
      summary: List export templates
      description: Returns the static export-template catalog for the current export-center skeleton. These templates only describe placeholder export intent over stable read models; they do not imply a real template engine or file-generation pipeline.
      tags: [ExportCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, Audit_A, Audit_B, Warehouse, Admin]
      responses:
        '200':
          description: Export template catalog
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportTemplateListResponse'
  /v1/integration/connectors:
    get:
      summary: List integration connectors
      description: Returns the static connector catalog for the current integration-center boundary. Most connectors remain placeholder-only. `erp_bridge_product_upsert` represents the narrow task business-info filing trace.
      tags: [IntegrationCenter, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Admin, ERP]
      responses:
        '200':
          description: Integration connector catalog
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationConnectorListResponse'
  /v1/integration/call-logs:
    post:
      summary: Create integration call log
      description: Persists one internal integration call log as the business/request envelope above later execution attempts. This is still mainly a placeholder/internal troubleshooting surface; it does not provide a general ERP executor, retry queue, or callback platform.
      tags: [IntegrationCenter, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Admin, ERP]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateIntegrationCallLogRequest'
      responses:
        '201':
          description: Integration call log created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationCallLogResponse'
        '400':
          description: Invalid integration call log payload
    get:
      summary: List integration call logs
      description: Returns internal integration call logs plus latest execution summaries for troubleshooting. The payload exposes `retry_count`, `replay_count`, latest retry or replay action summaries, and separate retryability or replayability reasons so retry and replay remain distinguishable on the same execution boundary. This route also serves narrow ERP filing traces; admins can filter task filing traces with `connector_key=erp_bridge_product_upsert` and `resource_type=task_erp_filing`.
      tags: [IntegrationCenter, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Admin, ERP]
      parameters:
        - in: query
          name: connector_key
          schema:
            $ref: '#/components/schemas/IntegrationConnectorKey'
        - in: query
          name: status
          schema:
            $ref: '#/components/schemas/IntegrationCallStatus'
        - in: query
          name: resource_type
          schema:
            type: string
        - in: query
          name: resource_id
          schema:
            type: integer
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: page_size
          schema: { type: integer, default: 20 }
      responses:
        '200':
          description: Integration call log list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationCallLogListResponse'
        '400':
          description: Invalid integration call log query
  /v1/integration/call-logs/{id}:
    get:
      summary: Get integration call log
      description: Returns one internal integration call log record with request/response payload snapshots, layered lifecycle timestamps, latest execution summary, separate retry/replay admission hints, latest retry/replay action summaries, and shared adapter/handoff summaries. This remains an internal trace surface, not a general integration execution platform.
      tags: [IntegrationCenter, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Admin, ERP]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Integration call log
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationCallLogResponse'
        '404':
          description: Integration call log not found
  /v1/integration/call-logs/{id}/executions:
    get:
      summary: List integration executions
      description: Internal or admin placeholder execution inspection route. Returns execution attempts beneath one call log so request-envelope lifecycle and execution lifecycle stay visibly separate. Each execution record includes the shared adapter and handoff summaries used in export and storage. This is not a real external worker timeline, callback stream, or retry queue.
      tags: [IntegrationCenter, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Admin, ERP]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Integration execution history
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationExecutionListResponse'
        '404':
          description: Integration call log not found
    post:
      summary: Create integration execution
      description: Internal/admin placeholder execution-start boundary beneath one call log. This formalizes a manual execution attempt without introducing a real ERP/HTTP/SDK executor, callback processor, or async platform.
      tags: [IntegrationCenter, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Admin, ERP]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateIntegrationExecutionRequest'
      responses:
        '201':
          description: Integration execution created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationExecutionResponse'
        '400':
          description: Invalid integration execution payload or invalid call-log state
        '404':
          description: Integration call log not found
  /v1/integration/call-logs/{id}/retry:
    post:
      summary: Retry integration call log
      description: Internal/admin placeholder retry route. `retry` is allowed only when the latest visible outcome is a retryable failed execution and creates a new execution attempt beneath the same call log. It does not introduce a real retry scheduler, queue, callback, or external executor.
      tags: [IntegrationCenter, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Admin, ERP]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IntegrationExecutionActionRequest'
      responses:
        '200':
          description: Integration call log retried
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationCallLogResponse'
        '400':
          description: Invalid integration retry payload or invalid call-log state
        '404':
          description: Integration call log not found
  /v1/integration/call-logs/{id}/replay:
    post:
      summary: Replay integration call log
      description: Internal or admin placeholder replay route. `replay` re-drives the existing call-log envelope through a new execution attempt for troubleshooting or controlled redelivery semantics, including previously succeeded or cancelled logs. This is not a real external replay engine.
      tags: [IntegrationCenter, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Admin, ERP]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IntegrationExecutionActionRequest'
      responses:
        '200':
          description: Integration call log replayed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationCallLogResponse'
        '400':
          description: Invalid integration replay payload or invalid call-log state
        '404':
          description: Integration call log not found
  /v1/integration/call-logs/{id}/executions/{execution_id}/advance:
    post:
      summary: Advance integration execution
      description: Internal/admin placeholder execution-state advancement route. This advances one persisted execution through `prepared|dispatched|received|completed|failed|cancelled` while synchronizing the parent call-log lifecycle summary. It still does not introduce a real external executor, callback processor, or retry scheduler.
      tags: [IntegrationCenter, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Admin, ERP]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: execution_id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdvanceIntegrationExecutionRequest'
      responses:
        '200':
          description: Integration execution advanced
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationExecutionResponse'
        '400':
          description: Invalid integration execution payload or transition
        '404':
          description: Integration execution not found
  /v1/integration/call-logs/{id}/advance:
    post:
      summary: Advance integration call log
      description: Backward-compatible internal or admin call-log lifecycle advancement route. `queued` requeues the parent call log directly, while `sent`, `succeeded`, `failed`, and `cancelled` reuse the explicit execution boundary so call-log lifecycle and execution lifecycle remain layered. This route does not introduce a real integration worker, callback, or retry engine. It is compatibility-only and should not be treated as the preferred execution API.
      tags: [IntegrationCenter, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Admin, ERP]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdvanceIntegrationCallLogRequest'
      responses:
        '200':
          description: Integration call log advanced
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationCallLogResponse'
        '400':
          description: Invalid integration call log payload or transition
        '404':
          description: Integration call log not found
  /v1/export-jobs:
    post:
      summary: Create export job
      description: Persists a minimal export job over an existing stable read model. This endpoint does not generate a real file yet; it only records export intent, source filters, initial `queued` status, and structured placeholder download-handoff metadata in `result_ref`. For task-query-derived exports, frontend should pass the current `query_template` and can optionally include `normalized_filters` from task-board handoff payloads.
      tags: [ExportCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, Audit_A, Audit_B, Warehouse, Admin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateExportJobRequest'
      responses:
        '201':
          description: Export job created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJobResponse'
        '400':
          description: Invalid export job payload
    get:
      summary: List export jobs
      description: Returns persisted export jobs for the current export-center skeleton. List items expose lifecycle read fields such as `progress_hint`, `latest_status_at`, `download_ready`, `can_start`, `can_attempt`, `can_retry`, `can_dispatch`, `can_redispatch`, admission reason fields (`can_*_reason`, `dispatchability_reason`, `attemptability_reason`, `latest_admission_decision`), `start_mode`, `execution_mode`, `adapter_mode`, `dispatch_mode`, `storage_mode`, `delivery_mode`, `execution_boundary`, `storage_boundary`, `delivery_boundary`, `is_expired`, and `can_refresh`, plus shared `adapter_ref_summary`, `resource_ref_summary`, and `handoff_ref_summary`, placeholder dispatch visibility through `dispatch_count` and `latest_dispatch`, placeholder execution-attempt visibility through `attempt_count` and `latest_attempt`, and lightweight audit summaries through `event_count`, `latest_event`, `latest_dispatch_event`, and `latest_runner_event`. `result_ref` remains placeholder handoff metadata only.
      tags: [ExportCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, Audit_A, Audit_B, Warehouse, Admin]
      parameters:
        - in: query
          name: status
          schema:
            $ref: '#/components/schemas/ExportJobStatus'
        - in: query
          name: source_query_type
          schema:
            $ref: '#/components/schemas/ExportSourceQueryType'
        - in: query
          name: requested_by_id
          schema:
            type: integer
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: page_size
          schema: { type: integer, default: 20 }
      responses:
        '200':
          description: Export job list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJobListResponse'
        '400':
          description: Invalid export job query
  /v1/export-jobs/{id}:
    get:
      summary: Get export job
      description: Returns one persisted export job skeleton with full placeholder download-handoff metadata plus lightweight lifecycle-audit summaries. Detail payloads also expose `can_start`, `can_attempt`, `can_retry`, `can_dispatch`, `can_redispatch`, admission reason fields (`can_*_reason`, `dispatchability_reason`, `attemptability_reason`, `latest_admission_decision`), `start_mode`, `execution_mode`, `adapter_mode`, `dispatch_mode`, `storage_mode`, `delivery_mode`, `adapter_ref_summary`, `resource_ref_summary`, `handoff_ref_summary`, `execution_boundary`, `storage_boundary`, `delivery_boundary`, `dispatch_count`, `latest_dispatch`, `attempt_count`, `latest_attempt`, `latest_dispatch_event`, `latest_runner_event`, `is_expired`, and `can_refresh` so frontend or internal tools can distinguish export-job lifecycle from dispatch handoff state, placeholder execution-attempt state, placeholder storage representation, and placeholder delivery handoff state. `result_ref` is not a real file location, signed URL, or storage integration.
      tags: [ExportCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, Audit_A, Audit_B, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Export job
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJobResponse'
        '404':
          description: Export job not found
  /v1/export-jobs/{id}/dispatches:
    get:
      summary: List export job dispatches
      description: |
        Internal/admin placeholder adapter-dispatch inspection route for export jobs. This endpoint
        returns persisted dispatch handoff records such as trigger source, adapter key, submitted /
        received / rejected / expired / not-executed status, additive dispatch-level start-admission
        hints (`start_admissible`, `start_admission_reason`), and placeholder notes so the dispatch
        boundary is explicit without pretending a real scheduler queue or worker platform exists.
      tags: [ExportCenter, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Export job dispatch history
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJobDispatchListResponse'
        '404':
          description: Export job not found
    post:
      summary: Submit export job dispatch
      description: |
        Internal/admin placeholder adapter-dispatch submit boundary for queued export jobs. This
        route persists one explicit dispatch handoff and appends `export_job.dispatch_submitted`
        audit context without creating a real scheduler queue item, worker lease, or background
        execution. Submission admission is now explicitly surfaced on export-job read models through
        `can_dispatch` and `can_dispatch_reason`; only one unresolved submitted/received dispatch is
        allowed at a time.
      tags: [ExportCenter, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateExportJobDispatchRequest'
      responses:
        '201':
          description: Export job dispatch submitted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ExportJobDispatch'
        '400':
          description: Invalid dispatch payload or invalid dispatch state
        '404':
          description: Export job not found
        '409':
          description: Export job is not in a dispatchable queued state
  /v1/export-jobs/{id}/dispatches/{dispatch_id}/advance:
    post:
      summary: Advance export job dispatch
      description: |
        Internal/admin placeholder dispatch-state advancement route. This endpoint advances one
        persisted dispatch handoff to `received`, `rejected`, `expired`, or `not_executed` without
        introducing a real scheduler callback or worker lifecycle. Dispatch state stays separate
        from both export-job lifecycle and execution-attempt lifecycle.
      tags: [ExportCenter, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: dispatch_id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdvanceExportJobDispatchRequest'
      responses:
        '200':
          description: Export job dispatch advanced
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ExportJobDispatch'
        '400':
          description: Invalid dispatch payload or invalid dispatch transition
        '404':
          description: Export job or dispatch not found
        '409':
          description: Export job dispatch is not in an advanceable placeholder state
  /v1/export-jobs/{id}/attempts:
    get:
      summary: List export job attempts
      description: |
        Internal/admin placeholder execution-attempt inspection route for export jobs. This endpoint
        returns persisted attempt records such as trigger source, execution mode, adapter key, and
        terminal attempt status, plus additive attempt-level admission hints (`blocks_new_attempt`,
        `next_attempt_admission_reason`) so current placeholder runner-adapter boundary behavior is
        visible without pretending a real scheduler, worker lease, or distributed runner platform exists.
      tags: [ExportCenter, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Export job attempt history
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJobAttemptListResponse'
        '404':
          description: Export job not found
  /v1/export-jobs/{id}/events:
    get:
      summary: List export job events
      description: Returns the export-job lifecycle audit timeline ordered oldest to newest. Event payload is audit context only and must not be interpreted as a full runner log stream or proof of real file generation/download delivery.
      tags: [ExportCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, Audit_A, Audit_B, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Export job lifecycle audit timeline
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJobEventListResponse'
        '404':
          description: Export job not found
  /v1/export-jobs/{id}/claim-download:
    post:
      summary: Claim export job download handoff
      description: |
        Claims placeholder download handoff for a ready export job. This does not start a real file
        transfer and does not return file bytes; it records a handoff-claim audit event and returns
        structured placeholder handoff metadata for frontend consumption. This action is allowed only
        when the export job is `ready` and the current placeholder handoff is not expired. Expired
        ready handoff returns a placeholder-expired invalid-state response and requires refresh.
      tags: [ExportCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, Audit_A, Audit_B, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Placeholder download handoff claimed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJobDownloadHandoffResponse'
        '404':
          description: Export job not found
        '409':
          description: Export job is not in a claimable placeholder-download state, including expired ready handoff
  /v1/export-jobs/{id}/download:
    get:
      summary: Read export job download handoff
      description: |
        Reads structured placeholder download handoff metadata for a ready export job. This endpoint
        is the current read boundary only: it does not return real file bytes, signed URLs, NAS paths,
        or object-storage references. A `download_read` audit event is appended to the existing export-job
        event chain each time this handoff metadata is read. This action is allowed only when the export
        job is `ready` and the current placeholder handoff is not expired. Expired ready handoff requires refresh.
      tags: [ExportCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, Audit_A, Audit_B, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Placeholder download handoff metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJobDownloadHandoffResponse'
        '404':
          description: Export job not found
        '409':
          description: Export job is not in a readable placeholder-download state, including expired ready handoff
  /v1/export-jobs/{id}/refresh-download:
    post:
      summary: Refresh export job download handoff
      description: |
        Refreshes expired placeholder download handoff for a ready export job. Refresh rotates the
        placeholder `result_ref.ref_key`, extends `expires_at`, appends `result_ref_updated` and
        `download_refreshed` audit events, and returns refreshed handoff metadata. This endpoint is
        placeholder-only and does not mint signed URLs, return file bytes, re-run export generation,
        or connect to NAS/object storage.
      tags: [ExportCenter, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Designer, Audit_A, Audit_B, Warehouse, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Placeholder download handoff refreshed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJobDownloadHandoffResponse'
        '404':
          description: Export job not found
        '409':
          description: Export job is not in a refreshable placeholder-download state
  /v1/export-jobs/{id}/start:
    post:
      summary: Start export job placeholder runner
      description: |
        Internal/admin placeholder runner-initiation boundary for export jobs. This route formalizes
        the `queued -> running` start contract without introducing a real async runner, scheduler,
        file generator, NAS integration, or object storage. It is allowed only when the current export
        job status is `queued`, and a latest `submitted` dispatch blocks start until it is received
        or otherwise resolved. Admission reasons are exposed through `can_start_reason` and
        `can_attempt_reason`. Successful start creates or consumes one placeholder dispatch handoff:
        if latest dispatch is `received`, start consumes it; if no startable dispatch exists, start
        may auto-create one placeholder submitted and received dispatch when no startable dispatch exists
        before creating the new attempt. This remains a skeleton only and does not imply a real
        scheduler or asynchronous dispatch platform.
      tags: [ExportCenter, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Export job placeholder runner initiated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJobResponse'
        '404':
          description: Export job not found
        '409':
          description: Export job is not in a startable queued state
  /v1/export-jobs/{id}/advance:
    post:
      summary: Advance export job lifecycle
      description: Internal or admin skeleton endpoint for manually advancing export-job lifecycle state. This endpoint updates placeholder lifecycle, execution-attempt visibility, and download-handoff metadata while writing audit-trace events. `action=start` remains available for compatibility, but `POST /v1/export-jobs/{id}/start` is the preferred explicit placeholder runner-initiation boundary.
      tags: [ExportCenter, InternalPlaceholder]
      x-api-readiness: internal_placeholder
      x-rbac-placeholder:
        auth_mode: debug_header_role_enforced
        required_roles: [Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdvanceExportJobRequest'
      responses:
        '200':
          description: Export job lifecycle advanced
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJobResponse'
        '400':
          description: Invalid advance payload or lifecycle transition
        '404':
          description: Export job not found
  /v1/tasks/{id}/warehouse/receive:
    post:
      summary: Mark warehouse receipt as received
      tags: [Warehouse, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Warehouse receipt
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WarehouseReceipt'
  /v1/tasks/{id}/warehouse/reject:
    post:
      summary: Reject warehouse receipt
      tags: [Warehouse, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Warehouse rejection receipt
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WarehouseReceipt'
  /v1/tasks/{id}/warehouse/complete:
    post:
      summary: Complete warehouse flow and move task to pending close
      description: "Requires a prior receive record, and the task must have `sku_code`. This endpoint moves the task into explicit `PendingClose` rather than closing it directly. `receiver_id` is optional and defaults to the current authenticated actor. This action uses minimum role plus org or handler gating over canonical task ownership. Non-management actors must be the current warehouse handler."
      tags: [Warehouse, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Warehouse, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                receiver_id:
                  type: integer
                  description: Optional override for compatibility. Defaults to the current authenticated actor.
                remark: { type: string }
      responses:
        '200':
          description: Warehouse flow completed and task moved to PendingClose
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WarehouseReceipt'
        '400':
          description: Task missing sku_code
        '403':
          description: "`PERMISSION_DENIED` with `deny_code` such as `task_out_of_department_scope`, `task_out_of_team_scope`, `task_not_assigned_to_actor`, or `task_status_not_actionable`."
        '404':
          description: Task not found
        '409':
          description: Invalid task or warehouse state
  /v1/tasks/{id}/customization/review:
    post:
      summary: Submit customization review for task
      description: "Dedicated customization reviewer entry. The primary `customization_job` is created at task creation. Review writes business-entered review reference data on that record (`customization_level_code`, `customization_level_name`, `review_reference_unit_price`, `review_reference_weight_factor`, `customization_note`), moves approved tasks into customization production, and keeps `return_to_designer` tasks inside the customization review lane instead of sending them through the normal design workbench. Review does not freeze execution settlement pricing."
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [CustomizationReviewer, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reviewer_id: { type: integer }
                source_asset_id: { type: integer, nullable: true }
                customization_level_code:
                  type: string
                  description: Business-entered review reference level code.
                customization_level_name:
                  type: string
                  description: Business-entered review reference level name.
                customization_price:
                  type: number
                  format: double
                  nullable: true
                  description: Business-entered review reference unit price. Not the execution freeze snapshot.
                customization_weight_factor:
                  type: number
                  format: double
                  nullable: true
                  description: Business-entered review reference weight factor. Not the execution freeze snapshot.
                customization_note:
                  type: string
                  description: Reviewer-entered business note for this review record.
                customization_review_decision:
                  type: string
                  enum: [approved, return_to_designer, reviewer_fixed]
      responses:
        '200':
          description: Customization review handled
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    allOf: [{ $ref: '#/components/schemas/CustomizationJob' }]
                    nullable: true
        '400':
          description: Invalid decision or missing required review fields
        '409':
          description: Invalid workflow state transition
        '403':
          description: Permission denied
        '404':
          description: Task not found
  /v1/customization-jobs:
    get:
      summary: List customization jobs
      description: "Lists customization work records without ERP order-detail dependency, including tasks that just entered the customization lane at creation time, pricing snapshot fields (`pricing_worker_type`, `unit_price`, `weight_factor`), current effective稿 tracking, and stored `order_no` trace."
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [CustomizationReviewer, CustomizationOperator, Ops, Designer, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: query
          name: task_id
          schema: { type: integer }
        - in: query
          name: status
          schema: { type: string }
        - in: query
          name: operator_id
          schema: { type: integer }
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: page_size
          schema: { type: integer, default: 20 }
      responses:
        '200':
          description: Customization jobs list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CustomizationJob'
                  pagination:
                    $ref: '#/components/schemas/PaginationMeta'
  /v1/customization-jobs/{id}:
    get:
      summary: Get customization job detail
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [CustomizationReviewer, CustomizationOperator, Ops, Designer, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Customization job detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CustomizationJob'
        '404':
          description: Customization job not found
  /v1/customization-jobs/{id}/effect-preview:
    post:
      summary: Submit customization effect preview
      description: "Customization-operator work entry. The first successful submission freezes pricing snapshot fields by `(employment_type + customization_level_code)` into `pricing_worker_type`, `unit_price`, and `weight_factor`. `decision_type=effect_preview` enters second review, while `decision_type=final` skips effect review and advances directly to production transfer."
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [CustomizationOperator, Designer, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                operator_id: { type: integer }
                current_asset_id: { type: integer, nullable: true }
                order_no: { type: string }
                decision_type:
                  type: string
                  enum: [final, effect_preview]
                note: { type: string }
      responses:
        '200':
          description: Effect preview submitted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CustomizationJob'
        '400':
          description: Missing or invalid pricing rule snapshot input
        '409':
          description: Invalid workflow state transition
  /v1/customization-jobs/{id}/effect-review:
    post:
      summary: Review customization effect
      description: "Effect review only accepts jobs in `pending_effect_review`; `return_to_designer` sends workflow back to effect revision, and `reviewer_fixed` may replace the effective working稿 through `current_asset_id` before advancing to production transfer."
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [CustomizationReviewer, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reviewer_id: { type: integer }
                current_asset_id: { type: integer, nullable: true }
                customization_review_decision:
                  type: string
                  enum: [approved, return_to_designer, reviewer_fixed]
                customization_level_code: { type: string }
                customization_level_name: { type: string }
                customization_price: { type: number, format: double, nullable: true }
                customization_weight_factor: { type: number, format: double, nullable: true }
                customization_note: { type: string }
      responses:
        '200':
          description: Effect review completed
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CustomizationJob'
        '409':
          description: Invalid workflow state transition
  /v1/customization-jobs/{id}/production-transfer:
    post:
      summary: Transfer customization production to warehouse QC
      description: "Production transfer requires `pending_production_transfer` job status, updates task last customization operator snapshot for warehouse reject backflow, and records bounded transfer trace fields for later robot or system integration."
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [CustomizationOperator, Designer, Ops, Admin, SuperAdmin, HRAdmin, RoleAdmin, DepartmentAdmin, TeamLead, DesignDirector]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                operator_id: { type: integer }
                current_asset_id: { type: integer, nullable: true }
                transfer_channel: { type: string }
                transfer_reference: { type: string }
                note: { type: string }
      responses:
        '200':
          description: Customization production transferred
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CustomizationJob'
        '409':
          description: Invalid workflow state transition
  /v1/tasks/{id}/events:
    get:
      summary: List task events
      tags: [TaskEvents, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Ordered task event log
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/TaskEvent'
        '404':
          description: Task not found
  /v1/code-rules:
    get:
      summary: List code rules
      tags: [CodeRules, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Admin]
      responses:
        '200':
          description: Code rules
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CodeRule'
  /v1/code-rules/{id}/preview:
    get:
      summary: Preview generated code
      tags: [CodeRules, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Preview
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CodePreview'
        '404':
          description: Rule not found
  /v1/code-rules/generate-sku:
    post:
      summary: Generate SKU code
      tags: [CodeRules, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [Ops, Admin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [rule_id]
              properties:
                rule_id: { type: integer }
      responses:
        '200':
          description: SKU generated
  /v1/sku/preview_code:
    post:
      summary: "[V6] Preview SKU code"
      tags: [V6-Legacy]
      responses:
        '200': { description: ok }
  /v1/sku/list:
    get:
      summary: "[V6] List SKUs"
      tags: [V6-Legacy]
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/SKU'
  /v1/sku:
    post:
      summary: "[V6] Create SKU"
      tags: [V6-Legacy]
      responses:
        '201':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/SKU'
  /v1/sku/{id}:
    get:
      summary: "[V6] Get SKU by ID"
      tags: [V6-Legacy]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/SKU'
  /v1/sku/{id}/sync_status:
    get:
      summary: "[V6] Frontend sequence-gap recovery"
      tags: [V6-Legacy]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: query
          name: since_sequence
          schema: { type: integer }
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/SKUSyncStatusResult'
  /v1/audit:
    post:
      summary: "[V6] Submit audit decision"
      deprecated: true
      x-removed-at: v1.3
      x-workflow-api-status: deprecated
      x-workflow-successor-path: /v1/tasks/{id}/audit/*
      x-workflow-new-usage-allowed: false
      x-workflow-target-removal-phase: candidate_for_v1_0_removal
      tags: [V6-Legacy]
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AuditSubmitResult'
  /v1/agent/sync:
    post:
      summary: "[V6] NAS agent sync"
      tags: [V6-Legacy]
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AgentSyncResult'
  /v1/agent/pull_job:
    post:
      summary: "[V6] Agent pull job"
      tags: [V6-Legacy]
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/PullJobResult'
  /v1/agent/heartbeat:
    post:
      summary: "[V6] Agent heartbeat"
      tags: [V6-Legacy]
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/HeartbeatResult'
  /v1/agent/ack_job:
    post:
      summary: "[V6] Agent ack job"
      tags: [V6-Legacy]
      responses:
        '200': { description: ok }
  /v1/incidents:
    get:
      summary: "[V6] List incidents"
      tags: [V6-Legacy]
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Incident'
  /v1/incidents/{id}/assign:
    post:
      summary: "[V6] Assign incident"
      tags: [V6-Legacy]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [assignee_id, reason]
              properties:
                assignee_id: { type: integer }
                reason: { type: string }
      responses:
        '200': { description: ok }
  /v1/incidents/{id}/resolve:
    post:
      summary: "[V6] Resolve incident"
      tags: [V6-Legacy]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason: { type: string }
      responses:
        '200': { description: ok }
  /v1/policies:
    get:
      summary: "[V6] List policies"
      tags: [V6-Legacy]
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/SystemPolicy'
  /v1/policies/{id}:
    put:
      summary: "[V6] Update policy"
      tags: [V6-Legacy]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [value, reason]
              properties:
                value: { type: string }
                reason: { type: string }
      responses:
        '200': { description: ok }
  /v1/rule-templates:
    get:
      summary: "[V6] List rule templates"
      tags: [V6-Legacy]
      responses:
        '200':
          description: Rule templates
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/RuleTemplate'
  /v1/rule-templates/{type}:
    get:
      summary: "[V6] Get rule template by type"
      tags: [V6-Legacy]
      parameters:
        - in: path
          name: type
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Rule template
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/RuleTemplate'
    put:
      summary: "[V6] Upsert rule template by type"
      tags: [V6-Legacy]
      parameters:
        - in: path
          name: type
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: string
      responses:
        '200':
          description: Rule template
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/RuleTemplate'
  /v1/tasks/pool:
    get:
      summary: List task pool entries
      description: |
        Lists R3 module-pool entries generated from `task_modules` rows in `pending_claim` state.
        This is a module claim pool, not the generic assignment/unassigned task list; use
        `GET /v1/tasks` filters for `PendingAssign` / unassigned-pool task assignment views.
        Response `data` is always an array; empty pools return `[]`.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      parameters:
        - in: query
          name: module_key
          schema: { type: string }
        - in: query
          name: pool_team_code
          schema: { type: string }
        - in: query
          name: page
          schema: { type: integer, minimum: 1, default: 1 }
        - in: query
          name: page_size
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
        - in: query
          name: limit
          description: Compatibility offset-pagination size. Prefer `page_size`.
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
        - in: query
          name: offset
          description: Compatibility offset. Prefer `page`.
          schema: { type: integer, minimum: 0, default: 0 }
        - in: query
          name: sort
          schema:
            type: string
            enum: [created_at, -created_at, updated_at, -updated_at]
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                required: [data, pagination]
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      required: [task_id, module_key, pool_team_code, priority, created_at, updated_at, task_type, task_no, title, product_code]
                      properties:
                        task_id: { type: integer, format: int64 }
                        module_key: { type: string }
                        pool_team_code: { type: string }
                        priority: { type: string }
                        created_at: { type: string, format: date-time }
                        updated_at: { type: string, format: date-time }
                        task_type: { type: string }
                        task_no: { type: string }
                        title: { type: string }
                        product_code: { type: string }
                  pagination:
                    $ref: '#/components/schemas/PaginationMeta'
  /v1/tasks/{id}/modules/{module_key}/claim:
    post:
      summary: Claim a task module
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R3
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: module_key
          required: true
          schema: { type: string }
      responses:
        '202': { description: Claim accepted }
        '4XX':
          description: Module action denied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/tasks/{id}/modules/{module_key}/actions/{action}:
    post:
      summary: Trigger a task module action
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R3
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: module_key
          required: true
          schema: { type: string }
        - in: path
          name: action
          required: true
          schema:
            $ref: '#/components/schemas/TaskModuleAction'
      responses:
        '202': { description: Action accepted }
        '4XX':
          description: Module action denied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/tasks/{id}/modules/{module_key}/reassign:
    post:
      summary: Reassign a task module within team scope
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R3
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: module_key
          required: true
          schema: { type: string }
      responses:
        '202': { description: Reassign accepted }
        '4XX':
          description: Reassign denied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/tasks/{id}/modules/{module_key}/pool-reassign:
    post:
      summary: Reassign a task module between pools
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R3
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - in: path
          name: module_key
          required: true
          schema: { type: string }
      responses:
        '202': { description: Pool reassign accepted }
        '4XX':
          description: Pool reassign denied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/tasks/{id}/cancel:
    post:
      summary: Cancel or close a task
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R3
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason:
                  type: string
                force:
                  type: boolean
                  default: false
      responses:
        '202': { description: Cancel accepted }
        '4XX':
          description: Cancel denied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/task-drafts:
    post:
      summary: Create or update a task draft
      description: |
        Source: V1_INFORMATION_ARCHITECTURE §3.5.9. Body shape mirrors `POST /v1/tasks` with an optional
        `draft_id` for update semantics; no strict validation (draft is a work-in-progress snapshot).
        Per-owner cap 20 rows (IA-A16); 7-day expiry; deleted in-transaction on successful `POST /v1/tasks`
        with `source_draft_id` (IA-A15).
      tags: [TaskDrafts, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-C
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TaskDraftPayload'
      responses:
        '200':
          description: Draft saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TaskDraft'
        '400':
          description: Malformed draft payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/me/task-drafts:
    get:
      summary: List my task drafts
      description: |
        Source: V1_INFORMATION_ARCHITECTURE §3.5.9. Returns only drafts owned by the authenticated user
        (cursor-based pagination).
      tags: [TaskDrafts, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-C
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
      parameters:
        - in: query
          name: task_type
          schema: { type: string }
          description: Optional task_type filter (e.g. `new_product`, `customer_customization`).
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 50, default: 20 }
        - in: query
          name: cursor
          schema: { type: string }
          description: Opaque cursor returned by a previous page; omit for first page.
      responses:
        '200':
          description: Draft list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/TaskDraft'
                  next_cursor:
                    type: string
                    nullable: true
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/task-drafts/{draft_id}:
    get:
      summary: Get a task draft
      description: |
        Source: V1_INFORMATION_ARCHITECTURE §3.5.9. Only the draft creator may read; DeptAdmin / SuperAdmin
        are NOT authorized (privacy). Non-owner → 403.
      tags: [TaskDrafts, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-C
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
      parameters:
        - in: path
          name: draft_id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Draft detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TaskDraft'
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Not the draft owner
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Draft not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      summary: Delete a task draft
      description: |
        Source: V1_INFORMATION_ARCHITECTURE §3.5.9. Only the draft creator may delete; non-owner → 403.
      tags: [TaskDrafts, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-C
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
      parameters:
        - in: path
          name: draft_id
          required: true
          schema: { type: integer }
      responses:
        '204': { description: Draft deleted }
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Not the draft owner
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Draft not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/tasks/batch-create/template.xlsx:
    get:
      summary: Download batch create Excel template
      description: |
        Downloads the batch-create workbook. For `new_product_development`, the Items sheet requires only `产品名称` and `设计要求`; `商品编码` (mapped to `batch_items[].product_i_id`) and `参考图` are optional but recommended when the user wants row-scoped ERP filing and image handoff. Users may paste one or more reference images into the same row; `parse-excel` extracts and uploads them server-side. For `purchase_task`, purchase-specific fields remain, and `商品编码`/`参考图` are also supported as optional row-scoped fields.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R5
      parameters:
        - in: query
          name: task_type
          schema:
            type: string
            enum: [new_product_development, purchase_task]
            default: new_product_development
      responses:
        '200':
          description: Excel template stream
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid task_type
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permission denied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/tasks/batch-create/parse-excel:
    post:
      summary: Parse a batch create Excel file
      description: |
        Parses the batch-create workbook into `batch_items`. For `new_product_development`, only `产品名称` and `设计要求` are required. If `商品编码` is filled, the backend maps it to `batch_items[].product_i_id` and validates exact membership against ERP i_id options. If users pasted images into workbook rows, the backend extracts the row-anchored pictures, uploads them through the task reference upload flow, and returns row-level `reference_file_refs`; frontend should only preview/confirm and submit the returned refs unchanged.
      tags: [Tasks, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R5
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [task_type, file]
              properties:
                task_type:
                  type: string
                  enum: [new_product_development, purchase_task]
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Excel parse preview
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/BatchCreateParseResult'
        '400':
          description: Invalid upload or parse error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permission denied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '413':
          description: File too large
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/assets/search:
    get:
      summary: Search assets across tasks
      description: Cross-task asset search for the asset management center. Source V1_ASSET_OWNERSHIP §5.2 / §5.3.
      tags: [Assets, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-A
      parameters:
        - in: query
          name: keyword
          description: Fuzzy match file_name / task_no / task title.
          schema: { type: string }
        - in: query
          name: module_key
          description: Restrict to one source module.
          schema:
            type: string
            enum: [basic_info, design, audit, warehouse, customization, procurement, retouch]
        - in: query
          name: owner_team_code
          description: Restrict to one owner team.
          schema: { type: string }
        - in: query
          name: is_archived
          description: Archive filter. Default `false`. `all` returns active + archived.
          schema:
            type: string
            enum: ['true', 'false', 'all']
            default: 'false'
        - in: query
          name: task_status
          description: Task lifecycle filter.
          schema:
            type: string
            enum: [open, closed, archived, all]
            default: all
        - in: query
          name: created_from
          description: Inclusive lower bound for asset created_at.
          schema: { type: string, format: date-time }
        - in: query
          name: created_to
          description: Inclusive upper bound for asset created_at.
          schema: { type: string, format: date-time }
        - in: query
          name: page
          schema: { type: integer, minimum: 1, default: 1 }
        - in: query
          name: size
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        '200':
          description: Asset search results
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Asset'
                  total:
                    type: integer
                  page:
                    type: integer
                  size:
                    type: integer
  /v1/assets/{asset_id}/versions/{version_id}/download:
    get:
      summary: Download a specific asset version
      description: Returns presigned download metadata for a specific historical version. Returns 410 GONE when the version has been auto-cleaned (V1_ASSET_OWNERSHIP §7.4); 404 when the asset itself is soft-deleted.
      tags: [Assets, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-A
      parameters:
        - in: path
          name: asset_id
          required: true
          schema: { type: integer }
        - in: path
          name: version_id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Asset version download metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AssetDownloadInfo'
        '404':
          description: Asset deleted or not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '410':
          description: Asset version auto-cleaned (V1_ASSET_OWNERSHIP §7.4)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/assets/{asset_id}/archive:
    post:
      summary: Archive an asset
      description: Archive asset (is_archived=1, OSS retained). SuperAdmin only; reason required. Source V1_ASSET_OWNERSHIP §7.3.
      tags: [Assets, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-A
      parameters:
        - in: path
          name: asset_id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssetReasonRequest'
      responses:
        '202': { description: Asset archive accepted }
        '403':
          description: Not SuperAdmin
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Asset not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/assets/{asset_id}/restore:
    post:
      summary: Restore an archived asset
      description: Un-archive asset (is_archived=0). SuperAdmin only. Source V1_ASSET_OWNERSHIP §7.3.
      tags: [Assets, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-A
      parameters:
        - in: path
          name: asset_id
          required: true
          schema: { type: integer }
      responses:
        '202': { description: Asset restore accepted }
        '403':
          description: Not SuperAdmin
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Asset not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/erp/products/by-code:
    get:
      summary: Lookup ERP product by code
      description: |
        Source: V1_CUSTOMIZATION_WORKFLOW §3.1.2. Backend-side wrapper of upstream `/open/combine/sku/query` (ERP 8081).
        Failure policy (§3.1.2): upstream 5xx / timeout → 502; upstream 'not found' → 404.
        Task creation callers MUST block on 404/502 and surface a retry or correction UX.
      tags: [ERP, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-C
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
      parameters:
        - in: query
          name: code
          required: true
          schema: { type: string }
          description: ERP product code (matches `tasks.erp_product_code`)
      responses:
        '200':
          description: ERP product snapshot
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ERPProductSnapshot'
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: ERP upstream reports code not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: ERP upstream 5xx / timeout
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/design-sources/search:
    get:
      summary: Search design source entries
      description: |
        Source: V1_CUSTOMIZATION_WORKFLOW §3.2.2. v1 MVP — file_name / task_id keyword full-text search,
        ordered by created_at DESC. No advanced filters; SuperAdmin-maintained independent material
        repository deferred to R7+.
      tags: [Customization, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-C
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
      parameters:
        - in: query
          name: keyword
          schema: { type: string }
          description: Full-text match against file_name and origin_task_id.
        - in: query
          name: page
          schema: { type: integer, minimum: 1, default: 1 }
        - in: query
          name: size
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        '200':
          description: Design source search results
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DesignSourceEntry'
                  total: { type: integer }
                  page: { type: integer }
                  size: { type: integer }
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/search:
    get:
      summary: Perform a global search
      description: |
        Source: V1_INFORMATION_ARCHITECTURE §4.2. Global search across tasks / assets / products / users.
        Row-level policy (R1.7-D Q1=A1 + Q2=U1): tasks/assets/products return full matches regardless
        of caller scope; `users[]` is always `[]` unless caller role ∈ {super_admin, hr_admin}.
        Backend (R1.7-D Q3=B1): v1 uses MySQL LIKE; no ES / relevance scoring in v1.
      tags: [Search, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-D
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
      parameters:
        - in: query
          name: q
          required: true
          schema: { type: string, minLength: 1 }
        - in: query
          name: scope
          schema:
            type: string
            enum: [all, tasks, assets, products, users]
            default: all
        - in: query
          name: limit
          description: Max items per result array. Default 20 (IA §4.2).
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
      responses:
        '200':
          description: Grouped search results
          content:
            application/json:
              schema:
                type: object
                required: [query, results]
                properties:
                  query:
                    type: string
                  results:
                    $ref: '#/components/schemas/SearchResultGroup'
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/me/notifications:
    get:
      summary: List my notifications
      description: |
        Source: V1_INFORMATION_ARCHITECTURE §8.3. Cursor-based pagination; optional `is_read` filter
        (omit for 'all'). Notifications are always scoped to the authenticated user; no cross-user
        reads regardless of role.
      tags: [Me, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-C
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
      parameters:
        - in: query
          name: is_read
          schema: { type: boolean }
          description: Optional filter; omit to return both read and unread.
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
        - in: query
          name: cursor
          schema: { type: string }
          description: Opaque cursor returned by a previous page; omit for first page.
      responses:
        '200':
          description: Notification list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Notification'
                  next_cursor:
                    type: string
                    nullable: true
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/me/notifications/{id}/read:
    post:
      summary: Mark one notification as read
      description: |
        Source: V1_INFORMATION_ARCHITECTURE §8.3. Only the notification owner (`user_id = current_user`)
        may mark it read; other users → 403. Repeated marking on already-read rows is idempotent.
      tags: [Me, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-C
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '204': { description: Notification marked read }
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Not the notification owner
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Notification not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/me/notifications/read-all:
    post:
      summary: Mark all notifications as read
      description: |
        Source: V1_INFORMATION_ARCHITECTURE §8.3. Scoped to the authenticated user; UPDATE targets
        `notifications WHERE user_id = current_user AND is_read = 0`.
      tags: [Me, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-C
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
      responses:
        '204': { description: All notifications marked read }
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/me/notifications/unread-count:
    get:
      summary: Get unread notification count
      description: |
        Source: V1_INFORMATION_ARCHITECTURE §8.3 (右上角 badge 用). Counts `notifications WHERE user_id =
        current_user AND is_read = 0`.
      tags: [Me, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-C
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
      responses:
        '200':
          description: Unread count
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      unread_count:
                        type: integer
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/me:
    get:
      summary: Get my profile
      description: |
        Returns the currently authenticated user's full profile. Source: V1_INFORMATION_ARCHITECTURE §7.2 (账户信息).
      tags: [Me, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-B
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
      responses:
        '200':
          description: Profile
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WorkflowUser'
    patch:
      summary: Update my profile
      description: |
        Partial self-update for the authenticated user's profile. Source: V1_INFORMATION_ARCHITECTURE §7.2 (账户信息 · 编辑 昵称/头像/手机/邮箱).
      tags: [Me, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-B
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMyProfileRequest'
      responses:
        '200':
          description: Updated profile
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WorkflowUser'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/me/change-password:
    post:
      summary: Change my password
      description: |
        Self password change. Server enforces `new_password == confirm` and that `old_password` matches the current stored hash. Source: V1_INFORMATION_ARCHITECTURE §7.2 (安全 · 改密).
      tags: [Me, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-B
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangePasswordRequest'
      responses:
        '204': { description: Password changed }
        '400':
          description: Validation failed (old_password mismatch or new_password != confirm)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/me/org:
    get:
      summary: Get my org profile
      description: |
        Read-only org snapshot for the authenticated user: department, primary team, managed scope (for DeptAdmin / TeamLead), and role set. Source: V1_INFORMATION_ARCHITECTURE §7.2 (我的组织 · 只读).
      tags: [Me, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-B
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
      responses:
        '200':
          description: Org profile
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/MyOrgProfile'
  /v1/users/{id}/activate:
    post:
      summary: Activate a workflow user
      description: |
        Enable the target user's account. Source: V1_INFORMATION_ARCHITECTURE §5.3 / §5.4 (`is_active` field).
        `TeamLead` may activate only members of own team; `DeptAdmin` only users in own department; `HRAdmin` / `SuperAdmin` global.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-B
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [SuperAdmin, HRAdmin, DepartmentAdmin, TeamLead]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '204': { description: User activated }
        '403':
          description: Forbidden (scope check failed)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/users/{id}/deactivate:
    post:
      summary: Deactivate a workflow user
      description: |
        Disable the target user's account. Scope rules identical to `activate`. Source: V1_INFORMATION_ARCHITECTURE §5.3 / §5.4.
      tags: [ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-B
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [SuperAdmin, HRAdmin, DepartmentAdmin, TeamLead]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '204': { description: User deactivated }
        '403':
          description: Forbidden (scope check failed)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/departments/{id}/org-move-requests:
    post:
      summary: Create an org move request
      description: |
        DeptAdmin in the source department initiates a cross-department user move. The request is created in state `pending_super_admin_confirm`. Source: V1_INFORMATION_ARCHITECTURE §5.2.
      tags: [Org, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-B
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [DepartmentAdmin, HRAdmin, SuperAdmin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
          description: Source department ID. Must match the user's current department and the caller's managed department (for DeptAdmin).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOrgMoveRequestPayload'
      responses:
        '201':
          description: Org move request created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/OrgMoveRequest'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden (caller is not DeptAdmin of source department, or the target user does not belong to the source department)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Department or user not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/org-move-requests:
    get:
      summary: List org move requests
      description: |
        Lists org-move-requests for the SuperAdmin review queue. Source: V1_INFORMATION_ARCHITECTURE §5.2.
      tags: [Org, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-B
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [SuperAdmin, HRAdmin, DepartmentAdmin]
      parameters:
        - in: query
          name: state
          schema:
            $ref: '#/components/schemas/OrgMoveRequestState'
          description: Filter by request state. Omit to list all states.
        - in: query
          name: user_id
          schema: { type: integer }
          description: Filter by target user.
        - in: query
          name: source_department_id
          schema: { type: integer }
          description: |
            Filter by source department. DeptAdmin callers are server-side forced to own managed department; this parameter is a safe filter hint and not a bypass.
        - in: query
          name: page
          schema: { type: integer, default: 1, minimum: 1 }
        - in: query
          name: page_size
          schema: { type: integer, default: 20, minimum: 1, maximum: 100 }
      responses:
        '200':
          description: Org move requests
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/OrgMoveRequest'
                  pagination:
                    $ref: '#/components/schemas/PaginationMeta'
  /v1/org-move-requests/{id}/approve:
    post:
      summary: Approve an org move request
      description: |
        SuperAdmin confirms a pending org-move-request. On approval the target user's `department` / `team` are updated and audit event `user_department_changed_by_admin` is emitted. Source: V1_INFORMATION_ARCHITECTURE §5.2.
      tags: [Org, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-B
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [SuperAdmin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '204': { description: Org move request approved }
        '403':
          description: Forbidden (caller is not SuperAdmin)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Request not found or not in `pending_super_admin_confirm` state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Request already decided (approved or rejected)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/org-move-requests/{id}/reject:
    post:
      summary: Reject an org move request
      description: |
        SuperAdmin rejects a pending org-move-request with a required reason. The target user remains in the source department. Source: V1_INFORMATION_ARCHITECTURE §5.2.
      tags: [Org, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-B
      x-rbac-placeholder:
        auth_mode: session_token_role_enforced
        required_roles: [SuperAdmin]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RejectReasonRequest'
      responses:
        '204': { description: Org move request rejected }
        '400':
          description: Validation failed (reason missing)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden (caller is not SuperAdmin)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Request not found or not in `pending_super_admin_confirm` state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Request already decided (approved or rejected)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/reports/l1/cards:
    get:
      summary: Get L1 report cards
      description: |
        Source: V1_INFORMATION_ARCHITECTURE §1 一级菜单「报表」. Returns the top-row report cards
        (task counts, throughput delta, etc.). RBAC (R1.7-D Q5=E1): super_admin only.
      tags: [Reports, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-D
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
        allowed_roles: [super_admin]
      responses:
        '200':
          description: Report cards
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/L1Card'
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: |
            Forbidden. `deny_code=reports_super_admin_only` when the caller role is not `super_admin`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/reports/l1/throughput:
    get:
      summary: Get L1 throughput report
      description: |
        Source: V1_INFORMATION_ARCHITECTURE §1 一级菜单「报表」 + V1_MODULE_ARCHITECTURE §12.
        Daily task throughput (created / completed / archived counts) within a [from, to] window.
        RBAC (R1.7-D Q5=E1): super_admin only.
        Backend (R1.7-D Q6=C1): v1 直查 `task_module_events` + `tasks`,不建物化表。
      tags: [Reports, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-D
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
        allowed_roles: [super_admin]
      parameters:
        - in: query
          name: from
          required: true
          schema: { type: string, format: date }
          description: Start of the report window (inclusive, ISO 8601 date).
        - in: query
          name: to
          required: true
          schema: { type: string, format: date }
          description: End of the report window (inclusive, ISO 8601 date).
        - in: query
          name: department_id
          schema: { type: integer, format: int64 }
          description: Optional filter by owning department.
        - in: query
          name: task_type
          schema: { type: string }
          description: Optional filter by task type key.
      responses:
        '200':
          description: Throughput report
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      required: [date, created, completed, archived]
                      properties:
                        date:
                          type: string
                          format: date
                        created:
                          type: integer
                        completed:
                          type: integer
                        archived:
                          type: integer
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: |
            Forbidden. `deny_code=reports_super_admin_only`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/reports/l1/module-dwell:
    get:
      summary: Get L1 module dwell report
      description: |
        Source: V1_INFORMATION_ARCHITECTURE §1 一级菜单「报表」 + V1_MODULE_ARCHITECTURE §12.
        Average and P95 dwell time per module (computed from `task_module_events`) within [from, to].
        RBAC (R1.7-D Q5=E1): super_admin only.
      tags: [Reports, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-D
      x-rbac-placeholder:
        auth_mode: session_token_authenticated
        allowed_roles: [super_admin]
      parameters:
        - in: query
          name: from
          required: true
          schema: { type: string, format: date }
        - in: query
          name: to
          required: true
          schema: { type: string, format: date }
        - in: query
          name: department_id
          schema: { type: integer, format: int64 }
          description: Optional filter by owning department.
        - in: query
          name: task_type
          schema: { type: string }
          description: Optional filter by task type key.
      responses:
        '200':
          description: Module dwell report
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      required: [module_key, avg_dwell_seconds, p95_dwell_seconds, samples]
                      properties:
                        module_key:
                          type: string
                          enum: [task_detail, design, audit, customization, warehouse]
                        avg_dwell_seconds:
                          type: number
                        p95_dwell_seconds:
                          type: number
                        samples:
                          type: integer
                          description: Number of dwell samples used for the aggregation.
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: |
            Forbidden. `deny_code=reports_super_admin_only`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /ws/v1:
    get:
      summary: Open v1 websocket channel
      description: |
        WebSocket upgrade endpoint. Bearer token authentication required (same token as REST APIs).
        Frame contract authoritatively defined in V1_INFORMATION_ARCHITECTURE §9 (触发范围 + 实现):
          - message format: `{ "type": "...", "payload": {...} }`
          - v1 event types: `task_pool_count_changed`, `my_task_updated`, `notification_arrived`
          - disconnect fallback: frontend polls every 15s
        This OpenAPI spec only describes the HTTP upgrade handshake; async frame schemas are out of OpenAPI 3.0 scope.
      tags: [Realtime, ReadyForFrontend]
      x-api-readiness: ready_for_frontend
      x-owner-round: R4-SA-C
      x-rbac-placeholder:
        auth_mode: bearer_authenticated
      responses:
        '101': { description: Switching protocols }
        '400':
          description: Websocket upgrade failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
